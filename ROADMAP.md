# JevCompass roadmap

**Read by stage:** [current pilot evidence](PILOT.md) · [20-case design (not execution)](PILOT_CASES.md) · [release/task receipts](TASKS.md) · [quick start](README.md).

Published [v0.1.21](https://github.com/acidkill/JevCompass/releases/tag/v0.1.21) removes the false remote choice between a shell executor and its test command. The Apache-2.0-licensed [GitHub releases](https://github.com/acidkill/JevCompass/releases) contain a reviewed local catalog, two default nonblocking Codex hooks, optional OpenRouter Decisions ranking, explicit recommendations, an installer, and diagnostics. PyPI v0.1.17 added explicit custom-skill curation; published v0.1.18 corrects its privacy disclosure and the four-document release gate is enforced in the existing CI job. Published 0.1.20 suppresses the generic singleton shell for package documentation after the installed 0.1.19 P05 pair tied on blind README quality. Published 0.1.19 locally filters Python test runner choices using unambiguous CI commands after 0.1.18 remotely selected pytest for a unittest-gated repository; its exact-tag Python suite, distribution check and clean registry pipx install passed. Registration requires a preview and approval of generic fields before they may be sent to OpenRouter. The bundled coding skills and agent-spawn advice remain opt-in. Neither macOS runtime nor a repeatable speed or quality gain is verified. The first four synthetic CLI pairs tied on blind quality; later focused evidence was mixed. See [PILOT.md](PILOT.md) for observations and [TASKS.md](TASKS.md) for task/commit tracking.

## 0.1.22 candidate and remaining release evidence

Published v0.1.22 places selected candidate IDs immediately below the advice ID. Installed-0.1.21 CLI probes prepared a full `exec_command`/`unittest` advisory but sometimes got an agent pre-tool report with no candidate IDs or no advice. One source-built synthetic CLI session reported both IDs before first command (9.24 ms). PR #101 passed hosted CI; tag peels to `9720cd7`, exact-tree artifact audit and Trusted Publishing run 36156580002 passed. Registry wheel package members matched local bytes. Fresh registry pipx 0.1.22 doctor found two hooks; an isolated CLI session reported both IDs and matching trace `0623c35b` before first command (5.99 ms local hook). Different model runs and a tiny sample preclude a causal effectiveness claim. Fresh Desktop prompt delivery remains open.

## 0.1.21 candidate and release evidence

Published 0.1.21 treats shell-invoked commands as companions to the executor rather than alternative tools. A clean-profile synthetic `testing/python` request against 0.1.20 took 321.15 ms and fell back locally after a remote decision request; a fresh 0.1.21 registry pipx profile composed `exec_command` and `unittest` locally in 2.96 ms. PR #99 passed hosted CI at `87bb952`, annotated v0.1.21 peels to `ecffe5e`, and Trusted Publishing run 36154620335 passed. The 13 package wheel members matched the PyPI build by bytes, and doctor found exactly two default advisory hooks. This is a narrow synthetic comparison, not a measured host or broad workflow benefit. Fresh Desktop prompt delivery, macOS runtime, and the paired efficacy study remain open.

## Release evidence to complete

- **Desktop prompt delivery:** open a new Desktop session, confirm each hook in `/hooks`, and submit a substantive task. Record the advice ID visible in the agent's context *before its first tool* and correlate it with `~/.local/state/jevcompass/advisor.jsonl`. A metric alone proves adapter execution, not agent delivery. Named-role Desktop smokes include an earlier `explorer` ID, a published-v0.1.18 `worker` ID `96b34a1d`, and a new installed-v0.1.22 `worker` ID `9ceb0fdd` naming `exec_command` and `jevcompass-focused-tests` before first tool (`SubagentStart/coding/local`, 54.64 ms). A `luna_worker` correctly stayed silent because the default matcher covers only `explorer|worker`. These do not prove prompt delivery, default-profile coverage, actual skill application, or benefit. Separate host invocation failures from intentional adapter abstention or low-signal singleton shell advice.
- **macOS runtime:** on current macOS Codex Desktop and CLI, verify fresh pipx installation, Python 3.11 and a recent Python version, hook trust and reloading, keyring behavior, optional OpenRouter choice, and unattended fallback. The lightweight PR check now uses one Linux `ubuntu-slim` container with Python 3.11; macOS is validated separately before a platform claim. Earlier six-job CI runs stopped before steps with GitHub's payment-or-spending-limit annotation, while the private repository's Actions budget was capped at $0 with spending stopped. Public hosted CI passed all 177 tests on [PR #3](https://github.com/acidkill/JevCompass/pull/3); macOS validation remains separate. A green local Linux result does not certify macOS.
- **Usability:** an early public clone of main `db7a0d3` installed from source with isolated pipx and `CODEX_HOME` on Linux; `recommend`, `install --dry-run`, `install`, and `doctor --json` passed with exactly the two advisory hooks and no PreToolUse gate. The published 0.1.19 registry package later passed clean pipx installation, two-hook doctor and a direct unittest/no-pytest recommendation. The frozen CLI pair runner now accepts exact installed 0.1.19 for a new blinded comparison. A fresh isolated CLI 0.155.1 session also reported correlated local `unittest` advice before its first tool; the fictional required suite failed an existing assertion, so no usefulness benefit follows. The maintainer-profile upgrade preserved the production `hooks.json` hash. These are setup and manual-advice checks; fresh Desktop prompt delivery, representative routine-operation coverage and macOS runtime remain open.
- **Artifact quality:** build wheel and sdist from the exact release commit, inspect members for secrets/private paths, run the full suite, and install with pipx from the built wheel. Repeat for each public candidate. The tagged v0.1.20 release passed 273 Python 3.11 tests, exact-tree distribution checks, green hosted CI and an isolated registry pipx install from PyPI after index propagation. `doctor` confirmed the two default hooks. The optional bundled skills and custom registration remain opt-in. macOS runtime and fresh Desktop prompt delivery remain unverified; subsequent commits require new evidence.
- **Privacy:** keep synthetic Decisions request checks showing only category, role, domain, and generic candidate metadata. Explicitly approved custom-skill IDs and user-authored generic descriptions may reach OpenRouter; raw SKILL.md content, prompts, source, paths, memory, and client records must not. Keep diagnostic traces locally redacted.

## Efficacy study: 20 matched cases

Design eight planning tasks, six subagent tasks, and six routine controls spanning Desktop and CLI where feasible. Run randomized equivalent baseline/advisor arms with the same model/settings and freeze blind quality scores before opening diagnostic arm mappings. For each case, assess advice relevance and availability, required checks, blocks, data minimization, and time to the first **productive** action. Compare paired outcomes and collect safe latency metrics. The first four synthetic CLI pairs tied in blind quality; later focused results were mixed. A 0.1.19 P01 pair had a post-unblind scoring correction and missing required-test receipts, so it cannot establish blinded efficacy. The subsequent CLI pilot parser separates recognized test invocations and completions from an observed exit for future pairs; old receipts cannot be retroactively resolved. In one fresh repeat, both anonymous helper artifacts passed the predeclared quality rubric; after mapping, treatment had a recognized successful unittest while baseline test execution remained unobserved. This single local-advice pair does not establish causality or the acceptance thresholds. A corrected installed-0.1.22 P07 write-task pair was blind-scored before arm mapping (score SHA-256 `345043089f5c087f98f3beb28a148959ce261fa46b754bf675e08193ab754b41`): baseline produced a coherent API contract and treatment had no authored file. Treatment abstained with `api-design/insufficient-candidates` at 5.67 ms, so the quality difference cannot be attributed to Jev advice. First productive action and other acceptance ratings remained unscored. A fresh installed 0.1.20 CLI P05 smoke confirmed `low-signal-skip` and no agent-visible advice before first tool, while a routine help check exited 0. It does not measure speed or quality. A separate installed 0.1.19 P05 documentation pair tied on blind README quality; treatment's only listed candidate was generic `exec_command`, and its test failed in the fixture while baseline test execution was unobserved. First productive action stayed unscored, so do not infer a speed effect. The sample remains small.

Adoption targets: zero blocks or disclosures; zero omitted required checks; at least 80% useful advice; at least 90% coverage of eligible events; remote Jev p95 below two seconds; and better time to first productive action. If remote selection does not help, keep the local catalog and explicit command while disabling automatic remote choice. Count manual `recommend` trials separately. Do not infer a task from the unsupported default-role subagent transcript or silently change Codex's permission policy.

## Product candidates after measured results

1. **Better relevance:** use blinded negative cases to refine local classification and reviewed `use_when`/`avoid_when`. The opt-in spawn advisor is source-tested on one CLI child using a descriptive task name; run matched useful-action trials and Desktop coverage before considering default enablement. Keep generic-shell singleton abstention. A clean profile has no reviewed API-design skill and correctly stays silent there. A source-only `jevcompass-api-contract` prototype was tested in two blinded P07 pairs and withheld: ambiguous wording gave two missing files despite treatment advice, while an explicit file request yielded a valid baseline contract and no treatment file despite local advice. The skill and catalog mapping were removed; only the clearer P07 test prompt remains. These small local-only pairs do not establish a causal effect or justify releasing the candidate. The optional two-skill prototype remains explicitly opt-in and experimental. v0.1.16 adds conditional matching-skill guidance from PR #63; a source-built C05 pair read `create-plan` but tied 6/7 on blind scores. This does not demonstrate speed or quality benefit. The installer ships as an explicit experimental opt-in after artifact and CI gates; require stronger task and Desktop evidence before recommending it broadly or enabling it by default. Never install a skill silently or claim that a configured MCP tool is callable.
2. **Coding workflow:** trial test ordering, error triage, and task priority based on coarse metadata with explicit criteria. Repository-required tests and real exit codes remain authoritative. Enable only after matched tasks show value and the data sent to Jev remains safe.
3. **Platform support:** document host-specific hook event schemas and trust behavior after macOS and Linux validation; keep Windows as a separately tracked idea, not a supported platform promise.
4. **Packaging and distribution:** production PyPI publication for `acidkill/JevCompass` completed through [workflow run 36114012972](https://github.com/acidkill/JevCompass/actions/runs/36114012972), using `pypi.yml` and the `pypi` environment, from tag commit `7e44137f3471532dfbf74d7a61c0e8946a58062f`. PyPI JSON reports wheel SHA-256 `b4331c437f1b193ffd50a3da60dcf6105e0cb50b6f085f4745f14cd5ec1bcd7a` and sdist SHA-256 `a066bef4c3fd700b938e1b9f705556d755a4d423e33a92a5653c4b6f22e27303`. A fresh isolated pipx install from the registry succeeded; `doctor` passed with the two default hooks and no `PreToolUse` gate. Continue to verify other Python versions and macOS separately before making those platform claims. TestPyPI remains a distinct `release.yml` workflow and `testpypi` environment with its own publisher and approval. The repository is licensed under Apache 2.0.
5. **Later integrations:** consider narrowly scoped permission-request advice or native Codex plugin packaging only if the advisor shows value. Any permission feature needs opt-in, separate privacy and regression tests, and must preserve Codex approval; do not restore broad `PreToolUse` gates.

Each feature moves into a release only with its own tests and host-visible evidence. JevCompass remains optional guidance, never an authority for permissions, skill use, or required validation.
