# Vanilla Codex Readiness

## Delivery rule

Target Codex Desktop and CLI in a vanilla user setup, without relying on this repository's project instructions or custom MCPs. Keep recommendations optional and non-blocking. For every completed task: run its checks, record a concise smem checkpoint, make one focused commit on `main`, push it to `origin/main`, and verify the remote head.

## Task list

| ID | Task | Status | Exit checks |
|---|---|---|---|
| VCR-01 | Establish this delivery checklist and push cadence | Complete | Checklist is tracked on `main` and its commit is on `origin/main`. |
| VCR-02 | Make prompt advice relevant in vanilla sessions and name built-in tools as Codex exposes them | Complete | Substantive prompts can receive advice independent of unreliable permission-mode signals; trivial prompts skip; configured-only MCPs are excluded; built-in command recommendations use the actual Codex tool identifier; explanation, review, planning, and testing intents select separate catalog candidates; 45 unit tests pass; fresh CLI 0.155.1 smoke delivered `exec_command` before first tool (36.8 ms local fallback). |
| VCR-03 | Make the local catalog and doctor useful on an uncustomized Codex install | Complete | CODEX_HOME-aware skill/config discovery, installer, and privacy-safe aggregate doctor status; 50 unit tests pass. Isolated pipx local-source install and upgrade pass (0.1.0 → 0.1.1); doctor reads the isolated profile and withholds paths/model values. |
| VCR-04 | Tune subagent recommendations to metadata vanilla Codex actually provides | Complete | Official docs confirm built-in `default`, `worker`, and `explorer`; SubagentStart supplies role metadata but no task text. Only `worker`/`explorer` receive role-level advice; generic/custom types and transcript/session/project paths are skipped. 50 tests pass. |
| VCR-05 | Improve relevance of broad vanilla recommendations | Complete | API design and documentation have focused pools; planning, Kubernetes, and packaging skills are excluded from unrelated coding. 52 tests pass. Safe synthetic Jev spot-checks selected only `exec_command` for generic coding and `pytest` plus `python-testing-patterns` for Python testing; these do not count toward paired acceptance. |
| VCR-06 | Complete cross-host acceptance and update product guidance | In progress | Fresh Desktop and CLI delivery checks, routine negative controls, and the paired pilot are recorded with advice IDs, first-tool ordering, usefulness, latency, and limitations; README and PILOT state match the evidence. |
| VCR-06A | Verify fresh vanilla CLI prompt-hook delivery | Complete | In an isolated read-only Codex CLI 0.155.1 profile, the first agent message contained advice ID `c218487d` and `exec_command` before the first shell tool; local fallback metric was 6.44 ms. No remote key was present, so this is not remote Jev evidence. |
| VCR-06B | Verify remote Jev selection through a fresh CLI hook | Complete | Fresh Codex CLI 0.155.1 synthetic `review/python` prompt produced agent-visible advice ID `5914ac30` before the first tool; the first response named the optional `git` and `code-review-excellence` recommendations. Matching `UserPromptSubmit` / `review` / `jev` metric: 696.03 ms. The shell exited 0 in a read-only ephemeral fixture. This proves one remote-choice delivery smoke, not recommendation usefulness or paired acceptance. |
| VCR-06C | Record correlated Desktop named-role delivery smoke | Complete | One Desktop `explorer` child reported trace `968a1d2b`, `serena`, and `code-review-excellence` before its first tool. This is one named-role smoke, not fresh Desktop prompt-hook coverage or broad acceptance. |
| VCR-06D | Score the 20-case paired pilot and decide acceptance | Planned | Complete randomized matched cases across prompt, subagent, and routine work; assess usefulness, eligible coverage, required-check completion, privacy, blocking, p95 Jev latency, and time to first useful action. |
| VCR-06E | Evaluate task-aware spawn-hook feasibility for vanilla CLI | Complete | An isolated CLI 0.155.1 PreToolUse probe saw `collaborationspawn_agent`, but the `message` field was a 204-character Fernet-shaped string with no synthetic sentinel. The task cannot be classified safely or inserted into the child context through this hook, so no production PreToolUse hook was added. |
| VCR-06F | Verify Desktop UserPromptSubmit delivery in a fresh session | Planned | In a newly started Desktop session, capture an agent-visible advice ID and selected IDs before the first tool, then match the ID to a safe local hook metric; record whether selection used local fallback or remote Jev. |
| VCR-07 | Preserve useful local advice on a vanilla profile without an OpenRouter key | Complete | A blank-profile `project-setup` pool with `exec_command` and `git` now emits a clearly labeled unranked local shortlist when Jev cannot decide; confident per-kind Jev choices remain intact. Missing-key/API failure and low-confidence behavior are tested; the suite passes 53 tests and 36 subtests; isolated no-key smoke measured 0.42 ms with no blocking fields. |
| VCR-08 | Evaluate useful role-level advice for vanilla default subagents | Complete (deferred) | Serena review found that `SubagentStart` has no task description. An isolated marker proves context delivery, not recommendation usefulness; there are no scored mixed-task advice cases. Keep `default` and custom roles silent until blinded first-action evaluation shows low-noise value. |
| VCR-09 | Make built-in command availability accurate across supported Codex hosts | Complete | `exec_command` now follows Codex's built-in shell feature default and explicit `features.shell_tool` setting instead of requiring `bash` in PATH; external `git` remains PATH-checked. Tests cover default, explicit true/false, malformed TOML, and no-bash behavior; full suite: 55 tests and 39 subtests passed. |
| VCR-10 | Review OpenRouter's Jev permission-approval integration pattern | Complete | Record what can transfer safely: permission-time triggering, deterministic local risk exclusions, typed judgments, strict allow threshold, and fail-safe return to Codex's normal prompt. Document raw-data and hook-key limitations. |
| VCR-11 | Prototype an opt-in Jev-assisted Codex permission flow | Planned | Separately evaluate a disabled-by-default `PermissionRequest` mode with a tested key source, narrow eligible command policy, no raw prompt/code/path disclosure, unchanged host deny rules, and normal approval on every uncertain/error case. Never restore broad `PreToolUse`. |
| VCR-12 | Review Pi + Jev custom-harness patterns for vanilla Codex | Complete | JevCompass already uses bounded typed judgments, local policy, fail-open advice, caching, and privacy-safe metrics. The [DAIR.AI Pi + Jev tutorial](https://academy.dair.ai/resources/jev-decisions-in-a-pi-sdk-harness) adds a useful future verifier pattern: batch typed quality/grounding judgments, keep thresholds in testable local policy, and cap retries; VCR-15 captures the opt-in and privacy boundary. Do not port the tutorial's fail-closed tool gate or model router into the current vanilla Codex hooks: the former risks disrupting ordinary work, while the latter has no supported routing point in this integration. The Codex `Stop` contract exposes the last assistant message and supports continuation, but not stable task criteria or tool evidence; transcript parsing remains unsupported. |
| VCR-13 | Evaluate native Codex plugin packaging for vanilla onboarding | Planned | After VCR-06 acceptance, prototype a plugin/marketplace package around the tested JevCompass runtime and recommendations. Verify Desktop and CLI discovery, local hook executable availability, hook trust, and OpenRouter key access; keep pipx as a documented fallback. |
| VCR-14 | Add optional secure credentials for GUI-launched vanilla Codex | Complete | Environment key remains first; optional `secure-store` extra supports only native OS stores; hook lookup has a 350 ms timeout and fails open; TTY-only auth commands redact secrets; 71 tests pass; wheel and sdist pass distribution checks and an isolated wheel install. |
| VCR-15 | Evaluate opt-in Jev-assisted answer verification | Planned | After VCR-06 acceptance, evaluate a supported `Stop` flow only with an explicit local rubric and user opt-in for any answer/evidence payload sent to OpenRouter. If useful and API-supported, batch typed `score` (answer quality) and `noul` (grounded in approved evidence) questions; keep thresholds and retry budget in a testable local policy. Low confidence or API failure must preserve normal completion, never claim verification or block; allow at most one explicitly enabled continuation, respect `stop_hook_active`, and keep required tests/checks authoritative. Do not parse transcripts; close as unsuitable without stable task criteria, approved evidence, or privacy-safe inputs. |
| VCR-16 | Reduce false positives in local task-intent classification | Complete | Regression tests keep review-with-bug language in `review`, documentation mentioning tests in `documentation`, and Bash fix/defect tasks in `debugging/software`; full suite: 72 tests and 55 subtests. No paired-pilot scores were added. |
| VCR-17 | Prevent `fix` from matching `fixture` in task classification | Complete | The debugging pattern recognizes bounded English forms (`fix`, `fixed`, `fixes`, `fixing`) without matching `fixture`; routine fixture prompts stay unclassified, and Bash fix prompts remain `debugging/software`. Full suite: 73 tests and 55 subtests. |
| VCR-18 | Prepare a synthetic matched-pair acceptance bank | Complete | The 20-case core is host-balanced 10/10: four prompts plus six routine controls on CLI, four prompts plus six supported-role subagent cases on Desktop. Three extra Desktop routine pairs are specified outside the core count. Local classifier preflight passed; no Codex arms or live score were produced. |
| VCR-19 | Extend vanilla CI coverage through supported Python 3.14 | Complete (local; hosted CI pending) | CI matrix now covers Python 3.11–3.14, matching `requires-python >=3.11`; official `actions/setup-python` supports Python 3.14. On Python 3.14.7, all 73 tests pass, wheel and sdist build, distribution contents pass inspection, and an isolated pipx install exposes the `jevcompass` CLI. GitHub Actions did not start any runner or test step because GitHub reports an account payment or spending-limit problem; hosted confirmation remains pending. |
| VCR-20 | Verify `SubagentStart` matcher in doctor | Complete | Doctor requires the installer's canonical explorer/worker matcher before marking the subagent hook present; a wrong matcher is reported missing. Focused tests and full 74-test suite pass; Graphify rebuilt. |
| VCR-21 | Exclude Python-only testing advice from web tasks | Complete | Curated pytest and Python testing skill entries carry a Python domain; the web testing pool excludes them even when locally available, while Python retains both. Focused tests and full 75-test suite pass; Graphify rebuilt. |
| VCR-22 | Align onboarding example and manual category list with runtime | Complete | README shows the actual locally selected shortlist syntax and explicitly labels its illustrative ID; the allowed category list includes `api-design`. Reviewed against `_context`, catalog entries, CLI choices, and existing local-fallback tests; 21 focused tests pass. |
| VCR-23 | Recognize substantive code-flow investigation requests | Complete | `investigate`/`investigation` map to codebase intent when no higher-priority bug signal exists; debugging retains priority for errors, and short routine requests still skip. Focused and full 76-test suites pass; Graphify rebuilt. |
| VCR-24 | Diagnose locally disabled hooks in Codex base config | Complete | `doctor` reports disabled canonical or legacy hooks feature even when both hooks are registered; no config values or paths are printed, and runtime trust remains a separate check. Focused and full 77-test suites pass; Graphify rebuilt. |
| VCR-25 | Track native Windows hook path quoting as a future idea | Deferred | [Issue #1](https://github.com/acidkill/jev-advisor-for-codex/issues/1) records the POSIX quoting concern and requires native Windows Desktop/CLI runner evidence before implementation. Current work targets macOS and Linux. |
| VCR-26 | Add synthetic pilot cases shaped like the user's Codex workflow | Complete (design only) | Three supplemental pairs cover local setup-code tracing, canonical-source review of an unsent client draft, and read-only Helm diagnostics, with privacy-safe fixture and scoring constraints. Their local classifier outcomes match the specified categories; no paired arms ran and core 20-case acceptance is unchanged. smem recall failed through MCP and CLI; file memory and repository maps informed the task. |
| VCR-27 | Route proposal and offer reviews to source-aware local advice | Complete (local) | W02 classifies as `source-review/general`; code reviews retain `review`. In a vanilla profile the pool contains `exec_command` only; a reviewed documents skill joins when installed, while Git and code-review guidance stay out. The locally composed advice calls for authoritative sources, missing facts, and unsent drafts without sending prompt or client text to Jev. 79 tests pass and Graphify rebuilt; paired usefulness remains unmeasured. |
| VCR-28 | Make W02 workflow pilot fixture executable without answer leakage | Complete (local) | Fully synthetic repository map, AGENTS rules, current/legacy prices and unsent draft are copyable into isolated arms; evaluator expectations live outside the fixture. Tests assert answer-key separation and source conflict, full suite passes, Graphify rebuilt. No Codex paired arms were run. |
| VCR-29 | Prevent `initial` from masquerading as repository initialization | Complete | Fresh isolated CLI W02 smoke exposed `project-setup` when the meta prompt mentioned `initial context`; bounded `init` matching now preserves `source-review/general` while explicit `Initialize` and `git init` remain project setup. 81 tests pass and Graphify rebuilt; the failed smoke is not delivery evidence. |
| VCR-30 | Verify W02 local advice before first CLI tool | Complete (single smoke) | In fresh isolated CLI 0.155.1 read-only session, first agent message at event 4 reported `93dcedec — exec_command` before command event 5; matching local `source-review` metric took 3.28 ms. No OpenRouter key or evaluator key was exposed to the fixture. This is one hook-trust-bypassed smoke, not paired usefulness or Desktop proof. |
| VCR-31 | Run a paired synthetic W02 CLI pilot | Complete (delivery deviation) | One randomized baseline-then-treatment CLI pair on identical fictional W02 fixtures exited 0 in both arms and satisfied all four deterministic answer indicators. Treatment logged `UserPromptSubmit/source-review/local` in 3.33 ms but the agent did not echo an advice ID before the first command, so delivery is unconfirmed. First actual command appeared at 12.32 s baseline and 14.54 s treatment; no benefit shown. The runner excludes startup `error` events from first-tool timing. This supplemental pair is not a scored core pilot case; investigate hook context delivery before claiming acceptance. |
| VCR-32 | Diagnose CLI `additionalContext` delivery with an isolated canary | Complete (CLI) | Fresh isolated CLI `gpt-6-sol` test-only hook delivered a random canary in the first assistant message before the first actual tool. A separate synthetic W02 production-hook diagnostic prompted explicit preflight and showed agent-visible advice ID `203e5703` before first tool, matching local `source-review` selection at 3.2 ms. Earlier missing echoes did not prove hook delivery failure; the runner now ignores diagnostic-only metric entries when correlating advice. Desktop delivery and 20 core pairs remain open. |
| VCR-33 | Prepare synthetic CLI core fixtures and separate evaluator key | Complete | Added fictional `tinytext` fixture with intentionally broken whitespace helper, unset Bash variable, outdated install guidance, status API requirements, and stable inputs for six routine controls. Evaluator checklist lives outside the copied fixture. Three focused pytest checks pass; no model arms run in this task. |
| VCR-34 | Build bounded randomized CLI core paired runner | Complete (runner only) | Added randomized ten-case CLI runner with identical isolated fixtures and explicit model/approval/sandbox settings per pair, baseline without hooks and treatment with advisory hooks, bounded JSON event capture, and redacted receipts. Review fixed global CLI approval-option placement and initialized a real inert `fixture-main` Git branch in an isolated configuration. Seven focused tests and 105 repository unittest cases pass; no model arms run in this task. |
| VCR-35 | Capture bounded CLI outcome checks before fixture cleanup | Complete (mechanical checks) | R03 negative control emitted no advisor metric or Jev call. Revised P01 pair completed with sandbox-observed focused unittest exit 0 in both arms; treatment local `coding` metric took 3.56 ms but ID was not echoed. First source-read heuristic was 9.28 s baseline vs 18.34 s treatment, no improvement in this pair. The evaluator never runs agent-modified Python outside Codex sandbox; missing test evidence stays unknown. Corrected R05 key to reflect no root-level Python files. Blinded usefulness and Desktop acceptance remain open. |
| VCR-36 | Execute remaining eight core CLI pairs | Complete (mechanical receipt) | Eight randomized synthetic pairs completed with both arms exit 0 and identical fixture copies. R01/R02/R04/R05/R06 treatment produced no Jev metric or advice and all answer indicators were true. P03 syntax and unset-variable indicators true both arms; treatment `debugging/local` 3.73 ms. P07 contract indicator false baseline/true treatment but `api-design/insufficient-candidates` 2.86 ms. P05 install coherence true baseline/false treatment, help true both, observed fixture tests failed both; `documentation/insufficient-candidates` 3.61 ms. No ID echoed before first tool in these arms; no blinded usefulness or Desktop scores. |
| VCR-37 | Evaluate vanilla API/documentation singleton shell advice | Complete (kept silent) | Trial addition made `exec_command` the sole vanilla candidate for documentation/API design. In a P05 preflight pair the advice ID reached the agent before its first tool (`documentation/local`, 3.31 ms), yet installation coherence was false in treatment and true baseline; first tool 8.64 s vs 7.07 s. P07 preflight pair lacked an echoed ID and both contract indicators were false. Other P05/P07 runs varied. No reliable usefulness or speed gain was shown, so the trial catalog change was removed; vanilla remains silent when no specific relevant skill/tool exists. Added optional equal-arm preflight instrumentation for future eligible cases. |
| VCR-38 | Produce blinded, privacy-bounded CLI pilot receipts | Complete (export) | Optional `--blind-dir` writes random-token per-arm JSON to `receipts/` and a separate mapping plus post-scoring agent-reported candidate IDs to private `mapping.json`. Evaluator receipts contain only case ID, coarse first-action class/time, bounded exit/outcome indicators and an explicit quality limitation. Both dirs 0700, files 0600, no overwrite/symlink traversal; stdout omits per-arm results in blind mode. 122 unittest cases and 3 fixture pytest checks pass; offline dry-run created four redacted receipts. Human blind scoring and Desktop pairs remain open. |
| VCR-39 | Verify native Desktop named-role advice delivery | Complete (one explorer smoke) | Fresh Desktop `explorer` reported advice ID `19b24b54` and `exec_command` in its initial context while locating `normalize_whitespace` and its empty-input test in a fictional fixture. Matching safe `SubagentStart/codebase/local` metric took 40.14 ms. No edits, private data or external calls. This proves one named-role delivery, not baseline/treatment usefulness or fresh Desktop UserPromptSubmit coverage. |
| VCR-40 | Smoke test blind export with live fictional CLI pair | Complete (one pair) | A fresh P01 pair using equal `gpt-6-sol` medium settings produced two blinded receipts and private mapping. Both arms exited 0 with focused unittest exit true; first-action classes/times were other 7.48 s and source_read 11.07 s. File permissions and no arm label in scorer receipts verified. No blinded usefulness, Desktop pair, or speed gain established; 0/20 fully scored. |
| VCR-41 | Reconcile blind human scores without leaking arm labels | Complete (scorer) | Offline scorer validates a precommitted score-file digest, exact opaque token pairing and strict schemas, then reports paired task quality and first productive action. Both arms accept blind task-quality ratings; recommendation usefulness remains unscored. 128 unittest cases and 3 fixture checks pass; an all-null score against the live P01 pair correctly returned incomplete with no incident gates passed; no human scores have been assigned, and overall acceptance remains incomplete. |
| VCR-42 | Repeat a workflow-shaped W02 pair and inspect usefulness signal | Complete (supplemental pair) | Randomized treatment-then-baseline fictional proposal review: both exited 0 and passed canonical price/source, missing count and intended-path indicators. Baseline passed the unsent keyword indicator; treatment did not, with answer discarded so the cause is unknown. Treatment local hook metric was 2.99 ms but no pre-tool advice ID was echoed. First commands 7.52 s vs 8.49 s do not prove productive-action improvement. No blinded score; core remains 0/20. |
| VCR-43 | Diagnose W02 advice delivery with equal-arm preflight | Complete (diagnostic) | Optional `--preflight` asks both arms to report advice ID and selected IDs before tools. Two treatment-first fictional W02 pairs yielded correlated local advice IDs `2524df67` and `f807974b` before the first command; reported selected IDs were empty, as the fallback is unranked. The second baseline reported none; both answers passed four mechanical indicators. This proves explicit trace visibility, not spontaneous tool choice or Jev selection. 15 focused tests passed; core remains 0/20. |
| VCR-44 | Show when a local catalog can actually offer a choice | Complete (diagnostic) | `doctor` now reports privacy-safe local choice capacity for source-review/general, codebase/software, and coding/python, distinguishing silence, local candidates, and a same-kind decision set. A clean CODEX_HOME on this host still inherited its custom PATH, so those counts describe the observed profile, not a universal vanilla install. Configured-only MCP entries are excluded; no remote call or recommendation quality is inferred. |
| VCR-45 | Avoid remote Jev for irrelevant generic coding alternatives | Complete (local optimization) | A live synthetic `coding/python` recommendation selected obvious `exec_command` via Jev in 561.05 ms because `smem` and `git` were also in the generic code pool. Removed only the generic `code` tag from those two entries; memory remains in planning/research and Git in review/debug/project/ops. The same recommendation now returns local singleton advice with a `local` metric at 40.65 ms and no Jev call; `doctor` reflects local candidates. Focused tests cover retained relevant categories. This is hook-decision latency evidence, not first productive action or overall usefulness. |
| VCR-46 | Silence generic shell-only subagent advice | Complete (local noise reduction) | Role-only `explorer` and `worker` events in a vanilla-like profile each had just `exec_command` and no task signal. `SubagentStart` now returns no context only in that case, with a safe `low-signal-skip` metric; prompt singletons and specific role candidates remain. Synthetic explorer and worker hook calls exited 0 with empty stdout, no stderr or Jev request; explorer metric took 41.6 ms. This reduces generic context noise but does not prove task-level speed or quality gain. |
| VCR-47 | Prevent false availability from same-name skill collisions | Complete (catalog correctness) | A standalone `gitops-workflow` or a different plugin package could falsely satisfy curated `kubernetes-operations:gitops-workflow`. Qualified skill availability now requires matching package identity in plugin layout; unqualified skills still work in Codex and agents roots. Isolated tests reproduce the impostor and matching package paths; no paths, descriptions, or private names are emitted to Jev. |
| VCR-48 | Let doctor pass a healthy keyless local setup | Complete (diagnostic semantics) | An optional OpenRouter credential is reported as unconfigured without failing overall doctor status; explicit paid live testing still requires a key. Regression covers isolated profile with installed hooks and local catalog; full suite passes. |
| VCR-49 | Synchronize the local pipx runtime and advisory hooks | Complete (local installation only) | Initial live `doctor` saw only UserPromptSubmit because the installed SubagentStart matcher still included custom `luna_worker`, unlike the current canonical matcher. Updated the existing pipx environment in place from this checkout, ran idempotent `jevcompass install`, and verified `doctor --json` sees both advisory hooks, no Jev PreToolUse, and overall ok. Smem events remained in hooks.json; installer backed up the previous file. New-session hook trust and agent-visible delivery are not established by this check. |
| VCR-50 | Export blind fictional task output for human quality review | Complete (tooling only) | Opt-in `--blind-quality-artifacts` captures bounded changed allowlisted fixture files for P01/P03/P05 and a bounded final answer for P07, keyed by opaque arm tokens without variant labels. Symlink/size/common secret checks and tests cover the boundary; existing metadata receipts and scorer schema remain unchanged. Human inspection is still required before sharing because pattern screening cannot establish absence of all sensitive text. 141 unit tests passed; synthetic fixture's pre-existing whitespace test remains intentionally failing. No new scored pairs ran at this task. |
| VCR-51 | Blindly score one fresh fictional CLI coding pair | Complete (partial pilot evidence) | Randomized P01 `gpt-6-sol` medium arms both passed focused tests. An evaluator blind to arm mapping marked both generated helper/test artifacts correct and required checks preserved; SHA-256-committed scorer reported one quality tie and `not_accepted`. Post-unblind treatment reported `exec_command`; no remote key was forwarded. First productive action, block/privacy ratings, host-wide delivery/usefulness and Desktop coverage remain unknown; 0/20 cases fully satisfy acceptance evidence. |
| VCR-52 | Blindly score remaining three CLI prompt pairs | Complete (partial pilot evidence) | P03/P05/P07 generated six private fictional artifacts. SHA-256-committed blind evaluation found three task-quality ties: P03 both pass; P05 both omit required help instruction; P07 both contract answers cover most requirements despite false keyword indicators. Post-unblind P03 treatment reported irrelevant Git/browser guidance. First productive action and live Jev choice remain unmeasured. Four CLI prompt pairs have quality scores; 0/20 fully satisfy acceptance evidence. |
| VCR-53 | Remove irrelevant Git and browser choices from shell debugging | Complete (catalog relevance) | P03 treatment reported Git and browser debugging skill for a Bash unset-variable fix. Local classifier now tags Bash/shell as `shell`; only Codex's shell tool remains a candidate. Browser diagnostic skill is scoped to web, Git to software; Python/web matching remains tested. Full unit suite and a real local advice call confirm no irrelevant candidate or remote Jev call for this synthetic shell task. No new paired benefit is claimed. |
| VCR-54 | Repeat P03 after shell candidate filter | Complete (partial pilot evidence) | Fresh equal-setting blind Bash pair: both scripts handled unset OUTPUT_PATH and passed syntax checks; committed blind ratings yielded a quality tie and `not_accepted`. After unblinding, treatment reported only `exec_command`, confirming removal of irrelevant Git/browser IDs. First source-read heuristic remained slower in treatment (9.04 s vs 7.38 s), not a productive-action measure. Acceptance remains open. |
| VCR-55 | Abstain from generic shell-only advice | Complete (noise reduction) | Fresh P01 and P03 blind CLI pairs produced task-quality ties; no first-action speed benefit was observed from generic `exec_command` advice. Both automatic hooks and explicit recommend now stay quiet when that is the sole candidate, before a Jev request. Specific singleton and multi-candidate choices remain eligible. Focused and full suites pass; direct explicit `coding/python` recommendation reports no actionable choice. This is a product relevance decision, not a measured speed gain. |
| VCR-56 | Recommend stock OpenAI docs for narrow Codex questions | Complete (local catalog) | A stock `.system/openai-docs` skill is curated only for Codex documentation/troubleshooting with a narrow Codex domain; generic Python/web tasks mentioning Codex remain outside it. Blank CODEX_HOME does not fabricate availability. Direct local `debugging/codex` advice lists `openai-docs` and `exec_command` without a Jev round trip; 147 unit tests and graphify rebuild passed. Fresh agent-visible delivery and usefulness are not inferred. |
| VCR-57 | Synchronize local pipx runtime with current catalog | Complete (local install) | In-place pipx Python package update from checked-out source preserved existing hooks and secure-store dependencies. Installed `doctor --json` reports 20 curated entries, both advisory hook registrations, no Jev PreToolUse gate, overall ok; installed explicit `debugging/codex` now lists stock `openai-docs`. No new hook trust or agent-visible delivery evidence is inferred. |
| VCR-58 | Give Python package install documentation a useful local check | Complete (local relevance; benefit contradicted in VCR-59) | Narrow `package-docs/python` classification adds a checklist to compare README install steps with project metadata, actual CLI help, and the repository test command. Installed `python-packaging` becomes an optional specific candidate; a blank profile does not fabricate it. Ordinary Python/web docs remain unchanged. 151 tests and graphify rebuild pass; one local direct call returned the skill and checklist. The first fresh P05 pair found baseline better; see VCR-59. |
| VCR-59 | Blindly test package-docs guidance on fresh P05 CLI pair | Complete (negative result) | The SHA-256-committed evaluator rated baseline README better: it retained the required module-form help command, while treatment changed to `tinytext --help`; both arm test commands failed on the deliberately broken fixture. Treatment reported `exec_command`; first source-read heuristic 10.22 s versus baseline 7.39 s. The generic checklist wording is a hypothesis for the deviation, not proven cause. Acceptance remains open and guidance needs correction or removal. |
| VCR-60 | Correct ambiguous package help guidance | Complete (hypothesis tested once) | Local package-docs checklist now says to preserve the existing README's exact supported CLI invocation unless a replacement is run and verified, and to distinguish pre-existing test failures. It does not embed fixture package names. 151 tests and graphify rebuild pass; one subsequent P05 treatment was better, but advice delivery was not confirmed. |
| VCR-61 | Blindly retest revised package-docs guidance | Complete (partial positive evidence) | A fresh P05 pair received committed blind quality ratings: treatment README preserved module-form help and coherent install steps, baseline did not; both test-command exits failed on the intentionally broken starter. Neither arm echoed advice ID or candidates before first tool, so causal attribution and delivery remain unverified. First-action heuristic 8.94 s vs 9.31 s cannot establish speed. Acceptance remains open. |
| VCR-62 | Synchronize installed runtime with revised package guidance | Complete (local install) | In-place pipx update from current checkout preserved advisory hook registrations. Installed doctor reports 20 curated entries, both hooks, no Jev PreToolUse, overall ok; installed explicit `package-docs/python` returns the verified-invocation checklist and optional installed `python-packaging`. New-session hook trust and agent-visible delivery remain unverified. |
| VCR-63 | Distinguish missing hook invocation, deliberate skip, and emitted but unreported advice in private CLI pilot | Complete (local tooling) | Treatment enables bounded hook diagnostics; the private 0600 sidecar keeps only opaque arm tokens, allowlisted category/status, duration, and a before-first-tool report flag. Blinded receipts remain unchanged. Scorer validates the sidecar and preserves missing-delivery evidence for unreported advice. 154 tests pass with `PYTHONPATH=src`; host delivery and usefulness remain open. |
| VCR-64 | Run fresh P05 CLI blind pair with private hook diagnostics | Complete (single pair) | Baseline/treatment randomized with identical gpt-6-sol medium settings and preflight; no OpenRouter key or other hooks. Blind scores committed before unblinding (SHA-256 39d8d021a3d46d8f109bd68800bc9bae835b6927d9dd3662ebf62c1152e31710). Both README artifacts meet task quality and required checks; the starter test failed in both. Treatment `package-docs/local` advice was emitted in 3.64 ms and its ID reported before first tool. Privacy rating and productive-action time remain unknown; quality tie, no demonstrated speed gain, overall acceptance not passed. |
| VCR-65 | Run fresh P03 Bash repair pair with private diagnostics | Complete (single pair) | Randomized P03 CLI pair, identical gpt-6-sol medium and preflight; no OpenRouter key or other hooks. Blind score schema was corrected before unblinding and the valid bytes committed at SHA-256 aab2de8a82e8039244a77e35c33fc48bc38d3decb1ef1e8d466a64d346c6a6ad. Both scripts passed Bash syntax and unset/empty OUTPUT_PATH behavior; quality tie. Treatment invoked the hook, then `debugging/low-signal-skip` in 5.48 ms; no advice emitted. Privacy, block, and productive-action ratings remain unknown. |
| VCR-66 | Scope first alpha to four pairs and move later work to roadmap | Complete (release scope) | `ROADMAP.md` details the first alpha gate, full 20-case adoption study, cross-host coverage, future coding use cases, packaging, and platform scope. README has a short `Soon available:` section and distinguishes the four-pair release sample from full acceptance. |
| VCR-67 | Run first-alpha P01 blind coding pair | Complete (single pair) | Fresh randomized synthetic CLI pair on identical gpt-6-sol medium/preflight settings, with no OpenRouter key and no other hooks. Blind score SHA-256 534a7df152fb90810c576ba2ac98ac5f4f8c895b52586feef53e2ea9dd032446 was frozen before unblinding. Both arms passed task quality and checks; productive-action, blocks, and privacy remain unscored. Treatment invoked hook then `coding/low-signal-skip` in 1.75 ms, correctly avoiding generic singleton advice; no speed/usefulness claim. |
| VCR-68 | Redact incidental local paths from P07 blind final-answer artifact | Complete (local fix) | Two P07 attempts failed closed because the agent final answer contained a local path; no blind output was written. The builder now replaces only recognized local absolute paths before validation, while credentials, prompts, and other private content still fail closed. 154 tests pass and graphify rebuilt. A fresh P07 pair was then completed in VCR-69. |
| VCR-69 | Complete fourth first-alpha P07 blind pair | Complete (single pair) | Fresh randomized synthetic CLI pair from c53b5dc, equal gpt-6-sol medium/preflight, no OpenRouter key or other hooks. Blind score SHA-256 84df6c0d038499d7f13ce570bbfe4d119fb87145c370d952adea8424255ea638 froze before unblinding. Both substantive API contracts met task quality; no required test was specified, productive-action time unknown. Treatment invoked hook and returned `api-design/insufficient-candidates` in 3.73 ms, without advice. Four fresh pairs (P01/P03/P05/P07) now have blinded quality scores, all ties; the 20-case adoption gate remains open. |
| VCR-70 | Ship private v0.1.0 alpha with four-pair evidence | Complete (private release) | Tagged commit 4f83d3d, attached verified wheel/sdist with GitHub SHA-256 digests, and published a private prerelease. 154 local tests, distribution-content check, isolated Linux pipx install, idempotent two-hook setup, and doctor passed. GitHub Actions jobs did not start because account billing/spending limit blocked runners; macOS and broad efficacy remain unverified. TestPyPI/PyPI and public visibility remain separate. |
| VCR-71 | Verify downloaded release wheel in a fresh vanilla profile | Complete (release asset) | Authenticated download of the private v0.1.0 wheel matched GitHub API SHA-256 `64462c751749fb153b470cd4b0330365ee79be8cbd75962d30d99094fa5b0f12`. Fresh isolated pipx install, two installer runs, doctor 0.1.0 PASS, and keyless local unranked recommend succeeded; profile had one UserPromptSubmit, one SubagentStart, and zero PreToolUse. This validates the distributed Linux artifact, not Desktop delivery or macOS runtime. |
| VCR-72 | Confirm actual released-wheel advice delivery in fresh CLI | Complete (single smoke) | A fresh isolated Codex CLI session used the authenticated downloaded v0.1.0 wheel, no OpenRouter key, and only JevCompass hooks. First assistant event 4 reported advice ID `3dfbb31a` and `exec_command`/`git` before first tool event 5; local `project-setup/local` metric matched in 31.31 ms and tool exited 0. Safe metric had only category/status/duration/trace/event fields. Isolated hook trust was bypassed with the documented CLI flag; normal host trust, Desktop prompt delivery, and usefulness remain unverified. |
| VCR-73 | Probe current Desktop explorer role after alpha | Complete (negative/ambiguous smoke) | A fresh native `explorer` child reported no initial JevCompass ID or candidate IDs before its first tool; it answered a read-only synthetic fixture question. The installed `SubagentStart` matcher includes explorer, but no contemporaneous safe metric was identifiable, so hook invocation versus intentional low-signal abstention is unresolved. Earlier positive Desktop smoke used a previous catalog and does not prove current role delivery. Diagnose hook reload/trust and invocation separately; no release acceptance credit. |
| VCR-74 | Separate installed adapter abstention from Desktop invocation | Complete (targeted local diagnostic) | Confirmed active hook command points to pipx JevCompass 0.1.0 and its current metric path under `~/.local/state/jevcompass/`; a separate legacy `~/.codex/log/jev-advisor.jsonl` is not authoritative. Direct synthetic `SubagentStart/explorer` with no OpenRouter key exited 0, emitted no advice/stderr, and appended `codebase/low-signal-skip` in 48.46 ms. This proves current adapter abstention works, but it cannot prove the Desktop host called the hook during VCR-73. |
| VCR-75 | Reject stale release artifacts | Complete (local gate) | The distribution checker compares wheel and sdist modules, catalog, version and README metadata with current source; stale checked-in `dist/` artifacts now fail. Six new unittest cases cover fresh and stale archives. Full 160-test suite and a fresh wheel/sdist build pass; this protects future release builds, not the immutable v0.1.0 assets. |
| VCR-76 | Separate fresh Desktop child delivery from installed adapter behavior | Complete (negative host probe) | A fresh native `explorer` child reported no JevCompass advice ID or tool ID in initial context before its first `functions.exec` call. The active safe metric file had no new entry (mtime remained 18:15:53 +02:00 after the 18:26 probe), while `hooks.json` still registered `^(explorer|worker)$` and config retained a trusted hash for `subagent_start`. No evidence that Desktop invoked the hook; verify host reload and hook event coverage in a fresh session before counting delivery. The child found `normalize_whitespace`, not a greeting, in the synthetic fixture. |
| VCR-77 | Show hook invocation separately from installation in doctor | Complete (local implementation) | `doctor` now reads at most 64 KiB of the safe local metric tail and reports only allowlisted event/status and log modification age. It distinguishes a registered hook from an observed adapter invocation and explicitly does not claim agent-visible delivery. Nine focused tests and 162 full-suite tests pass; a source-tree doctor returned a `low-signal-skip` observation. A packaged release and fresh Desktop host run remain future verification. |
| VCR-78 | Prepare private v0.1.1 diagnostics update | Complete (local package gate) | Version and private download instructions updated. Full 162-test suite passed; fresh wheel/sdist passed byte-for-byte source/metadata inspection; isolated Linux pipx install exposed CLI and returned a local project-setup shortlist. Wheel SHA-256 `0a118b6ce608a26c9612bc7502ca13980c3bdd756644d03a02656874d4432d93`, sdist SHA-256 `03fc2a89c571e5514d25cf6ace3de32d0128876a779f99d227a4cdbe9a4398d3`. These are pre-commit local build hashes; the committed-source release build has its own verified hashes in VCR-79. |
| VCR-79 | Publish and verify private v0.1.1 alpha | Complete (private release) | Tag `v0.1.1` resolves to `0e961fe6a80877afe7650a1645118e614d11883b`; GitHub reports private prerelease with wheel/sdist attached. Fresh committed-source build passed distribution freshness inspection. GitHub asset digests match release artifacts: wheel `cc87832baf7f2f3f8c545c4a290f3ff91ba493b8c8c9c0806705995cad89cf6e`, sdist `572ae8d9e2492176584957f5c3c92a98824874bdd173ed711455ea96ca576bc1`. Authenticated wheel download matched digest and installed in isolated Linux pipx profile. Hosted CI, macOS, and fresh Desktop advice delivery remain unverified. |
| VCR-80 | Upgrade normal host profile and isolate Desktop hook path | Complete (negative host probe) | Upgraded the existing pipx JevCompass environment from 0.1.0 to the authenticated v0.1.1 wheel without replacing hooks or optional extras. Installed `doctor` passed with both advisory registrations and no PreToolUse gate. A fresh native Desktop `explorer` child reported no advice ID before its first tool and produced no concurrent safe metric (log mtime unchanged at 18:41:04 +02:00). Direct invocation of the same installed `SubagentStart/explorer` hook exited 0 and recorded `codebase/low-signal-skip` in 49.78 ms. Thus adapter abstention works locally; host invocation and delivery remain unverified. |
| VCR-81 | Resolve JevCompass SubagentStart trust state | Complete (CLI trust; Desktop retest pending) | In Codex CLI 0.155.1 `/hooks`, `SubagentStart` showed Installed 1 / Active 0 / Review 1. Its detail identified the exact pipx JevCompass command, `^(explorer|worker)$` matcher, 2 s timeout, and modified-since-trusted state. Reviewed and trusted only this hook; UI then showed Trusted and enabled. A child started in the already-running Desktop task still reported no advice and no matching metric; a new Desktop host session is needed to establish whether fresh loading invokes it. README now explains targeted trust review and delivery verification. |
| VCR-82 | Verify trusted SubagentStart in a fresh CLI session | Complete (invocation, intentional abstention) | A fresh CLI 0.155.1 ephemeral read-only run used the normal hook trust path (no bypass), a synthetic tinytext fixture, and a native explorer child. The installed JevCompass hook recorded `SubagentStart` / `codebase` / `low-signal-skip` / 51.99 ms with trace `1e5b28df`. The child reported no initial advice before its first tool, as expected from generic singleton suppression. The parent made no edits and exited 0. This proves fresh CLI invocation after trust but not helpful advice for a vanilla explorer or current Desktop invocation. |
| VCR-83 | Verify installed v0.1.1 prompt delivery and local-wheel upgrade | Complete (CLI smoke) | `pipx runpip` left pipx metadata at 0.1.0 despite 0.1.1 runtime; `pipx install --force` failed with the uv-managed existing venv. Clean pipx uninstall/install of the authenticated wheel made both metadata/runtime 0.1.1, preserved `hooks.json` byte-for-byte, and passed doctor with two advisory hooks. A fresh CLI 0.155.1 keyless read-only synthetic project-setup prompt (normal hook trust, no bypass) reported ID `59a04798` and `exec_command`, `git`, `create-plan`, `python-packaging` in its first response (event 4), before first tool (event 6). Matching `UserPromptSubmit/project-setup/local` metric was 100.15 ms. This proves one CLI advice delivery, not Jev remote selection, task benefit, or Desktop delivery. README documents the tested wheel upgrade path. |
| VCR-84 | Confirm installed v0.1.1 remote Jev advice before first CLI tool | Complete (single remote smoke) | An explicit synthetic billed `doctor --test-jev` passed, followed by a fresh normal-trust CLI 0.155.1 read-only review of tinytext. Agent event 4 reported ID `72576fcd` with selected `git` and `developer-essentials:code-review-excellence`; first tool event 5 followed. Matching `UserPromptSubmit/review/jev` metric was 437.21 ms. No edits. This proves one remote decision and delivery, not paired usefulness, speed improvement, macOS, or Desktop coverage. |
| VCR-85 | Make CI unittest gate discover all fixture tests without pytest | Complete (local matrix) | GitHub Actions run `36031171344` still started zero steps; a check annotation cites account payment or spending-limit failure. Local clean Python 3.11 revealed `tests/test_cli_core_fixture.py` imported pytest, although CI installs only `build` and runs `unittest discover`. Converted its three fixture checks to unittest, preserving assertions and removing pytest dependency. Full local discovery now passes **165 tests** on Python 3.11, 3.12, 3.13, and 3.14; hosted CI remains unverified until account billing is fixed. |
| VCR-86 | Verify cross-version packaging and isolated pipx installation | Complete (local) | From main `3626d387`, all **165 unittest cases** pass on Python 3.11–3.14. Each interpreter built a wheel and sdist (8 artifacts total); `tools.check_distribution.check()` accepted every artifact. An isolated Python 3.11 pipx installation of the local wheel reports package and pipx metadata version **0.1.1**, and a keyless `recommend --category project-setup --domain software` returns a local shortlist. No global hooks or configuration changed. Hosted CI remains unverified: latest run `36031711897` failed before steps amid the account payment/spending-limit issue. |
| VCR-87 | Distinguish current Desktop child silence from hook invocation | Complete (diagnostic) | A new `explorer` child reported no initial advice ID or selected IDs; the safe metric gained no `SubagentStart` event for it. `doctor` reports registered hooks but cannot prove Desktop activation. The current profile has only `exec_command` for `codebase/software`, so a direct adapter invocation would deliberately abstain. Record both facts without calling this a fresh-host delivery failure; next run requires a fresh Desktop host reload and an eligible profile. See `PILOT.md`. |
| VCR-88 | Add a macOS test and packaging gate to CI | Complete (workflow configured; hosted run pending) | CI keeps Ubuntu Python 3.11–3.14 and adds macOS latest on 3.11 and 3.14, using the same unittest, build, and distribution checks. Local YAML parse and six-entry matrix assertion pass. GitHub Actions cannot currently start jobs because of the account payment/spending-limit condition, so this does not establish macOS compatibility. |
| VCR-89 | Keep edits and tests in an existing project out of project setup advice | Complete | A prompt to create Python tests for this project was incorrectly classified as `project-setup`. Project creation now requires a setup verb to reach the target project/repository/package before an intervening test, feature, function, file, script, documentation, module, or component target. Regression cases distinguish creating tests/features inside an existing project from a new package/repository. All 165 unittest cases pass on local Python 3.14. |
| VCR-90 | Label local commands accurately in agent advice | Complete | `git` and `pytest` were previously rendered as separate Codex tools. Command-backed catalog entries now retain their internal choice group but render as local shell commands run via `exec_command`, with an explicit active-session availability caveat. Built-in Codex tools and skills keep their labels. Regression tests and the full 166-case unittest suite pass on Python 3.14. |
| VCR-91 | Release JevCompass v0.1.2 as a private GitHub prerelease | Complete (private release verified) | Commit/tag target `79bbb896359bbdcd8034f4bf16b6109211bc9007`; Peeled `v0.1.2` matches the source commit; `origin/main` later advanced with the task-log commit. Private prerelease: https://github.com/acidkill/jev-advisor-for-codex/releases/tag/v0.1.2. All 166 unittest cases pass on Linux Python 3.14.7; the fresh wheel/sdist and independent remote downloads pass `tools.check_distribution.check()` and are byte-identical. GitHub SHA-256: wheel `097752467e81b55fef041e445ec8425b19b8ce844961c172f065669279552669`; sdist `7392c2c2ededb8cedfc6cfae3fceeea3f88575be64f22ac50228bf7212c89c8e`. Remote wheel installs in isolated pipx with runtime and metadata version 0.1.2; keyless doctor passes and recommend returns the local shortlist. Hosted CI, macOS, and fresh Desktop delivery remain unverified. smem recall/remember CLI both failed with the existing SurrealDB root-authentication error; this TASKS checkpoint is the local receipt. No PyPI/TestPyPI publish. |
| VCR-92 | Upgrade active pipx runtime and diagnose v0.1.2 CLI advice | Complete (CLI smoke; relevance defect found) | Downloaded private v0.1.2 wheel SHA-256 `097752467e81b55fef041e445ec8425b19b8ce844961c172f065669279552669`; normal host pipx runtime and metadata now report 0.1.2. `jevcompass install` left `hooks.json` SHA-256 `31cfe6a4f766aefa4bd73457891449b70f8dba34016de93c051547974fdd1b16` unchanged, retaining smem hooks and two advisor hooks. Keyless local recommendation succeeds; doctor initially failed a transient public model metadata check, then passed on retry without a paid request. A fresh read-only CLI review reported advice ID `dab8fa6b` before its first tool, matched `UserPromptSubmit/source-review/local` at 55.24 ms, but suggested `documents`: the preflight verb `quote` falsely triggered price-quote classification. This is delivery evidence and a relevance defect, not a useful review recommendation. See `PILOT.md`; fix tracked in VCR-93. |
| VCR-93 | Keep the verb quote out of price-quote review routing | Complete (local) | The v0.1.2 CLI smoke classified a code review as `source-review` when the measurement prompt asked the agent to quote an advice ID. Source-review routing now requires an explicit price/pricing/client/commercial quote or another offer artifact term; plain quote remains ordinary review. Regression cases cover both intents, and all 166 unittest cases pass on Python 3.14. Installed v0.1.2 still contains the old classifier; include this fix in a subsequent release. |

## Latest task checkpoint (2026-09-24)

VCR-06A is complete: isolated vanilla CLI local advice ID `c218487d` and `exec_command` arrived before the first shell tool (6.44 ms). VCR-06B is complete for one fresh CLI remote-choice smoke: ID `5914ac30` and the `git` / `code-review-excellence` recommendations appeared before the first `exec_command`; matching `UserPromptSubmit` metric was `review` / `jev` / 696.03 ms. VCR-06C is complete for one correlated Desktop `explorer` smoke: trace `968a1d2b`, `serena`, and `code-review-excellence` were reported before the first tool. VCR-06E found no safe task text in the spawn pre-hook, so no third production hook was added. VCR-06D and VCR-06F remain planned; paired usefulness, the fresh Desktop prompt test, and the 20-case pilot remain open. VCR-05 and Graphify results remain in prior checkpoints. VCR-16 corrected local review/debugging priority, documentation/test priority, and Bash/shell domain detection; VCR-17 then narrowed the `fix` token so routine `fixture` requests stay silent. VCR-18 prepared a synthetic matched-pair bank with a 20-case core balanced 10/10 across hosts plus three supplemental Desktop routine pairs. Local preflight verified eight prompt category/domain outcomes and nine routine skips; no Codex arms or live scores have been produced. These remain classifier and test-design evidence, not pilot results. The prior six CLI routine pairs inspected the private JevCompass repository with custom smem hooks and are excluded from this synthetic acceptance set. smem checkpoint synchronization remains deferred during the planned Surreal-Memory benchmark window; this task list and `PILOT.md` retain the local evidence, and no server investigation or changes were made. VCR-12 reviewed the Pi + Jev custom-harness article: the safe decision patterns are already present, while automatic model routing, broad tool gating, and transcript-based answer grading are deferred or rejected for vanilla Codex. VCR-13 tracks a later native Codex plugin packaging evaluation after core cross-host acceptance. VCR-14 adds optional OS keyring access for GUI-launched Desktop sessions: `OPENROUTER_API_KEY` still wins, only native Keychain/Secret Service/KWallet/Windows backends are accepted, and the hook helper times out after 350 ms to preserve local advice. The interactive `jevcompass auth` commands never place the key in arguments, configuration, or output. All 73 tests and 55 subtests pass after VCR-17; VCR-14's wheel/sdist checks and isolated `auth status` check remain recorded above. The smem recall attempt for this task returned the previously observed authentication error; this checkpoint is retained locally and no Surreal-Memory diagnosis or changes were made. Graphify was rebuilt with its pipx-managed interpreter because `graphify-out/.graphify_python` is absent.

## Vanilla usability backlog (2026-09-24)

A read-only catalog audit identified three vanilla-Codex considerations: keyless profiles silently lost ambiguous multi-candidate recommendations; `SubagentStart` does not include task text for a generic `default` child; and shell-based availability probing may hide Codex's built-in command tool on hosts without `bash`. VCR-07 and VCR-09 are complete. The VCR-08 evaluation is complete with a decision to keep generic and custom roles silent until blinded mixed-task testing demonstrates low-noise value. VCR-07's isolated keyless smoke returned `exec_command` and `git` as an unranked local list (0.42 ms); 53 tests and 36 subtests passed. VCR-09 now follows Codex's built-in shell default and explicit disablement without depending on `bash`; the full suite passes 55 tests and 39 subtests. These synthetic/local checks do not establish live agent delivery. VCR-06's fresh Desktop prompt test and paired acceptance pilot remain open; remote Jev selection is proven for one CLI smoke, not usefulness or cross-host coverage.

Optional credential fallback for vanilla GUI-launched Codex is implemented in VCR-14. The environment variable remains the preferred source; an opt-in native OS keyring extra offers an interactive setup path, strict backend allowlisting, short bounded lookup, redacted diagnostics, and local fallback on errors. No real key was stored.

## Jev permission-flow article review (2026-09-24)

Reviewed [OpenRouter's Jev permission-approval recipe](https://openrouter.darenbot.com/docs/cookbook/coding-agents/auto-approve-permission-prompts-with-jev) against [Codex hook documentation](https://learn.chatgpt.com/docs/hooks). Transferable pattern: evaluate only a real `PermissionRequest`, run local deterministic exclusions before Jev, use typed bounded judgments, allow only after every required score clears a validated threshold, and leave the original Codex prompt untouched on uncertainty, timeout, or API failure. Codex's `PermissionRequest` runs only when approval is about to be requested; if no hook decides, Codex continues its normal approval flow.

Important limits: the Codex event has no reliable task objective; the article therefore drops task-fit and asks only about reversibility. The recipe sends command text and project path to OpenRouter and reports that its hook did not receive `OPENROUTER_API_KEY` in a CLI 0.155.1 capture; its `.env` workaround is not adopted here. A future permission mode must solve key delivery safely, avoid raw prompts/code/paths, preserve Codex deny rules, keep high-risk actions on the normal prompt, and remain disabled unless a user explicitly enables that distinct capability. Its quoted latency, accuracy, and cost figures are the article author's measurements, not JevCompass results.

## Acceptance guardrails

- Jev recommendations never grant permissions or block prompts, tools, or subagents.
- Classify prompt intent locally; send only approved coarse metadata and candidate descriptions to OpenRouter.
- Never send raw prompts, code, paths, memory contents, or user-specific skill text.
- A configured MCP entry is not evidence the active session exposes its functions.
- Advice names model-facing tools such as `exec_command`; tool hooks use Codex's canonical matcher name `Bash` for that shell channel.
- Required project instructions, validation, and authoritative test results remain in force.
- If a host does not provide sufficient event data, record the limitation and use the explicit `jevcompass recommend` command where appropriate.
