# Synthetic status endpoint requirements

Design a contract for `POST /status` that accepts a JSON request and returns a
validated status result. Cover required and optional inputs, successful results,
invalid input, and server errors. No server or network access is part of this
fixture.
