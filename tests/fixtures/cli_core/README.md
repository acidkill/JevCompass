# Tinytext

Tinytext is a fictional Python package for small text helpers.

## Install

Install with `python setup.py install`.

## Command line

Run `python -m tinytext --help` to see the current command-line options.

## Tests

Run tests with `python -m unittest discover -s tests`.

The report renderer supports an optional timeout setting.
