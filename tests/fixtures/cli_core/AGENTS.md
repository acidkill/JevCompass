# Synthetic CLI core fixture

This temporary project is fictional. Keep all edits inside this project.
Do not access networks, credentials, external services, or files outside it.
For P03, fix the unset-variable defect and run `bash -n scripts/render_report.sh`.
For documentation and API design tasks, make the requested changes in this project.
