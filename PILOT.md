# JevCompass acceptance pilot

## Status

**Acceptance: not yet passed.** The initial four synthetic CLI pairs (P01, P03, P05, P07) tied on blind task quality; later focused P05 pairs produced one baseline win and one treatment win. The 20-case study has not passed; an installed-0.1.19 P01 pair had missing required-test receipts and a post-unblind quality correction; a subsequent repeat produced a predeclared anonymous quality tie and a recognized successful unittest only in treatment. A CLI prompt canary reached the first response before its first tool. Native Desktop `explorer` and published-0.1.18 `worker` children reported correlated advice before their first code tool in separate smokes; other subagent probes were inconclusive or correctly abstained. No repeatable productivity or quality gain is established.

Use [PILOT_CASES.md](PILOT_CASES.md) for the planned synthetic case bank, [ROADMAP.md](ROADMAP.md) for the thresholds and remaining gates, and [TASKS.md](TASKS.md) for release receipts. Entries below are dated development evidence, including older private-release observations; later records do not erase earlier results. Paths to local experiment artifacts are historical maintainer records and are not downloadable project assets.

### VCR-252-H core CLI runner receipt integration (2026-09-25)

The existing paired CLI runner now applies the redacted event parser to emit bounded first tool-start type and time in v3 anonymous receipts. Legacy v1 and v2 receipts still score under their own schemas. This field says when a tool started, **not** whether it was useful; the prior heuristic first-action and source-read fields retain their explicit limitations. Existing token and wall-time fields remain. Source integration has synthetic tests only: no new live paired arm has exercised v3, no choice ID is yet exported from an explicit coding-decision CLI invocation, and first useful action/cost remain unscored without independent validation and billing provenance.

### VCR-252-R receipt contract for future coding pairs (2026-09-25)

A source-only parser now accepts Codex CLI JSON event lines and explicit choice JSON, returning allowlisted candidate IDs, elapsed time, token counters and uncached-input proxy. It never retains raw prompt, command, output, path, key, or backend rationale. A tool start is measured separately from a useful action; the latter is populated only for a caller-supplied independently validated action ID with successful completion. Duplicated or incomplete usage, invalid candidate IDs and inconsistent triage exit status remain unscored. Synthetic tests cover these contracts. The parser has not been wired into the existing matched runner or exercised in a new live pair, so earlier omissions in VCR-249-S2 remain omissions. Billing cost needs explicit provider provenance and stays unscored here. Future paired runs must preserve predeclared validation labels, required test exits, and anonymous blind quality scores.

### VCR-249-S2 native CLI retry-contract pair (2026-09-25)

One randomized, isolated `gpt-6-luna` low pair used the fictional retry client and its written contract. The blind rubric was fixed before model calls (SHA-256 `0e81fca93400be65899a9a43c0d54c0f517c03c91d80c7e852c7ceb31744dddc`). Treatment received a precomputed real `remote-choice` strategy JSON in its prompt; the baseline received the same coding task without that context. Both ran `python -m unittest discover -s tests -v`, passing nine tests. Independent anonymous review was frozen before arm reveal (score SHA-256 `52ce76aebc4f0dc45d0dda3307372fdb1b1d1ea0d4e75490c5665a077497dc5b`): A 4/6, B 5/6. Treatment A took 69.535 s versus baseline B 39.076 s. Both incorrectly retried HTTP statuses above 599 despite the 5xx-only contract; treatment also lacked an assertion of the last returned HTTP response on exhaustion. The strategy choice ran outside the timed Codex sessions, so its time/cost would make the treatment slower still. The receipt confirms `remote-choice` and that valid JSON reached treatment, but the harness did not retain selected strategy IDs or token usage; first useful action is likewise unscored. A preflight harness retry occurred before any model arm. This is a second negative observation, not proof that Jev worsens work generally or a completed cost comparison. Anonymous artifacts and private mapping are under `/tmp/vcr249-pair-oo80qxwd/` on the maintainer host. Fix the receipt fields and compare on additional relevant tasks before any effectiveness claim.

### VCR-249-S1 native CLI simple coding pair (2026-09-25)

One randomized `gpt-6-luna` low pair used isolated copies of the fictional `tinytext` fixture. Both arms had the same model, required `python -m unittest discover -s tests -v` command, key presence, and source CLI availability; treatment additionally received an explicit instruction to call `jevcompass strategy choose` before editing. The rubric was frozen before the runs (SHA-256 `a3e69e02c29fc7707842789b7b4247f643cd201c7e4ef161505051469fc1e95c`). Independent anonymous scoring was frozen before mapping (SHA-256 `fd088b58f723bdd24beed69e794662c7ecb368f49f18f6818cb1b7c16a8c8fa6`): both arms passed five observable criteria and one final-report criterion was unobservable from the blinded export. After reveal, treatment was A and baseline B. Both changed only `tinytext/text.py`, ran the exact suite with two passing tests, and exited 0. Treatment took 28.966 s versus 22.156 s baseline; input tokens 83,946 versus 69,146, of which cached 76,288 versus 62,208; uncached input proxy 7,658 versus 6,938; output tokens 709 versus 526. Treatment's explicit strategy command returned `no-remote-choice` and two local strategies; it was the first observed tool. This is an added-overhead observation for one easy task, **not a Jev model quality test** or evidence of savings. A preflight CLI invocation was corrected before the scored pair; its record and private artifacts remain under `/tmp/vcr249-coding-pair.2iQO4L/`. Future comparisons should reserve strategy selection for tasks with genuine uncertainty and measure against the same completion gate.

### VCR-251 explicit ambiguous failure triage candidate (2026-09-25)

The source checkout accepts a locally classified failure kind, allowlisted hypotheses, and the actual test exit code. Only categorical metadata and fixed local criteria reach Jev; no logs, paths, commands or source are accepted. A successful test, one plausible explanation, low confidence or transport failure does not trigger a remote recommendation. Synthetic tests verify the returned steps never execute and retain the observed failure. The full 320-test suite, graphify, wheel/sdist and one 0.49 s real synthetic Jev choice pass. Exact numeric exit status remains local; Jev sees only `test_outcome=failed`. No matched Codex diagnostic task, root-cause accuracy, elapsed time or token benefit has been measured.

### VCR-249 explicit pretask strategy candidate (2026-09-25)

The source checkout adds allowlisted coding task kinds and signals, locally reviewed applicable strategy descriptions, and an explicit CLI entry. It uses a typed remote choice only when at least two strategies have similar local evidence; singleton/uncertain/unavailable cases use a labeled local order. The backend sees only task/signal enums and fixed catalog criteria. A direct source CLI call with only `coding`, `existing_symbol`, and `behavior_change` enums got a real Jev `remote-choice` in 0.49 s and locally composed two rationales. The full 311-test suite, graphify and package build pass. Synthetic privacy and validation tests do not demonstrate that a Codex agent adopts the strategy or that task quality, time or token use improves. A randomized matched coding pilot remains open.

### VCR-250 explicit post-change test order candidate (2026-09-25)

The source checkout provides an explicit local JSON intake for coarse changed surface, candidate test kinds and relevance, commands, and mandatory gate. A typed choice calls Jev only when at least two different kinds have similarly plausible local relevance; clear local evidence, one candidate, uncertainty, invalid answers, and transport failures use deterministic local ordering. Synthetic tests verify no raw paths, commands, IDs or relevance values enter the request, preserve mandatory commands, and prove the CLI never executes a supplied command. This is an API/CLI contract test only. Local Python discovery from staged/unstaged/untracked changes is now source-tested, including a synthetic unit-versus-integration remote-choice path; it does not infer the required gate. Codex agent uptake, first actionable test failure, completed-task time, tokens/cost and blind quality remain unmeasured. No benefit claim.

### VCR-247 native `/plan` classification failure and correction (2026-09-25)

A randomized isolated CLI Plan-mode pair used equal fictional queue-migration fixtures and explicitly installed optional skills. The nine-item rubric SHA-256 was `e5397d1a13da47f9d6aa066d01827ef1759276ca8d935fa30296f637af99c5b1`; an independent blind review froze its score file SHA-256 `cdaec3b493b29d81813c9dc4aeb4ac1933271e617e41f5f265700cbcfe41efd3` before the mapping was opened. Treatment was A (4/9), baseline B (5/9). Treatment/baseline completion times were 30.17/43.94 seconds, input tokens 61,817/111,473. The treatment hook's developer context arrived before the agent's first tool, but it was `testing/local` with unrelated `exec_command` and `jevcompass-focused-tests` candidates (13.02 ms); the agent reported NO JEVCOMPASS ADVISORY. Thus this is a negative delivery/relevance observation, not evidence of a useful Jev ranking or performance gain. Source classification now recognizes `Plan moving ...` as planning; unit tests and graphify pass. A fresh randomized native pair must verify the selected planning skill is acknowledged and then assess quality and time.

### VCR-246 safe planning focus smoke (2026-09-25)

A source-only change classifies a planning prompt locally as `migration` or `implementation` and sends only that coarse enum with category/domain/role and curated candidate metadata. Cache keys include the focus so a prior implementation choice cannot be reused for a migration. A unit test covers distinct choices, reuse, and absence of private prompt fragments in the request. In a clean temporary HOME/CODEX_HOME with explicitly installed skills and a restricted PATH, two direct synthetic `UserPromptSubmit` hook calls used real Jev: a fictional JSON-to-SQLite migration selected `jevcompass-plan-cutover` (`planning/jev`, 533.18 ms), and a feature implementation selected `jevcompass-plan-implementation` (`planning/jev`, 286.45 ms). The hook returned only local candidate IDs and exited 0. This verifies API selection under safe coarse metadata, **not** actual Codex Plan mode delivery, skill reading, plan quality, or efficiency.

### VCR-245 optional planning candidates (2026-09-25)

The source checkout bundles two new, explicitly installed skills for different planning tasks: implementation decomposition and migration/cutover safety. The installer does not add them during default hook setup. A clean profile stays silent until the user installs skills; with both present, catalog tests confirm a genuine `choice` question for Jev rather than a manufactured alternative tool. Both SKILL.md frontmatter files passed the Codex skill validator, and source-built wheel/sdist include them. A clean HOME/CODEX_HOME with restricted PATH, explicit source skill installation and `recommend --category planning --domain general --role planner` made a real Jev decision on generic candidate metadata: it selected `jevcompass-plan-implementation` in 502.15 ms. The explicit command sent no task prompt and was not a Codex hook. This is one choice-capacity and API smoke; no agent skill read, native Plan delivery or improved plan output is established. The next controlled experiment will add a coarse local task facet and compare plans with a precommitted rubric.

### Native `/plan` webhook planning smoke (2026-09-25)

A single **unrandomized, unblinded** CLI 0.155.1 smoke used the same fictional task in isolated profiles and `gpt-6-luna` default: design a standalone Python webhook retry queue with idempotency keys, migration from an existing JSON store, validation and rollback. The baseline ran first with no JevCompass hooks; the treatment ran with the installed pipx hooks and `OPENROUTER_API_KEY` present in the launcher environment. Both used `/plan` in the interactive TUI and produced `<proposed_plan>`. The baseline answered three host questions using recommended options (standalone Python, capped exponential retry, copy and verify migration); treatment answered five recommended options across two question batches (SQLite, in-process worker, destination-scoped idempotency, capped exponential retry, one-time verified import). The different clarification paths limit comparability.

The treatment metric was `UserPromptSubmit / planning / local / 10.18 ms`, trace `f579847c`; its diagnostic entry recorded `permission_mode=default` and no collaboration signal. The isolated `CODEX_HOME` still inherited the maintainer's home directory and PATH: its planning pool had `smem` (shell command), `exec_command`, and user-installed `create-plan` (one skill). `_questions` therefore constructed **no choice question**. A separate metadata-only source probe with temporary `HOME` and a restricted `/usr/bin:/bin` PATH returned only `exec_command` for planning, again with no Jev choice; it did not run another model plan. This was a local unranked shortlist, not a Jev model decision. The first assistant message did not acknowledge the trace or candidate IDs; hook execution is confirmed, model-visible delivery is not. No blocking or private prompt upload was observed in this smoke, but it was not a full privacy audit.

The baseline plan explicitly specified at-least-once semantics, matching-key/conflicting-payload behavior, forwarding the `Idempotency-Key` header, concurrency claims, controlled cutover and reconciliation of post-cutover writes before rollback. The treatment specified destination-scoped keys, HTTP 429 retry, a migration marker, capped retries and investigation of the existing JSON schema, but omitted several baseline safeguards. Both covered core migration tests, retry exhaustion and backup. The baseline plan had 3,634 characters, 84,997 total CLI tokens (83,596 input), and about 90 seconds from turn context to plan message; treatment had 2,861 characters, 98,489 total (96,791 input), and about 152 seconds. These process observations include different question turns and are **not** an efficacy estimate. A predeclared blind rubric, equal clarifications, a verified Jev decision and agent-visible advice receipt are required before a quality or cost claim. Private synthetic plan artifacts remain under `/tmp/jev-plan-{baseline,treatment}.md` on the maintainer host.

### VCR-242 CI-backed unittest advice deduplication (2026-09-25)

A source candidate removes the optional `jevcompass-focused-tests` skill from ordinary coding advice when the locally available `unittest` candidate already reflects an unambiguous CI runner. It preserves the skill for an explicit request to choose focused tests and leaves the exact CI requirement intact. The focused 48-test suite and full 287-test suite pass; graphify rebuilt. This candidate changes only advice relevance, not repository test requirements.

One randomized, keyless, isolated CLI P09 pair used equal source bundled skills, `gpt-6-luna` low, and an existing predeclared synthetic helper rubric. Anonymous authored files were identical and valid. The blind score file (SHA-256 `d66ef859d3e71ba12f80d0fc53e1e521c35ac5860b6da214fc58c914e3ec2d87`) was frozen before opening the arm mapping. Treatment reported `exec_command` and `unittest` before its first tool (no focused-test skill); the correlated hook recorded `coding/local` in 6.28 ms. Treatment's exact CI discovery exited 0; baseline's same suite without `-v` exited 0, recorded as equivalent rather than exact. Baseline/treatment wall times were 22.26/27.46 seconds, input tokens 69,677/114,743, and uncached input proxy 10,541/8,247. The strict exact-command criterion did not pass in baseline; first productive action and blocked/privacy ratings were not scored. This single pair does **not** show a speed gain, a complete strict pair, or causal benefit of Jev. The optional source candidate remains subject to further matched tests. Private anonymous artifacts and mapping remain under `/tmp/jevcompass-vcr242-live/` on the maintainer host.

### P09 CI-guided coding pair (2026-09-25)

Supplemental P09 uses the same synthetic Python helper task but removes the runner instruction from the copied README and adds a fixture-local CI workflow requiring `python -m unittest discover -s tests -v`. It is outside the frozen 20-case core and the default CLI case list. Two randomized keyless source-checkout pairs used equal optional bundled skills and a predeclared blind rubric (SHA-256 `cdf5b24b3359bdc31c424e52241b091e84078eb61c723ec81a6fd16ea9eea77e`). Anonymous source quality tied in both pairs; blind score SHA-256 commitments were `b0bf4728508713198871ef0fba9fe64f08b732c89e3c882a8c0bacab4ecebfb0` and `1a9c12605f7e055fe2000cdf9aade1d170236acf170743ffa62f122116886ca6`, each frozen before mapping.

The earlier substring detector labeled both baseline exits as the CI check and did not label treatment. That label did **not** prove a standalone exact command: it also accepted shell wrappers and chains. Raw events were discarded by design, so neither arm can be reclassified retrospectively. In the first pair the hook recorded local coding advice in 5.71 ms, but the agent did not report its ID before first tool; in the repeat, it reported `exec_command`, `unittest` and `jevcompass-focused-tests` before first tool with a matching 5.53 ms local hook. Baseline/treatment process times were 25.89/28.58 s and 23.29/19.64 s respectively; input tokens were 99,255/88,886 and 84,603/71,093. Required-check preservation remains **unverified in both earlier pairs**; the privacy-safe receipts cannot distinguish an exact, equivalent or chained command. Their times are instrument observations, not completed-task benefit evidence.

A strict standalone parser initially missed even the baseline because Codex CLI wraps commands as `/usr/bin/bash -lc '<command>'`; a controlled synthetic probe confirmed this form. The corrected parser now unwraps only `bash -lc` (or accepts bare commands), then separates exact CI from the same discovery suite without `-v`; chains, alternate roots, selectors and extra options are excluded. A fresh randomized keyless P09 pair with equal skills froze source-quality scores before mapping (SHA-256 `c94a7b6463ba042d5896dcea00243b342c7b1da68d799f8c84e367210c576d89`, rubric `f97cb8926ec98203b0ea76bad847505037b549d0d9ab298991c90f9d8a67fbcd`). Both helpers passed blind review. Treatment reported `exec_command`, `unittest` and `jevcompass-focused-tests` before first tool, local advice 5.66 ms, and its exact CI command exited 0. Baseline ran the same suite without `-v`, exit 0; that is recorded separately as equivalent, not exact compliance. Treatment/baseline process time was 55.60/29.38 s, input 102,336/99,407. Under the predeclared exact-command criterion this is not a completed-task pair; under equivalent-suite behavior both passed tests, but treatment was slower and used more input. One pair cannot establish causality or savings.

### Prescribed test command relevance (2026-09-25)

For a coding prompt that already specifies an exact test command, the local advisor now omits redundant test-runner and focused-test skill suggestions. A separate candidate change offers `unittest` for Python coding only when local CI unambiguously uses it; pytest or mixed CI does not qualify. A keyless randomized source P01 negative-control pair with equal bundled skills froze the blind quality scores (SHA-256 `a701e203b86a0c4e2b3be69b3815baeac5646ad9ad684c58da758b2c2600ebef`) before mapping. Both anonymous helpers passed source review and observed unittest exit 0. Treatment returned `coding/low-signal-skip` in 4.49 ms with no advice. Baseline/treatment total time was 22.24/16.97 s, input tokens 69,737/69,031. The faster silent treatment is model/run variation, not an advice benefit; one pair cannot establish causal speed or cost savings. The new positive coding/unittest candidate still needs an advised pair where the task does not prescribe the runner.

### Compact advice source trial (2026-09-25)

An opt-in `JEVCOMPASS_ADVICE_STYLE=compact` experiment consolidates repeated context instructions without changing the published default. In a synthetic local coding example, the text fell from 780 to 616 characters; candidate IDs, local fallback, skill reading, project instructions and required tests remain explicit. A keyless randomized source-checkout P01 pair used equal optional bundled skills. The anonymous authored helpers tied on blind quality (score SHA-256 `7fd6e333abaa5e41b1cc50b93edfe970740cbeed665ee89e1bc3a3ae816aae61`, frozen before mapping); both observed unittest commands exited successfully. Compact treatment reported `exec_command` and `jevcompass-focused-tests` before its first tool, local hook 5.78 ms. Baseline/treatment total time was 18.50/21.24 s, input tokens 69,164/71,592 and output tokens 401/600. This pair does not support a speed or usage benefit; first productive action and advice usefulness were not independently scored. The experiment remains opt-in and does not alter the installed default or establish a billing cost result.

### Token and completion-time instrumentation with simple tasks (2026-09-25)

The CLI runner now records strict, bounded numeric `turn.completed` token counters and Codex process wall time in anonymous v2 receipts; old v1 receipts remain scoreable. Input and cached input are reported separately, with cached input included in total input. The [Codex event schema](https://github.com/openai/codex/blob/main/sdk/typescript/src/events.ts) scopes usage to the root turn; separate turns and child agents are outside this measurement. These counts are usage-volume proxies, not billing cost; process completion is distinct from a human-rated first productive action. A fresh installed-v0.1.22 keyless P08 CLI surrogate pair used the unchanged predeclared scaffold rubric. Both anonymous packages and observed unittest exits passed; blind quality score SHA-256 `1a93ed44de6e09e58dbc90a1c2c602d1637108d6a8e4908175ea38dfcc8364f5` was frozen before mapping. Treatment received local `exec_command`/`git` advice before first tool (`project-setup/local`, 8.69 ms). Baseline process wall time was 35,069.07 ms and treatment 28,180.99 ms (treatment minus baseline −6,888.08 ms). Input tokens were 84,762 versus 71,558, with cached input 77,312 versus 64,256; uncached input differed by −148 tokens and output by −134. This is one favorable pair, not a causal speed or cost result; remote Jev was not used.

### Completed simple-task repeat with equal bundled skills (2026-09-25)

A fresh installed-v0.1.22, keyless randomized P01/P03 repeat used equal optional bundled skills and revised prompts matching the existing fixture tests. The prospective rubric SHA-256 was `5e46aa9b2c57ed4eb150349eb5ba1108c2433ab15a05a69485c043d8dccfb76d`; anonymous quality score SHA-256 `2b34b161cfe50f1db9a42421f794608cff9026889caa2eac5ce06d1c0e5a0174` was frozen before mapping. Both P01 helpers passed blind source review, and both arms had an observed successful `python -m unittest discover -s tests` command. Both P03 repairs passed blind source review, and both arms had an observed successful `bash -n scripts/render_report.sh` command. Thus all four arms completed their small fixture task and required command, within the limits of this synthetic CLI runner.

| Case | Baseline total | Treatment total | Baseline input / output | Treatment input / output | Treatment signal |
| --- | ---: | ---: | ---: | ---: | --- |
| P01 Python helper | 13.79 s | 20.60 s | 54,925 / 337 | 70,734 / 534 | Local coding advice before first tool, 6.56 ms |
| P03 Bash repair | 18.10 s | 15.57 s | 68,752 / 452 | 54,517 / 366 | Low-signal skip, 7.89 ms; no advice |

P01 treatment took 6.80 s longer and used 15,809 more input tokens, despite a quality tie. P03 treatment took 2.52 s less and used 14,235 fewer input tokens, but no advice was delivered, so it cannot show a recommendation benefit. The median treatment minus baseline total was +2.14 s across these two pairs. Cached input is a subset of input; neither token count is a billing-dollar estimate. These single keyless pairs do not establish faster or cheaper coding; root-turn usage excludes separate turns and child agents. First productive action, recommendation usefulness, fresh Desktop delivery, and remote Jev impact remain unscored.

Following the request to include **simple end-to-end tasks**, a randomized installed-v0.1.22 keyless P01 Python helper/P03 Bash defect bank ran four arms. The predeclared blind rubric SHA-256 was `6870cb1bb07bdd7cc54e458319febc2c64db4704508676792f782a8ee2b69707`; score SHA-256 `f2d628ae6916203d3eb0ba5a6fbad916caa350bb58e01f817d44be6d6b4bd36c` was frozen before mapping. P01 helper code normalized whitespace in both arms, but neither wrote the requested focused tests or recorded their execution, so both failed the full task rubric. P03 authored safe default or argument validation in both arms and passed syntax inspection, but the retained receipt does not prove the requested syntax-check command was run. The P01/P03 treatment hooks both returned `low-signal-skip` (8.25 and 8.33 ms), and no advice reached either agent. P01 total process time was 17,312.50 ms baseline versus 23,942.55 ms treatment; P03 was 18,592.28 versus 15,681.70 ms. The median treatment minus baseline across these two pairs was +1,859.74 ms, but one task failed and neither was advised. Their token difference likewise measures model variation and hook presence, not JevCompass benefit. Revise the simple-task fixture and required-check observation before claiming completed-task speed.

### Current Desktop prompt delivery observation (2026-09-25)

In this existing Desktop session, a substantive prompt exposed JevCompass advice ID `44d672d6` and candidate IDs `exec_command`, `jevcompass-focused-tests` to the primary agent before its next tool. The local redacted metric matched `UserPromptSubmit/testing/local` at 74.85 ms. This confirms one in-session prompt delivery, not a freshly started Desktop session, tool use, effectiveness, or speed.

### P08 CLI test-result receipt correction and repeated pair (2026-09-25)

The first repeat of the installed-v0.1.22 keyless CLI surrogate again tied on blind authored-package quality (frozen score SHA-256 `1143aecb20dff2eeda13071b4db4eb75d1e0caba650cc4f311a65491c58eb070`). Both arms' safe event stream recorded a unittest start and completion, but the blind receipt allowlist omitted the new P08 `smoke_unittest_exit` and `scaffold_files_present` fields. That run cannot prove a successful test from its retained receipt. The allowlist was fixed and covered with a redaction/roundtrip test.

A new randomized installed-v0.1.22 `gpt-6-luna` low pair used equal isolated profiles and the unchanged predeclared P08 rubric (`50a26faf...`). Both anonymous artifacts contained coherent package metadata, module and unittest smoke test; the quality tie was frozen before mapping at SHA-256 `d438a21996ab95d3cf39bf80902fe5915f10f01d165da1868f5b9e22a781b723`. After mapping, **both** treatment and baseline receipts showed scaffold files present, observed unittest invocation/completion, and `smoke_unittest_exit=true`. Treatment reported local `exec_command`/`git` advice before first tool (`project-setup/local`, 10.46 ms); no OpenRouter key was forwarded. First-action classes/times are coarse telemetry, not a human rating of the first productive action. No speed or quality improvement is established, and this remains a CLI surrogate rather than the planned Desktop P08 case.

### Installed v0.1.22 P08 CLI project scaffold surrogate (2026-09-25)

The case bank assigns P08 to Desktop. The CLI runner now has a separate synthetic surrogate that requests an authored package under `scaffoldpkg/`, and exports only three allowlisted files for blind review. An initial attempt before runner support stopped at argument validation without starting a model. A fresh randomized installed-0.1.22 `gpt-6-luna` low pair then ran with equal isolated profiles, no other hooks and no OpenRouter key forwarded. The predeclared rubric SHA-256 was `50a26faf3a077305254c085792f75e6c57387a52e37b6d524b89c4cff3386f10`. Both anonymous outputs included coherent package metadata, an importable module and a matching unittest smoke test. Their quality tie was frozen before mapping at SHA-256 `0aa0769cd136d7ed01e960aa9d435db34dde96b2fdc2c5c0fc3a6d972ff9f4b8`; a schema-only projection retained the same scores for the runner (`4d7677c0af57497f48a7cb695f72a407afbfa33fea1314566f8c2ae88f9af6a8`). After mapping, treatment reported `exec_command` and `git` before first tool, matching `project-setup/local` at 8.11 ms. The original runner receipt did not collect P08 test completion, so required-test preservation remains unrated; a later harness change records observed unittest exits for future runs. First productive action, blocking and privacy ratings remain unknown. This CLI surrogate cannot establish Desktop P08 coverage, remote Jev quality, or an effectiveness gain. The scorer returned `not_accepted`; private artifacts stay local.

### Source-only P07 API skill prototype and explicit deliverable (2026-09-25)

A proposed `jevcompass-api-contract` skill was tested in equal source-bundled-skill profiles without publishing or installing it for users. With the earlier P07 wording, both anonymous arms omitted `STATUS_API.md` despite treatment receiving local `api-design` advice naming the candidate (6.01 ms). Blind scores were frozen before mapping at SHA-256 `f9735f497131f8bed650417887deea251e0dbb5e913f77e0cfc335cc8762da03`; both failed. This exposed ambiguity between asking to design a contract and asking to author its file.

P07 now explicitly requests `STATUS_API.md` and retains `api-design/python` classification. A second randomized source-mode pair used identical synthetic fixtures and the proposed skill in both profiles, with only the treatment hook enabled. The anonymous baseline authored a coherent request/result/success/4xx/5xx contract; the anonymous treatment authored no file. The predeclared blind rubric SHA-256 was `dd1417a4bae9abf47c934178ea1a0baa427786a06986e509fafeea584da36799`, and the frozen score SHA-256 was `f57fd0dfef7853295fecac8ce7a053339c8f2a489fc22d4c2431a58afff98a5f`. After mapping, treatment had received the skill ID before its first tool (`api-design/local`, 8.26 ms). This single adverse pair does not establish causality; it does reject the proposed skill for this release. Its pack and catalog changes were removed. Neither pair assessed remote Jev, first productive action, or broad acceptance. The revised P07 prompt changes the case design, so prior P07 scores are historical and not directly comparable.

### Published v0.1.22 corrected P07 contract-writing pair (2026-09-25)

The current runner's P07 uses a disposable `workspace-write` fixture; `--dry-run --cases P07` confirmed this before execution. A fresh randomized installed-0.1.22 `gpt-6-luna` low pair used equal synthetic fixtures, optional preflight, no other hooks and no OpenRouter key forwarded. Both arms exited 0. The two opaque authored-file artifacts were assessed without opening the arm mapping: one contained a coherent endpoint contract with input schema, validated success response and error cases, while the other had no `STATUS_API.md`. The blind score JSON was frozen at SHA-256 `345043089f5c087f98f3beb28a148959ce261fa46b754bf675e08193ab754b41`. After mapping, the authored contract belonged to **baseline**; treatment had no file. Treatment's hook reported `api-design/insufficient-candidates` at 5.67 ms and no pre-tool advice ID, so JevCompass did not supply a recommendation in this pair. The baseline quality win is a model-outcome difference without Jev advice; it cannot be attributed to the advisor. Required checks, first productive action, block/privacy ratings and recommendation usefulness were not established. The scorer returned `not_accepted`; private receipts remain local and are not product assets.

### Installed v0.1.22 native Desktop worker delivery (2026-09-25)

The maintainer's pipx installation was upgraded from 0.1.20 to 0.1.22 with the production `hooks.json` SHA-256 unchanged. `doctor` reported exactly the two advisory hooks and no Jev PreToolUse gate. In this running Desktop host, a new named `worker` reported advice ID `9ceb0fdd` and candidate IDs `exec_command`, `jevcompass-focused-tests` in its initial context before its first tool. A matching redacted local metric recorded `SubagentStart/coding/local` at 54.64 ms and the same trace. A `luna_worker` reported no advice; the configured matcher explicitly covers only `explorer|worker`, so that silence is expected. This single role delivery smoke does not establish fresh Desktop `UserPromptSubmit` delivery, actual skill use, or paired benefit.

### Installed v0.1.21 testing delivery and source candidate (2026-09-25)

A fresh installed-0.1.21 P01 pair with `gpt-6-luna` low, no optional skills and no OpenRouter key forwarded exited 0 in both arms. Its treatment classified `coding` and returned `low-signal-skip` in 5.32 ms; no agent-visible advice ID was expected or reported. Metadata-only receipts cannot establish task quality, so this pair is not counted as an effectiveness result.

In an isolated fictional Git repository with an explicit `python -m unittest discover -s tests` CI command, the installed v0.1.21 adapter emitted `testing/local` advice. A direct hook invocation confirmed the full context contained both `exec_command` and `unittest`. CLI model responses before the first tool were inconsistent: one `gpt-6-luna` response reported no advice despite a 9.07 ms hook metric; subsequent `gpt-6-sol` responses echoed a correlated advice ID (`f6f79bf2`, 9.27 ms; `6520479d`, 8.77 ms) but did not name the candidates. A source-built candidate that places the selected IDs immediately after the trace produced a correlated first response naming `exec_command` and `unittest` (ID `83434dfb`, 9.24 ms) before its first command event. After v0.1.22 publication, a fresh registry pipx profile with exactly two JevCompass hooks repeated a fictional Git fixture: the first agent message named ID `0623c35b` and both candidates before first tool, matching `testing/local` at 5.99 ms; CLI exited 0 and the fixture remained unchanged. All CLI runs exited 0 with read-only commands; no OpenRouter key was forwarded. These small, model-varied probes show a possible presentation issue, not a controlled causal test or broad delivery rate. Fresh Desktop prompt delivery and full paired acceptance remain open.

### Published v0.1.16 P01 with equal opt-in bundled skills (2026-09-25)

A new explicit `--with-bundled-skills` option installs the published package's two first-party skills into both isolated CLI profiles and verifies identical `SKILL.md` bytes before any agent run. The installed 0.1.16 dry run and a randomized live P01 pair passed setup without an OpenRouter key. A blind evaluator saw the same correct `normalize_whitespace` implementation in both anonymous artifacts and froze scores before unblinding at SHA-256 `4f2ecb3b2c3332e1047b5ec1a407cc09dcbebc641d8124d70edacc4c9e803498`. Both passed the code-quality criterion, but only one receipt confirmed a successful focused unittest. After unblinding, that arm was **baseline**. The treatment's agent-reported candidate IDs were `exec_command` and `jevcompass-focused-tests` before its first tool, correlated with `UserPromptSubmit/coding/local` at 4.2 ms. No remote Jev request was measured. The treatment receipt has no recognized required test exit; that is missing evidence, not proof the agent omitted every valid test invocation. Scoring conservatively marked preservation unconfirmed, so the required-check gate cannot pass from this pair. First productive action, blocking, and disclosure were left unknown; metadata does not prove these. The scorer returned `not_accepted`. Private anonymous evidence and mapping are under `/tmp/jevcompass-core-p01-optin-20260925/`. This single tie with unconfirmed treatment validation is not evidence of quality or speed benefit. A repeat pair below reversed which arm had the recognized test.

### Published v0.1.16 full CLI core bank with equal bundled skills (2026-09-25)

A fresh installed-release bank ran all ten CLI core cases as 20 agent arms (`gpt-6-luna` low, isolated equal opt-in first-party skills, no OpenRouter key forwarded). Twenty anonymous receipts and eight quality artifacts were scored before opening the arm mapping. Frozen score-file SHA-256: `61f181f76c1240fd26cdaacf3549e3ab529c04621c925314bb0efd738567a55a`. The four substantive pairs tied on blind task-quality ratings: P01 produced correct helper code in both arms but neither receipt recognized a required unittest; P03 removed the unset-variable crash but left an empty output path in both arms, short of the safe default or argument validation in the rubric; P05 produced coherent documented commands in both arms but the fixture's tests failed, so required-check preservation was left unrated; P07 failed in both arms because the runner erroneously made an API contract writing task read-only. Six routine controls had answer indicators in both arms but no evaluator-visible final answers, so their task quality was unrated. All first-productive-action, privacy, block, and required-check ratings remain unknown in this bank.

After unblinding, P01 reported `jevcompass-focused-tests` plus `exec_command` before its first tool (local, 5.61 ms). P05 reported only generic `exec_command` (local, 4.85 ms). P03 had `debugging/low-signal-skip` and P07 `api-design/insufficient-candidates`; all six routine treatments reported `classification-skip` without advice. The runner reported no Jev request; this bank cannot measure remote decision quality or latency. P07's write-permission mismatch is a harness defect to repair before reusing that case. The scorer returned `not_accepted`; this bank does not establish usefulness or a speed/quality gain. Private evidence and mapping are under `/tmp/jevcompass-core-bank-v016-20260925/`.

### Optional Bash path skill experiment: held after blind P03 pairs (2026-09-25)

A opt-in prototype added `jevcompass-shell-path-debug` to the explicitly installed skill pack and a `--source-bundled-skills` mode that verifies identical checkout skill bytes in both disposable profiles before each arm. The first P03 pair used a misclassified catalog task kind (`debugging` rather than the adapter's `debug`), so treatment emitted no advice; blind scoring showed treatment better but **cannot attribute it to JevCompass** (SHA-256 `8fd99a1b62d55e173109aa989b533c693747441a3635651fa262ba40bce56af1`). After correcting the routing, a fresh pair delivered the local skill ID before treatment's first tool, but blind quality favored **baseline**: treatment still produced an empty destination (SHA-256 `3b3e33430e19fe8749386edf9b13d7eb4066165543f48efd2542dd82b1322081`). Revised wording produced a **failure/failure tie** in another routed pair (SHA-256 `da342997e8cb8ebd4d46c3fadd612a27f38050a92d60202b9a0f558601683713`). All pairs were keyless and therefore measured local advice, not remote Jev choices. The proposed skill was **removed** from the installable pack and catalog; no release or default behavior changes are justified by this result. The source-mode paired harness remains for later candidates, with the same privacy and equal-profile checks. Private artifacts are under `/tmp/jevcompass-p03-shellskill-source-20260925/`, `/tmp/jevcompass-p03-shellskill-routed-20260925/`, and `/tmp/jevcompass-p03-shellskill-revised-20260925/`. First productive action and mandatory test execution were not proven, and the overall scorer remains `not_accepted`.

### P07 write-permission repair and paired rerun (2026-09-25)

The P07 fixture's instructions require authoring a local contract, but the earlier runner mistakenly assigned a read-only sandbox. The runner now gives P07 a disposable workspace-write fixture and exports only its allowlisted, bounded `STATUS_API.md` as the blind quality artifact. A new randomized pair used installed 0.1.16 with equal opt-in skills. Both agents actually wrote detailed request, validated success, and error contracts; blind task quality was **pass/pass** before unblinding, frozen SHA-256 `c437fd74ddfa2f62c300e280d137eb6cbe77e57bd7fe0308aa197c804d7fa3c1`. The simple `contract_indicators` heuristic was true for treatment but false for baseline because the latter used a `Required: no` table cell rather than the literal word `optional`; the blind rubric still passes both. After unblinding, treatment recorded `api-design/insufficient-candidates` (5.58 ms), with no advice. The repair establishes that this pair can perform its requested task, not a Jev effect; first productive action and required checks remain unrated. Private evidence: `/tmp/jevcompass-core-p07-write-20260925/`.

### Published v0.1.16 P01 repeat and evidence interpretation (2026-09-25)

A second randomized P01 pair used the same installed 0.1.16 interpreter, equal opt-in skill profiles, fictional fixture, `gpt-6-luna` low and no OpenRouter key. Both anonymous artifacts implemented the correct whitespace normalization. Blind scores were frozen before unblinding at SHA-256 `b364912c5ee2cda184232842f315f205fbcddd12b6c7bee6b0a51a76a0e5bb2e`: quality tied; one receipt recognized a successful required unittest and the other returned `null`. Unblinding showed **treatment** had the recognized test, while **baseline** had `null`. Treatment reported `exec_command` and `jevcompass-focused-tests` before its first tool, correlated with `UserPromptSubmit/coding/local` at 4.74 ms. Together, the two P01 pairs show no quality difference and opposite test-observation patterns. The detector recognizes completed commands containing `unittest`, `discover`, and `tests`; `null` cannot distinguish an omitted test from a different command form or incomplete event. A `false` blind preservation rating records insufficient proof under the current rubric, not a confirmed skipped check. Neither pair establishes Jev remote choice, first productive action, usefulness, or an outcome gain. The scorer remains `not_accepted`. Private evidence is under `/tmp/jevcompass-core-p01-repeat-20260925/`.

### Published v0.1.16 core P03 pair and installed-runner check (2026-09-25)

The core CLI runner can now opt into an installed, version-verified 0.1.16 interpreter and equal `OPENROUTER_API_KEY` presence in both isolated arms; this also exposes the key to the Codex processes and potentially their tools, so the option is explicit and limited to fictional fixtures. A dry run confirmed identical copies. One live randomized P03 pair with `gpt-6-luna` low and preflight completed in both arms, with blind quality artifacts scored **pass/pass** before unblinding. Frozen score-file SHA-256: `4f22c7e755bb587b5744d3230b1a16cf5987471da06e423f833efa7722c58e49`. Both edited the script to avoid an unset variable and retained valid Bash syntax; required checks were marked preserved. The first productive action, blocks and disclosure ratings remain **unknown** because bounded receipts cannot prove them. After unblinding, the treatment's safe metric was `UserPromptSubmit/debugging/low-signal-skip` at 7.02 ms, with no emitted advice or Jev request: the vanilla candidate pool was insufficient. The private files under `/tmp/jevcompass-core-p03-v016-20260925/` contain no observed API key value, but this file check does not establish absence from discarded process output. This is one core pair with a safe abstention, not a useful-advice trial or a quality/speed gain. The scorer still returns `not_accepted`; its own report lists the remaining coverage and usefulness gaps.

### Published v0.1.16 C05 blind pair (2026-09-25)

A randomized supplemental C05 pair used isolated profiles, the published PyPI JevCompass 0.1.16 interpreter, identical fictional signed-webhook brief and two identical reviewed skill candidates (`create-plan`, `security-requirement-extraction`) in both arms. Both arms exited 0. Before opening the private arm mapping, the anonymous final answers were scored **6/7 and 6/7** against the prewritten seven-point rubric: both omitted recording which rotation key verified a request without exposing the key. After unblinding, baseline was `afcc0964781c6874c029c8c1` and treatment `449f232a8fa7ac7478eebdd9`. The treatment reported advice ID `b0fdf7e4` before its first tool, matching a `planning/jev` metric of 622.44 ms. Neither candidate skill read was observed; the runner recorded no confirmed successful fixture-target read, so first productive action timing is **unknown**. The blind answers and private mapping remain under `/tmp/jevcompass-c05-v016-retry-EYJWMW/jevcompass-blind/`, outside Git. This is one supplemental tie, outside the frozen core 20. It establishes delivered remote choice in this CLI case, not a measured speed or quality gain or Desktop coverage.

### Isolated CLI hook-path comparison (2026-09-25)

In the same Codex CLI 0.155.1 isolated read-only setup, `UserPromptSubmit` canary delivery with `gpt-6-sol` passed (`canary_before_first_tool=true`, exit 0). A `SubagentStart` canary run with `gpt-6-sol` exited 0 but returned `hook_invoked=false`, `child_observed=false`, and `spawn_tool_observed=false`. The last field is based only on allowlisted tool names in the parent JSON event stream; an absent event cannot prove no child existed. These observations isolate the negative result to the subagent path rather than general hook loading. They do not establish emitted-advice delivery for a child. The probe does not send an OpenRouter request or private task data.

### Current Desktop explorer abstention (2026-09-25)

A fresh native Desktop `explorer` child was asked to identify the SubagentStart classification and context functions in the product code. The child said its original initial context had **no JevCompass advice ID**. A contemporaneous local metric recorded `SubagentStart` / `codebase` / `low-signal-skip` / 55.96 ms / trace `3d3a4f44`. This confirms the adapter was invoked and intentionally did not emit advice for the available singleton candidate. It does **not** test delivery of emitted `additionalContext` or establish usefulness. The CLI canary below also remains unconfirmed.

### Published v0.1.18 Desktop role smoke (2026-09-25)

The active Linux pipx install was upgraded from 0.1.16 to public 0.1.18; `doctor --json` passed with exactly `UserPromptSubmit` and `SubagentStart`, no Jev `PreToolUse` gate. With only generic available candidates, a new native `explorer` child reported `NO JEVCOMPASS ADVISORY` before its read-only code check; a contemporaneous `SubagentStart/codebase/low-signal-skip` metric took 49.57 ms. No advice ID exists to correlate that specific skip.

After an explicit dry-run and installation of the two optional bundled skills on this maintainer profile, manual `coding/software/worker` advice listed `exec_command` and `jevcompass-focused-tests` as a local unranked shortlist. A new native Desktop `worker` child reported advice ID `96b34a1d` and those exact candidate IDs before its first read-only file check. The local metric matches `SubagentStart/coding/local`, trace `96b34a1d`, 56.79 ms. The child's preflight was sent by a collaboration message before its first shell read; this proves context visibility for that child, not actual skill use, Jev remote ranking, prompt-hook delivery, task-quality gain, or broad coverage. The optional skill pack changed the maintainer's profile and is not part of default installation.

### Published v0.1.18 remote test-runner mismatch and 0.1.19 correction (2026-09-25)

The active 0.1.18 package passed one explicitly billed synthetic `doctor --test-jev` request with a configured environment key. A direct `testing/python/worker` advisory then made a remote Jev selection (`SubagentStart/testing/jev`, 453.74 ms), but offered globally installed `pytest` while this repository's required CI gate runs `python -m unittest`. This is a concrete relevance failure, not task-outcome evidence. The 0.1.19 source candidate now reads bounded local CI commands and substitutes a generic `unittest` option when that runner is unambiguous; a targeted test checks unittest, pytest and mixed signals. A opt-in repeat selected unittest. After publication, an isolated registry pipx 0.1.19 install and the maintainer's upgraded pipx runtime both suggested unittest without pytest in this repository; the maintainer call used a cached Jev choice (52.45 ms). These manual calls do not prove agent-visible advice or a paired-outcome benefit. CI content and repository paths are not included in the remote candidate metadata.

### Published v0.1.19 P01 CLI pair with equal bundled skills (2026-09-25)

One randomized P01 baseline/treatment pair used the pipx-installed 0.1.19 interpreter, `gpt-6-luna` low, identical fictional fixtures and identical opt-in skill bytes, without forwarding an OpenRouter key. Both Codex arms exited 0. Treatment reported `exec_command` and `jevcompass-focused-tests` before its first tool; a correlated `coding/local` hook metric took 6.38 ms. Both receipts record `focused_unittest_exit: null`, so required test execution is unconfirmed. First productive action, advice usefulness, blocking and disclosure are unscored. The initial blind quality file (SHA-256 `62e0995257314c47f9c6ac1e19c8382e4791f2de0d739e141c2ca6bf3abb1823`) rated both false after overlooking focused tests already present in the starter fixture. A correction after unblinding (SHA-256 `7985ad09078462ef5ea637cc3b6f4b9826c2c9b516daf5ab5bdca561720d21d3`) rates both helper artifacts true, but **is not a blinded quality result**. The scorer returns `not_accepted/incomplete`; neither remote Jev selection nor speed or quality benefit is established. Private maintainer receipts: `/tmp/v019-p01-pair-20260925/`.

### Published v0.1.20 P05 CLI abstention smoke (2026-09-25)

A fresh isolated Codex CLI 0.155.1 run used pipx-installed 0.1.20, the fictional P05 task and only the default advisory hooks, without forwarding an OpenRouter key or installing extra skills. Before first tool, the agent reported `NO JEVCOMPASS ADVISORY`; a matching local metric showed `UserPromptSubmit/package-docs/low-signal-skip` in 5.73 ms. The first tool was `command_execution`; a routine CLI help check exited 0. The session exited 0 and observed no Jev request. This is one host-level abstention check, not a measured speed or task-quality gain. Redacted maintainer receipt SHA-256 `677c1b7940f48c9cb458633990a40f8b9d143864da3b9c04a69c60cc61c6971b` at `/tmp/jevcompass-v020-p05-host-smoke-20260925/receipt.json`.

### Published v0.1.19 P05 package-docs CLI pair (2026-09-25)

A fresh randomized installed-package P05 pair used `gpt-6-luna` low, equal fictional README fixtures and optional skill bytes, and no OpenRouter key. Both anonymous README artifacts documented the correct install, help and unittest commands; blind quality scores were frozen before mapping (SHA-256 `329e298bb7f779691844465fbffd1ff3ce056dfb9cc6c753e245b8cae9358692`). Treatment reported only `exec_command` before its first tool, with correlated `package-docs/local` hook duration 6.2 ms. Both arms exited 0 and passed the help and install checks. The treatment's observed required unittest exited nonzero in the fixture; the baseline test exit was unobserved. The test failure is evidence of an existing fixture behavior, not by itself a regression caused by advice. First-action metadata shows baseline `edit` at 5.81 s and treatment `source_read` at 17.85 s, but first **productive** action was not rated. No remote selection or quality/time benefit is established; scorer returned `not_accepted/incomplete`. Private maintainer receipts and mapping: `/tmp/jevcompass-p05-pipx019-IKAJni/`.

### Published v0.1.19 P01 repeat with test observation (2026-09-25)

A fresh randomized pair used pipx-installed 0.1.19, `gpt-6-luna` low, identical fictional fixtures and optional skill bytes, with no OpenRouter key. The anonymous artifacts both satisfied the helper-quality rubric accounting for two tests already in the starter. This assessment was frozen before unblinding (SHA-256 `c150301b71e03db8b837aa2984009c6cae26c6b1c320ff4324348768aa78c3e2`). After mapping, the **treatment** arm reported `exec_command` and `jevcompass-focused-tests` before its first tool and matched a `coding/local` hook metric of 4.73 ms; it had recognized unittest start/completion and exit 0. The **baseline** arm had no recognized unittest start/completion and exit `null`; this does not prove it ran no equivalent check. First productive action, advice usefulness, blocking and disclosure were not scored. An initial attempt wrote its runner output into the blind directory and exited before launching arms; the successful repeat used a separate log path. One pair does not establish causality, remote Jev ranking or acceptance. Private receipts and mapping: `/tmp/v019-p01-repeat3-20260925/`.

### Published v0.1.19 installed CLI test-runner delivery (2026-09-25)

A fresh isolated Codex CLI 0.155.1 run used pipx-installed JevCompass 0.1.19 with `gpt-6-luna` low and no OpenRouter key forwarded. Before the first tool, the agent reported advice ID `215744b8` and `exec_command`/`unittest`; the matching redacted `UserPromptSubmit/testing/local` metric took 33.66 ms. Its fictional fixture specified `python -m unittest discover -s tests`; the required suite exited 1 on an existing `test_repeated_spaces` assertion failure, and the Codex process exited 0. This proves one local recommendation was delivered before first tool and the validation failure was observed; it does not establish remote Jev selection, a matched outcome gain, or acceptance. Maintainer-only synthetic receipts: `/tmp/jevcompass-v019-testing-python-detail-ubo8vbqw/` and `/tmp/jevcompass-v019-testing-python-smoke-bgcbnbpd/`.

### Published v0.1.18 Desktop focused-test workflow (2026-09-25)

A second new native Desktop `worker` was given a read-only hypothetical formatting change in the fictional `cli_core` fixture. Its preflight reported advice ID `8dbda982`; the metric matched `SubagentStart/coding/local` at 55.39 ms. The child read the installed `jevcompass-focused-tests` skill, fixture instructions, implementation and tests. It chose `test_repeated_spaces` as a focused check and ran the fixture README's full `python -m unittest discover -s tests` gate. Both exited 1 on the same pre-existing repeated-spaces failure; the child reported failure accurately, made no edit, and noted that the hypothetical change was underspecified. This is a single observed skill read and preserved required-check failure, not a matched baseline, proven causal effect, speed/quality gain, or evidence that Jev remotely selected the skill.

### Isolated CLI SubagentStart canary (2026-09-25)

A test-only `SubagentStart` hook in a temporary `CODEX_HOME` asked a synthetic `explorer` child to echo a random marker before any tool. `python3 scripts/probe_hook_delivery.py --event subagent --model gpt-6-luna --timeout 90` ran in a read-only synthetic workspace without an OpenRouter key. The Codex CLI 0.155.1 process exited 0, but the hook receipt was absent (`hook_invoked=false`) and no identifiable child events appeared in the parent JSON stream (`child_observed=false`). Two runs had this result. This is **unconfirmed** delivery: it does not establish whether a child was spawned, whether the host loaded the hook, or whether advice reached a child. The probe preserves the existing prompt probe as the default `--event prompt`; no production hook was changed. Offline tests check event matching, first-message ordering, and redacted summary behavior.

### Installed v0.1.8 fresh CLI delivery smoke (2026-09-24)

A fresh Codex CLI 0.155.1 `exec --json` run used the active pipx JevCompass 0.1.8 runtime (package metadata 0.1.8, Python 3.14.7), an isolated temporary `CODEX_HOME` with only JevCompass `UserPromptSubmit` and `SubagentStart` hooks, and a synthetic Git/Python fixture with one uncommitted change. The child environment removed `OPENROUTER_API_KEY`, so the correlated `UserPromptSubmit` metric recorded local fallback, not a live OpenRouter request. The first assistant message (JSONL line 5) reported advice ID `a14d8368` and suggested IDs `exec_command`, `git` before the first shell tool (line 6); that tool inspected only the synthetic Git diff. Matching safe metric: `review` / `local` / 62.64 ms / trace `a14d8368`. Codex exit 0, read-only sandbox, no fixture changes. The finding was that the edit replaced the script's observable `print(2)` behavior with an uncalled function. Hook-trust bypass generated two informational CLI warning events; the operation continued. Private evidence: `/tmp/jevcompass-v018-cli-smoke.ijRvez/events.jsonl`, `final-message.txt`, `home/.local/state/jevcompass/advisor.jsonl`, and `exit-code.txt`. This is one fresh local-fallback delivery smoke, not remote Jev choice, recommendation usefulness scoring, or broader pilot acceptance.

### Desktop credential inheritance check (2026-09-24)

A read-only inspection of environment variable **names only** found `OPENROUTER_API_KEY` absent in the running Linux Desktop window process but present in its Codex `app-server` child. A terminal `doctor` alone still does not prove what environment the hook actually receives. The active pipx JevCompass 0.1.6 initially lacked optional `keyring`; `pipx inject jevcompass 'keyring>=25'` succeeded, and `env -u OPENROUTER_API_KEY jevcompass auth status` confirmed a usable native system keyring with **no OpenRouter key stored**. No credential value was read, copied, printed, or persisted. The app-server's key presence makes the earlier inference that interactive keyring setup is required invalid. The optional injected keyring and its dependencies were subsequently removed from the active pipx environment in VCR-118; v0.1.6, both hook registrations, and `doctor` remained healthy with the production `hooks.json` hash unchanged. Desktop remote-choice delivery remains unverified until an eligible hook event is correlated with agent-visible advice before its first tool; no key migration is required by this evidence.

### Installed v0.1.6 current Desktop explorer abstention (2026-09-24)

A read-only `explorer` subagent in the currently running Linux Desktop host was asked to report its initial JevCompass context before any tool. It reported `NO JEVCOMPASS ADVISORY`; the new safe metric was `SubagentStart/codebase/low-signal-skip` in 63.45 ms (trace `5f4fc8f2`). In this role-only event the available generic shell was not a specific enough recommendation, so no advice context was expected. The agent then answered a codebase question: curated MCP entries that are merely configured are removed in the actual `select_advice` path before ranking; configuration alone does not establish a live connection. This confirms a live hook invocation and deliberate abstention on the current host process, not a fresh Desktop prompt delivery, active MCP availability, or an improvement in task quality. The Desktop app process predates the latest hook configuration and is not the fresh-host acceptance run.

### Installed v0.1.6 planning classification smoke (2026-09-24)

The active Linux pipx profile was upgraded with the digest-verified v0.1.6 wheel; `doctor --json` passed, reported both advisor hooks, and the production `hooks.json` SHA-256 remained unchanged. A fresh read-only Codex CLI 0.155.1 session reused the prior temporary JevCompass-only profile and synthetic Git/Python fixture. The prompt asked to plan packaging and later to “inspect” files; the v0.1.5 form had classified this as `codebase` and skipped advice. The installed v0.1.6 first agent message (JSON event 4) reported advice ID `c73c13df` and tool `exec_command` before first tool event 5. The matching safe metric recorded `UserPromptSubmit/planning/cache` at 10.22 ms. This verifies the planning classification fix and agent-visible cached advice without edits; the cached outcome is not a new remote Jev call, a paired quality gain, or Desktop delivery. Private transcript remains under `/tmp/jevcompass-v015-cli.EXwTF4/` as `events-v016-mixed.jsonl`.

### Installed v0.1.5 fresh CLI delivery smoke (2026-09-24)

The active Linux pipx installation reported v0.1.5. Three fresh Codex CLI 0.155.1 `exec --json` runs used the same private temporary profile with only JevCompass hooks, a synthetic read-only Python fixture, and explicit hook-trust bypass in that isolated profile. The first non-Git review returned `NO JEVCOMPASS ADVISORY` before the first tool; its matching adapter result was `review/low-signal-skip` (7.29 ms). A packaging task whose test instruction included “inspect” also returned no advice; its result was `codebase/low-signal-skip` (8.15 ms), illustrating classification sensitivity to measurement wording. A packaging task classified locally as `planning/python` then reported advice ID `88c2ac8c` and tool ID `exec_command` in its first assistant message at JSON event 4, before the first tool at event 5. The matching `UserPromptSubmit/planning/jev` metric carried the same trace and took 536.83 ms. No skill ID was selected. All three sessions exited 0, used read-only sandboxes, and made no edits. Authentication was copied only into the mode-0700 temporary profile (auth file mode 0600); no credential content was printed. Evidence remains local under `/tmp/jevcompass-v015-cli.EXwTF4/` and is excluded from the blinded four-pair scores. This verifies one actual remote-choice CLI delivery for installed v0.1.5, not task benefit, broad coverage, Desktop prompt delivery, or the full acceptance gate.

The private CLI pilot can now produce `pilot-diagnostics.json` alongside the mapping. It records an allowlisted hook category, status, bounded latency, and whether the matching advice ID was reported before the first tool; no prompt, command, path, memory text, backend response, or raw trace is copied into that sidecar. Keep it with the private mapping, outside evaluator-visible receipts. Missing trace reports remain missing delivery evidence; the sidecar cannot prove the agent used the recommendation or establish cross-host acceptance. VCR-63 local tests pass (154 tests), but a fresh host run with this instrumentation is still required.

**Not yet accepted.** A previous six-pair CLI routine run exists, but it inspected the private JevCompass repository with custom smem hooks and is excluded from this synthetic acceptance bank. Four substantive CLI pairs have blinded task-quality scores (all ties), but advice usefulness and first-productive-action have not been scored. One correlated Desktop `explorer` smoke and one fresh CLI local-fallback prompt smoke prove advice reached the agent before its first tool. A separate fresh CLI 0.155.1 `review/python` run made a remote Jev choice and delivered advice ID `5914ac30` before its first `exec_command`; its matching metric reports `UserPromptSubmit` / `review` / `jev` / 696.03 ms. These are single delivery smokes, not usefulness or paired-study evidence. JevCompass classifies substantive prompts independently of permission mode; Codex does not provide a reliable Plan UI field in the hook event, so Plan-only automatic coverage is not a product requirement. Usefulness, both-host balance, and the full paired study remain open.

## VCR-105 installed v0.1.5 isolated CLI advice delivery (2026-09-24)

A fresh read-only Codex CLI 0.155.1 run used installed JevCompass 0.1.5 in a temporary `CODEX_HOME` whose `hooks.json` contained only JevCompass `UserPromptSubmit` and `SubagentStart` registrations; no smem hooks were present. The synthetic Python fixture was a temporary Git checkout with one working-tree change, so both `exec_command` and `git` were usable review candidates. The first assistant response (event 5) reported JevCompass advice ID `31415150` and those two tool IDs before the first command (event 6). The safe metric at `~/.local/state/jevcompass/advisor.jsonl` matched trace `31415150`: `UserPromptSubmit` / `review` / `local` / 727.61 ms. Codex exited 0, inspected the synthetic diff read-only, and made no edits; its review found a floating-point behavior difference in the changed accumulator. Two preliminary no-Git variants correctly abstained with `low-signal-skip` and are not delivery evidence. This confirms one fresh isolated **local-fallback** delivery; the matching metric did not record a remote Jev choice, and this smoke does not establish remote-choice behavior, recommendation usefulness beyond this spot-check, paired benefit, Desktop coverage, or pilot acceptance. Private runner output is under `/tmp/jev-v015-smoke-Lz2XE8/` and contains the synthetic transcript; no transcript or fixture source was committed.

## VCR-99 installed v0.1.4 CLI review smoke (2026-09-24)

The normal pipx environment was upgraded from an authenticated, digest-verified v0.1.4 wheel without changing `hooks.json`; keyless `doctor` passed. In a fresh read-only Codex CLI 0.155.1 session over a synthetic `counter.py` directory outside Git, the first agent message (event 4) reported JevCompass ID `2378addf`, selected `exec_command` and `code-review-excellence`, before the first command (event 5). The matching metric was `UserPromptSubmit` / `review` / `jev` / 609.67 ms. The agent read the review skill and inspected the helper; no Git recommendation or file edit occurred. This is one delivered, task-relevant remote decision after the `.py` domain and Git checkout fixes. It does not prove a paired quality gain, first-productive-action improvement, macOS compatibility, or fresh Desktop delivery.

## VCR-96 installed v0.1.3 CLI review smoke (2026-09-24)

The authenticated v0.1.3 wheel was installed into the normal pipx environment while `hooks.json` stayed byte-for-byte unchanged. A fresh read-only Codex CLI 0.155.1 session reviewed a synthetic Python helper and included the verb `quote` in its advice-ID preflight. The first agent message (event 4) reported advice ID `12ad94ae` with `git` and `code-review-excellence`; the first command was event 5. The matching metric was `UserPromptSubmit` / `review` / `cache` / 59.48 ms. The agent loaded the review skill and inspected the helper without edits. It later found the fixture directory was not a Git repository, so the Git recommendation was unusable in this case. This proves delivery and the quote-classification fix for one installed CLI run, but it fails full recommendation relevance and says nothing about Desktop or time benefit. Filter Git in non-repository review contexts in VCR-97.

## VCR-92 installed v0.1.2 CLI classification diagnostic (2026-09-24)

The private release wheel was installed in the normal pipx environment with the existing hook file unchanged. In a fresh read-only Codex CLI 0.155.1 session over synthetic `counter.py`, the first agent message (event 4) reported advice ID `dab8fa6b`, selected tool `exec_command`, and skill `documents` before the first tool event 5. The matching metric was `UserPromptSubmit` / `source-review` / `local` / 55.24 ms. The review prompt asked the agent to *quote* only the advice ID; that ordinary verb incorrectly matched the source-review classifier's price-quote term. Thus the trace proves delivery but **fails relevance**: `documents` was not a suitable code-review recommendation. The agent's code report saw no issue, and no files changed. Fix this ambiguity before counting a review recommendation as useful; avoid the contaminating measurement phrase in a repeat smoke.

A second fresh CLI 0.155.1 attempt omitted the verb `quote`. Its prompt named `counter.py` but did not say Python. The installed v0.1.2 adapter classified `review/general`, found only a generic shell candidate, and deliberately logged `low-signal-skip` in 59.62 ms (trace `7781d73c`). The agent reported no advice before its first command. This is adapter abstention, not host non-invocation or a regression of hook delivery; the local `.py` domain fix is tracked as VCR-95 and still needs packaging and a fresh-host check.

## VCR-87 current Desktop child invocation diagnostic (2026-09-24)

A newly spawned `explorer` child in the current Desktop host reported **no JevCompass advice ID or selected tool/skill IDs in its initial context before tools**. The safe local metric file `~/.local/state/jevcompass/advisor.jsonl` gained no `SubagentStart` entry for this child: its tail still ended with earlier CLI prompt events. The installed `doctor --json` sees two advisory registrations and no Jev PreToolUse gate, but does not establish active Desktop hook trust, host reload, or invocation. With this profile, `doctor` also sees only `exec_command` for `codebase/software`; a direct adapter event would intentionally return `low-signal-skip`, so absent advice alone would not prove host failure. The missing metric leaves host invocation unconfirmed in this running Desktop session. Repeat after a fresh Desktop host reload with an eligible profile and correlate the new metric with advice visible before the first tool. This diagnostic does not count as fresh-session Desktop delivery or usefulness evidence.

## v0.1.1 fresh CLI remote-choice delivery smoke (2026-09-24)

An installed, authenticated v0.1.1 wheel ran in a normal-trust Codex CLI 0.155.1 session on a read-only synthetic Python fixture. One explicit, billed `doctor --test-jev` request passed before the session. For the review prompt, the first agent response (event 4) reported JevCompass ID `72576fcd`, selected tool `git`, and selected skill `developer-essentials:code-review-excellence`; the first tool began at event 5. The matching local metric was `UserPromptSubmit` / `review` / `jev` / 437.21 ms. The agent made no edits. A focused source and offline-test audit traced the request to category/domain/role plus curated candidate metadata; the API key is carried in an authorization header and diagnostics keep only allowlisted fields. The live HTTPS payload was not captured, so this is a source-contract check rather than independent network observation. It assumes curated catalog descriptions contain no private data. This is one delivered remote Jev recommendation, not a paired quality/speed gain, a Desktop result, or evidence that the full 20-case acceptance thresholds passed.

## VCR-64 fresh P05 paired CLI diagnostic (2026-09-24)

A fresh randomized baseline/treatment pair ran the synthetic P05 package README task with `gpt-6-sol` medium and equal preflight. The isolated runner forwarded no OpenRouter key and installed no other hooks. Blind quality scores were saved and SHA-256 committed (`39d8d021a3d46d8f109bd68800bc9bae835b6927d9dd3662ebf62c1152e31710`) before arm mapping was opened. Both README artifacts gave coherent install steps and preserved the module-form help and required unittest command; both observed test commands failed on the deliberately broken starter. The scorer therefore found a quality tie with required checks preserved and no blocks. It did not score first productive action, privacy disclosure, or recommendation usefulness from incomplete evidence.

The private, validated diagnostic sidecar correlates the treatment to `package-docs/local` at 3.64 ms and confirms its ID was reported before the first tool. The baseline is `not_applicable`. This verifies delivery in one CLI pair, not causation or a speed gain. Private receipts, quality artifacts, mapping, sidecar, score, and rationale are at `/tmp/jevcompass-vcr64-p05-20260924/`; keep mapping and sidecar away from blind evaluators. Overall automatic-advice acceptance remains **not passed (0/20 complete gate cases)**. A separate post-unblind relevance assessment and Desktop coverage remain necessary.

## VCR-65 fresh P03 Bash repair pair (2026-09-24)

A randomized synthetic CLI P03 baseline/treatment pair completed with identical `gpt-6-sol` medium settings and preflight, no OpenRouter key and no other hooks. Two blind Bash artifacts were evaluated before unblinding. The first score file used an invalid string instead of a boolean; the evaluator corrected only those fields while still blind and committed the valid score SHA-256 `aab2de8a82e8039244a77e35c33fc48bc38d3decb1ef1e8d466a64d346c6a6ad`. Both scripts passed `bash -n`, exited 0 with unset or empty `OUTPUT_PATH`, defaulted to `report.txt`, and created no files in those cases. Quality and required-check ratings tied. Block, privacy, and first productive action were left unknown where evidence was insufficient.

After scoring, the private diagnostic showed the treatment hook invoked, classified `debugging`, and deliberately returned `low-signal-skip` in 5.48 ms. No advice was emitted, so this pair tests low-noise abstention and task quality but provides no recommendation usefulness or delivery evidence. Private evidence is in `/tmp/jevcompass-vcr65-p03-20260924/`. Overall acceptance remains open.

## First-alpha P01 pair and P07 artifact fix (2026-09-24)

P01 was freshly randomized in isolated CLI with `gpt-6-sol` medium, equal preflight, no OpenRouter key, and no other hooks. Blind task-quality bytes were frozen before unblinding (SHA-256 `534a7df152fb90810c576ba2ac98ac5f4f8c895b52586feef53e2ea9dd032446`). Both whitespace-helper arms met quality and required checks; first productive action, block, and privacy ratings remained unknown. The treatment hook returned `coding/low-signal-skip` in 1.75 ms; it emitted no advice. Private artifacts are in `/tmp/jevcompass-release-p01-20260924/`.

Two initial P07 attempts failed closed before writing blind artifacts because the agent's final answer contained a local absolute path. VCR-68 replaces recognized local paths with a neutral marker in the P07 blind answer before validating and storing it; other private-content and prompt checks remain intact. Those failed attempts are excluded. A fresh P07 pair is needed for the four-pair first-alpha sample.

## Four-pair first-alpha sample complete (2026-09-24)

Fresh randomized synthetic CLI pairs P01, P03, P05, and P07 used equal `gpt-6-sol` medium settings, equal preflight per arm, no OpenRouter key, and no other hooks. Each blind score was frozen before opening the private mapping and diagnostic sidecar; score SHA-256 values are recorded in the case sections and task list. All four pairs are task-quality ties. P05 received `package-docs/local` advice with a correlated ID before first tool (3.64 ms); P01 and P03 deliberately returned `low-signal-skip` (1.75 and 5.48 ms); P07 returned `insufficient-candidates` (3.73 ms). Three skips are not eligible advice deliveries or evidence of improvement. No pair has a human-rated first productive action, and full interaction-level privacy evidence is absent. The four-pair alpha sample demonstrates installable non-blocking guidance and abstention, not efficacy, remote Jev performance, Desktop prompt coverage, or the stronger 20-case acceptance gate.

For P07, the blind answers both covered method/path, JSON input, validation, status response, and error cases. The valid score SHA-256 was `84df6c0d038499d7f13ce570bbfe4d119fb87145c370d952adea8424255ea638`; no required test was specified, so the scorer left required-check preservation unknown. The local path redactor permitted the answer artifacts without copying workspace paths. Private evidence is in `/tmp/jevcompass-release-p07-fixed-20260924/`.

## Private v0.1.0 alpha release (2026-09-24)

The [private GitHub prerelease](https://github.com/acidkill/jev-advisor-for-codex/releases/tag/v0.1.0) tags commit `4f83d3dca0cbe881e614ecd575769d9e58436b7a`. Its attached wheel and sdist have verified GitHub SHA-256 digests `64462c751749fb153b470cd4b0330365ee79be8cbd75962d30d99094fa5b0f12` and `39773eae6c00a915b17aeefe39c3aace3a9ad1adaf3900167436eaa3038a38e1`. From that commit, 154 local tests passed; the distribution-content check passed; isolated Linux pipx installation, duplicate installer invocation, and doctor passed, with one registration of each advisory hook and no Jev PreToolUse. The four-pair sample is reported above with its limits. GitHub Actions runners did not start because GitHub reported failed account payments or a spending limit, so hosted CI provides no test result. macOS remains unverified; TestPyPI/PyPI and public visibility are separate decisions. This is a usable private alpha, not accepted efficacy.

## Downloaded release artifact verification (2026-09-24)

An authenticated download of the attached private v0.1.0 wheel matched the GitHub asset API SHA-256 `64462c751749fb153b470cd4b0330365ee79be8cbd75962d30d99094fa5b0f12`. In an isolated Linux profile, `pipx` installed that downloaded wheel; two installer runs kept exactly one `UserPromptSubmit` and one `SubagentStart` registration with no `PreToolUse`. `jevcompass doctor` passed at 0.1.0 and keyless explicit recommendation returned a labeled local fallback. This is distribution-to-install evidence, not host trust, Desktop delivery, macOS runtime, or efficacy evidence.

## Released-wheel fresh CLI hook delivery (2026-09-24)

The wheel downloaded from the private v0.1.0 release was installed into a fresh isolated pipx and Codex profile. Its SHA-256 matched the GitHub asset digest. With no OpenRouter key, a synthetic repository-setup prompt received first assistant event 4 containing advice ID `3dfbb31a` and candidate IDs `exec_command` and `git`; first tool event followed at 5 and exited 0. The local `UserPromptSubmit/project-setup/local` metric carried the same ID, took 31.31 ms, and contained only allowlisted fields, with no task prompt or fixture text. The isolated CLI run used `--dangerously-bypass-hook-trust`; it does not validate normal user approval of hooks. Evidence is private under `/tmp/jevcompass-wheel-runtime.KR9WFo/` (`evidence.json`, Codex JSON events, advisor metric, doctor result). This verifies one actual delivery from the distributed package, not task usefulness, remote Jev selection, Desktop prompt delivery, or macOS runtime.

## Current Desktop explorer negative smoke (2026-09-24)

A new native Desktop `explorer` child answered a read-only question about the synthetic whitespace fixture. Its first commentary reported **no JevCompass advice ID or candidate IDs** before its first tool. Local hooks configuration still lists `SubagentStart` for `explorer|worker`, but the available safe metric stream has no identifiable new SubagentStart record for this invocation. Since diagnostic correlation is absent, this run cannot distinguish a hook that did not run from a silent low-signal decision. The prior positive Desktop explorer smoke preceded the current catalog abstention calibration and does not prove current named-role delivery. Record this as no delivery observed, with hook reload/trust/invocation investigation on the roadmap; do not count it as accepted coverage.

## Installed adapter explorer abstention diagnostic (2026-09-24)

The active `SubagentStart` registration targets the pipx JevCompass 0.1.0 Python environment. Its actual safe metric file is `~/.local/state/jevcompass/advisor.jsonl`; the older `~/.codex/log/jev-advisor.jsonl` belongs to a legacy integration and is not evidence of current hook activity. A direct synthetic `SubagentStart` event for `explorer`, with the OpenRouter key absent, exited 0, emitted no context or stderr, and appended `codebase/low-signal-skip` in 48.46 ms. The current vanilla profile has only a generic shell candidate for that role, so silent abstention is consistent with product policy. This direct invocation tests the installed adapter, not Desktop's hook loading or trust. VCR-73 therefore remains ambiguous at the host invocation layer, and no Desktop delivery is credited.

## Current acceptance gate audit (2026-09-24)

| Gate | Evidence | Decision |
|---|---|---|
| Historical routine no-call behavior | Six randomized CLI pairs completed without blocks or credential-like output, but they inspected the private JevCompass repository with custom smem hooks | Historical behavior only; excluded from the synthetic privacy gate |
| Desktop named-role delivery before first tool | Correlated `SubagentStart` advice ID `968a1d2b` in hook metric and `explorer` first reply | Pass for one smoke, not coverage |
| CLI default-role advice | CLI `spawn_agent` exposes no `agent_type`; product matcher and policy intentionally skip `default` | Unmet for generic CLI children |
| Mode-independent substantive prompt advice | Fresh CLI 0.155.1 local-fallback smoke returned ID `bf04acfe` and `exec_command` before the first tool; Plan UI signal remains unavailable | Pass for one local smoke; cross-host and paired coverage remain open |
| Remote Jev choice through fresh CLI hook | Trace `5914ac30` appeared in the first assistant response before the first tool; matching metric is `UserPromptSubmit` / `review` / `jev` / 696.03 ms, and the response named `git` plus `code-review-excellence` | Pass for one synthetic smoke; p95 and usefulness remain unproven |
| Synthetic 20-case bank and host balance | Bank prepared with 10 core cases per host; zero arms executed. Three extra Desktop routine controls are specified outside the core count | Incomplete |
| Live privacy boundary for the synthetic pilot | No fresh arms have run; historical private-repository CLI pairs do not establish the boundary for this synthetic bank | Unproven pending explicit destination/payload approval and live verification |
| Advice usefulness ≥80%, eligible coverage ≥90%, Jev p95 <2 s, faster first useful action | Desktop and CLI delivery smokes plus a synthetic benchmark do not establish paired usefulness or host-wide coverage | Unproven |

**Release decision: do not mark automatic-advice acceptance as passed.** The explicit recommendation entrypoint remains useful for tasks the local classifier intentionally skips. Continue the live study using substantive prompt intent across ordinary permission modes; do not wait for a Plan UI field that Codex does not expose. SubagentStart supplies `agent_type` but no task text; JevCompass now targets only supported role-level profiles and skips generic or custom roles. This closes the VCR-04 design gap, not live pilot acceptance. Planned surreal-memory benchmarks can affect wall time; keep hook-local Jev durations separate and do not modify that server.

## Fresh isolated CLI source-review delivery (2026-09-24)

A fully synthetic W02 fixture was copied into a fresh temporary workspace; its evaluator key stayed outside the agent's workspace. An isolated `CODEX_HOME` held only JevCompass's two hooks and an ephemeral local copy of Codex auth, with no OpenRouter key, and the CLI ran `codex exec --json --ephemeral --sandbox read-only --skip-git-repo-check --dangerously-bypass-hook-trust`. The first attempt was rejected before the agent started because the fixture lacked Git metadata; the second attempt exposed a real classifier false positive (`initial` matched `init`) and is excluded from delivery evidence. After VCR-29, the successful run exited 0 in 26.27 s.

Its first agent message (event 4) was `93dcedec — exec_command`, before the first command tool event (event 5). The matching local metric recorded `UserPromptSubmit` / `source-review` / `local` / 3.28 ms / trace `93dcedec`. The synthetic draft remained unsent and no corrected artifact was created. This verifies one CLI-local advice delivery and ID correlation in an isolated profile; it does not verify ordinary hook trust, remote Jev selection, Desktop delivery, blinded usefulness, or any paired case. The agent's task result was not scored against the separate evaluator key in this smoke.

## Synthetic runtime evidence (2026-09-23)

The installed Python client made one live, synthetic Decisions API request and received the expected `pytest` choice with confidence 1 in 540 ms. In a 20-case fixture run using a fresh cache per case, 12 of 13 eligible cases produced advice, all 7 routine cases stayed silent, and eligible-call p95 was 505 ms (maximum 509 ms). The remaining debugging case returned confidence below the local 0.5 threshold and was intentionally skipped. This is 92.3% synthetic coverage, not host delivery or blinded usefulness evidence.

`jevcompass doctor` passed after installation. Direct installed-hook invocation produced advice for both a Plan mode event and an explorer start. A fresh CLI ordinary prompt invoked the prompt hook and correctly received no Jev advice. A new Desktop subagent did not receive SubagentStart advice in its initial context; investigate hook reload, trust, matching, and host delivery before acceptance. In an interactive Codex CLI 0.155.1 Plan mode session, the agent explicitly reported no JevCompass advice in initial context despite the Plan UI; automatic CLI plan delivery remains unverified and the explicit `recommend` command is the supported fallback. The same CLI session reported an unrelated invalid `Stop` hook JSON result from the existing integration.

## Host smoke evidence (2026-09-23)

A read-only Codex CLI 0.155.1 run with `JEV_ADVISOR_DIAGNOSTIC=1` recorded `UserPromptSubmit` as `bypassPermissions` and skipped it, as intended outside confirmed Plan mode. The same run recorded `SubagentStart` for the `explorer` profile as a cache hit in 37.33 ms with diagnostic ID `14f4b002`; the CLI parent response reported that ID and the selected identifiers `serena` and `code-review-excellence`. This corroborates that the hook ran and its advice appeared in model-visible context. The CLI JSON exposed no child thread ID, agent state, or child tool transcript, so this smoke is not counted as a fully verified before-first-tool delivery case and does not count toward the 20 paired cases.

A separate `--ephemeral` attempt logged a `SubagentStart` Jev result but failed to register a child thread; it is excluded from acceptance evidence. At the time of this earlier smoke, no paired or blinded live study had been completed.

## Local validation evidence (2026-09-23)

- The full unittest suite passed on CPython 3.11.15, 3.12.13, and 3.13.13: 37 tests on each version.
- `uv build` on CPython 3.14.7 produced one wheel and one sdist; the distribution-content checker passed against both archives.
- The wheel installed into a temporary isolated pipx environment on CPython 3.14.7, and the installed `jevcompass --help` exposed the `hook`, `recommend`, `doctor`, and `install` commands.
- GitHub Actions run 35897338176 did not start any job steps. Its check annotation reports failed recent payments or an insufficient Actions spending limit; account billing or the limit must be resolved before CI can be rerun.

## Fresh isolated vanilla CLI prompt delivery (2026-09-24)

A fresh Codex CLI 0.155.1 session used a temporary CODEX_HOME, isolated HOME/cache/state, a read-only synthetic fixture, and only JevCompass's two hooks. The OpenRouter key was intentionally absent. One request classified as `project-setup` had no local singleton and correctly produced no advice in 8.67 ms; this verifies fail-open behavior only. A separate `codebase/python` request received local fallback advice ID `c218487d` and the selected identifier `exec_command` in the first agent message, before the first shell tool event. Its matching metric was `UserPromptSubmit` / `codebase` / `local` / 6.44 ms; the CLI exited successfully and made no file changes. This verifies fresh CLI hook delivery for local fallback. It does not verify remote Jev selection, Desktop delivery, or any paired-case usefulness score.

## Isolated vanilla CLI spawn-hook feasibility probe (2026-09-24)

An isolated Codex CLI 0.155.1 profile temporarily used a `PreToolUse` probe solely to inspect the shape of one synthetic native subagent spawn. The event tool name was `collaborationspawn_agent`; `tool_input` had `message` and `task_name` keys, but the 204-character `message` was Fernet-shaped and did not contain the synthetic child-task sentinel. The probe logged only the tool name, field names, type, length, and sentinel/encryption-shape booleans; it never logged the message. The temporary hook also returned only a non-blocking marker. This confirms that the hook can run on the spawn call but cannot safely classify that child task from its encrypted message or tailor the child's initial context. No PreToolUse hook was added to the product or active Codex configuration. Keep named-role SubagentStart advice and skip the generic role unless Codex exposes safe task metadata.

## Required evidence

Run 20 paired, anonymized tasks in randomized order:

- 8 substantive user prompts across ordinary permission modes and distinct task intents; do not require a Plan UI signal
- 6 subagent starts for roles supported by the host; record generic default-role skips separately
- 6 routine tasks that should not trigger Jev

For each pair, record baseline/Jev arm, host and version, hook input mode/role, advice ID visible before first tool selection, first productive action time, recommendation usefulness, tool availability, required instruction/test coverage, blocks, data disclosures, and Jev duration. The evaluator should not know which arm used Jev.

## Acceptance thresholds

- Zero blocks or privacy disclosures
- No omitted mandatory instructions, review, or tests
- At least 80% of eligible recommendations marked useful
- At least 90% coverage of eligible events
- Jev-call p95 below 2 seconds
- Better time to first productive action on eligible tasks
- Routine tasks show no Jev calls or material latency regression

Score explicit jevcompass recommend trials separately from automatic hook coverage.

## Evidence table

Six live paired CLI routine controls are now recorded in the corrected evidence directory below. They test silent behavior, not advice delivery; no Plan or eligible subagent pair has been accepted. Do not treat configuration presence, a hook log, a direct script invocation, or a synthetic test as proof of agent delivery.

## Fresh acceptance smoke (2026-09-23, CLI)

This section records targeted live smoke evidence only. It is not part of the blinded paired pilot.

- In a fresh Codex CLI 0.155.1 session, `/hooks` showed three active `UserPromptSubmit` handlers, one active `SubagentStart` handler, and zero `PreToolUse` handlers.
- The CLI visibly entered Plan mode, but the real `UserPromptSubmit` event carried `permission_mode=bypassPermissions`; the Jev hook recorded `mode-skip` (trace `4fc79918`). Automatic plan advice therefore remains unavailable on this CLI path. The explicit `jevcompass recommend` command remains separately usable.
- `jevcompass doctor --json` passed all checks when its public OpenRouter metadata request ran outside the network-restricted sandbox. The sandboxed run could not reach that public endpoint; it was not a model-availability failure.
- One billed synthetic `jevcompass recommend --category testing --domain software --role planner` call returned `sequential-thinking` and `python-testing-patterns`; hook timing was 716.11 ms. This validates the explicit API path, not hook delivery.
- A fresh CLI `explorer` start produced a cache hit in 39.93 ms (trace `d499a687`). The child's only first response, before any tool call, contained the diagnostic ID and the selected identifiers `serena` and `code-review-excellence`. A prior explorer smoke (trace `975cef1e`, 41.85 ms) surfaced the ID but did not echo the selected identifiers; the stricter follow-up confirmed the complete injected context.
- Ordinary/non-Plan prompt events recorded `mode-skip` in 0.03-0.1 ms and did not call Jev.
- The CLI also reported invalid Stop-hook JSON from the configured `codex-smem-hook stop`. This is owned by the existing smem integration and is separate from JevCompass.
- Desktop delivery remains unverified in this run: the computer-use inventory exposed no native app window and this task has no attached app terminal. The user reported restarting Desktop, but no fresh Desktop hook trace or agent-visible context was available here.
- At the time of this earlier smoke, the blinded 20-pair study was 0/20. These smoke tests do not satisfy paired usefulness, coverage, p95, or time-to-first-action acceptance thresholds.

## Host contract follow-up (2026-09-23)

The [Codex hook runtime](https://github.com/openai/codex/blob/main/codex-rs/core/src/hook_runtime.rs) derives `UserPromptSubmit.permission_mode` from the approval policy, while the [hook input schema](https://github.com/openai/codex/blob/main/codex-rs/hooks/schema/generated/user-prompt-submit.command.input.schema.json) has no collaboration-mode field. This is consistent with the observed CLI Plan UI event carrying `bypassPermissions`. JevCompass must not infer Plan mode from prompt text; its explicit `recommend` command is a separately scored fallback. Automatic Plan delivery remains an unmet host contract, not an accepted case.

A strict `explorer` probe through this task's collaboration API answered `NO JEVCOMPASS ADVISORY` as its only initial response, before using tools. The local advisor log appended a `SubagentStart` `codebase` cache hit (50.94 ms) without a diagnostic trace. This proves neither delivery nor a native Desktop failure: the collaboration API route and native Desktop subagent route have not been shown equivalent, and the log lacks a correlation ID. A fresh native Desktop child transcript with matching diagnostic ID and recommendations before its first tool is still required.

## Isolated CLI harness correction (2026-09-23)

An isolated A/B harness copied the same Codex configuration and smem hooks to both variants, adding only JevCompass hooks to the full variant. The full variant completed a trivial no-tool prompt with exit 0 in about 6 seconds. Early baseline attempts used the wrong `CODEX_HOME` parent and ran in a network-restricted sandbox; they either timed out or received HTTP 401 from the unauthenticated parent directory. With `CODEX_HOME` set to the populated `.codex` directory and network access, the baseline completed the same prompt with exit 0 in about 15 seconds. Startup also warned about missing plugin installations in the isolated profiles. These runs were not randomized, equivalent production profiles or a tool-selection task, so they are unscored smoke evidence and do not count toward the 20 paired cases. The earlier suspicion that smem `Stop` caused the timeout is unsupported by the corrected network and authentication evidence.

## CLI routine negative-control pair (2026-09-23)

An additional randomized, read-only pair ran the same hook-test search and Git status task, full then baseline. Both completed with exit 0 and no repository edits. The full arm completed in 38.072 seconds after an initial sandboxed attempt had timed out at 180 seconds on network disconnects; the baseline completed directly in 38.829 seconds. The global JevCompass log recorded a fresh `UserPromptSubmit` `mode-skip` at 0.04 ms during the completed full arm (trace `4386468a`), and neither arm displayed advice before the first useful tool. This supports the routine no-Jev and no-block behavior, but the retry and startup history invalidate a paired time-to-first-action comparison; it is not scored in the 20-pair blinded study. Raw receipts remain in `/tmp/jevcli-pair-nyg4c9m1/evidence/`.

## Eligible CLI explorer pair attempt (2026-09-23)

The randomized baseline-first attempt did not start Codex because its timing wrapper referenced a missing `/usr/bin/time` (exit 127). After switching the wrapper to Python's monotonic clock, automatic approval review rejected the authenticated networked Codex launch before either arm started. The reviewer cited possible external transmission of private repository contents and session metadata by hooks. No alternate launch was attempted, no advice or timing was measured, and this case is not counted. The specific pending action is a read-only, authenticated CLI A/B explorer test with baseline and JevCompass hooks in isolated profiles; it requires explicit user approval of the networked invocation before continuing.

## Desktop collaboration subagent probe (2026-09-23)

A new `luna_worker` in this Desktop task was instructed to report only its initial JevCompass advisory before using tools. It answered `NO JEVCOMPASS ADVISORY`. The global advisor log appended a `SubagentStart` `operations` cache entry during the probe window (latest observed 34.36 ms), but the production hook had no diagnostic trace enabled; adjacent subagent starts also logged the same category. Thus this is a reproducible absence of advice in the child's reported initial context, with no exact event-to-child correlation. The collaboration subagent route has not been proven equivalent to a fresh native Desktop task's subagent route. Do not count this as a delivered Desktop advisory or as a fully diagnosed host failure.

## Explicit approval retry (2026-09-24)

The user approved the requested authenticated networked read-only CLI A/B test. Both staged run homes under `/tmp/jevcli-pair-nyg4c9m1/runs/{baseline,full}/.codex` and their authentication files were found, and the configurations match. Automatic approval review nevertheless rejected the baseline launch before execution because the approval did not specify the potential payload and remote destinations of the active hooks. No arm ran. The pending decision must name the classes of task/repository/session data potentially sent to Codex and the configured remote memory service, while JevCompass only sends sanitized candidate metadata to OpenRouter. Do not bypass the review or count this attempt.

## Full Access CLI retest (2026-09-24)

After the user enabled Full Access, a read-only baseline `codex exec` run and a full-profile run both completed the same `DecisionsClient` search. The full run took 37.792 seconds. Both children repeated a historical diagnostic ID `975cef1e` from prior-session memory and explicitly reported no current selected tool/skill identifiers; that memory ID is excluded from delivery evidence. The full run recorded `UserPromptSubmit` `mode-skip`, but no fresh `SubagentStart` record in the global advisor log. Thus this pair did not verify advice delivery and remains unscored.

An interactive CLI full-profile probe trusted its eight isolated hooks and its child replied `NO JEVCOMPASS ADVISORY`. A nearby global `SubagentStart` operations cache entry (44.6 ms) had no diagnostic trace although the CLI process enabled diagnostic mode, so it cannot be correlated to this child and may be from a concurrent Desktop task. A second interactive prompt explicitly requested `agent_type=explorer`; the CLI 0.155.1 parent reported that its exposed `spawn_agent` tool had no `agent_type` argument, so it spawned the generic role and no traceable SubagentStart event was recorded. The available evidence does not prove that this CLI subagent route invoked the installed hook. Neither probe counts toward 20 pairs. The current `UserPromptSubmit` event still carried `bypassPermissions` even for plan UI in earlier smoke. These are host-contract failures to fix or isolate before the full pilot.

A separate, isolated `PreToolUse` probe matched only `^(Agent|spawn_agent)$` and returned a fixed, nonblocking `additionalContext` marker. The actual interactive CLI `spawn_agent` never invoked that hook (no local invocation log), despite the new hook being trusted. The child distinguished the marker repeated in the user task from any separate hook context and reported none. The probe was removed by restoring the isolated `hooks.json` backup. This does not justify activating a production PreToolUse hook; matcher coverage in CLI 0.155.1 remains unproven and at this stage the 20-pair pilot was 0/20.

The official Codex CLI 0.156.1 binary was downloaded to `/tmp` and tested against the same isolated full profile without replacing the installed 0.155.1 binary. Its parent again reported that its exposed `spawn_agent` lacked `agent_type`, used the default role, and its child reported no current JevCompass IDs before attempting a read-only `rg` search. The command host then failed before that search ran. Nearby global operations cache entries were not correlated to this CLI child. The newer CLI therefore did not provide acceptance evidence for SubagentStart, and the source at the tagged release still derives `permission_mode` from approval policy rather than Plan UI. No production hook or Codex installation was changed.

## One routine CLI control pair (2026-09-24)

In isolated baseline and full profiles, the same read-only `git status --short` task completed without blocks or edits. Baseline took 8.624 s; full took 13.680 s, and both reported the existing modified `PILOT.md`. The full profile's advisor log recorded `UserPromptSubmit` `mode-skip` (0.04 ms) because its event carried `bypassPermissions`; no Jev choice was made. This single unblinded control pair is excluded from acceptance. A later trust-state audit found that the isolated baseline profile had no trusted entries for its local `hooks.json`, while the full profile had nine; configuration presence alone did not prove the smem hooks ran in both arms.

## Rejected six-pair CLI routine run (2026-09-24)

A randomized six-pair read-only run finished all 12 CLI arms and showed `UserPromptSubmit` mode skips in the full profile, but it is **excluded from acceptance**. The baseline profile retained the smem hook definitions yet had no trusted local `hooks.json` entries, whereas the full profile had nine. The arms may therefore differ in active smem hooks, which could confound timing and behavior. Raw rejected evidence is retained at `/tmp/jevcli-pair-nyg4c9m1/evidence/routine-six/`; a corrected run must verify hook execution in both arms.

## Corrected six-pair routine CLI control (2026-09-24)

The rerun used `codex exec --dangerously-bypass-hook-trust -s read-only` identically in isolated baseline and full profiles. Random case and arm order was recorded before execution. All 12 arms exited 0 within 120 seconds. Per-arm receipts show `session-start injected`, `user-prompt-submit no-context`, `post-tool-use buffered`, and `stop captured` from smem in **both** profiles. Only the full profile produced JevCompass diagnostic entries, each ending in `mode-skip`; no Jev recommendation was injected and measured hook-local skip duration was 0.03–0.13 ms. Model/CLI wall times varied and are not attributed to Jev.

An independent reviewer scored anonymized A/B final answers and first actions without the arm key: all 12 final answers met the prompts and no mandatory miss, block, or credential disclosure was observed in the supplied records. After unblinding, the full profile's `license` case had an unnecessary first search of its isolated memory file that returned no matches; the model later answered correctly using repository metadata. This is retained as a quality observation, without attributing it to Jev because Jev was skipped. These six live CLI routine pairs provide negative-control evidence, but no eligible advice usefulness evidence and no Desktop routine coverage. The host-balanced 20-pair acceptance set remains incomplete. Concurrent planned surreal-memory server optimization and benchmarking can cause intermittent memory latency; this is not classified as a JevCompass outage or regression, and the server was not changed. Full receipts: `/tmp/jevcli-pair-nyg4c9m1/evidence/routine-six-trusted/result.md`; blind assessment: `/tmp/jevcli-pair-nyg4c9m1/evidence/routine-six-trusted/blind/review.md`.

## Isolated wildcard SubagentStart diagnosis (2026-09-24)

The installed product matcher `^(explorer|worker|luna_worker)$` excludes `agent_type=default`. The available interactive CLI `spawn_agent` route creates a default child and exposes no `agent_type` argument. In an isolated test profile only, replacing the SubagentStart matcher with `*` and using a fixed, nonblocking advisory produced one logged event (`role=default`, marker `JEVPROBE-26a5a7a4`). The fresh CLI 0.155.1 child's **first reply before any tool** contained exactly `JEVPROBE-26a5a7a4`; its subsequent read-only search located `DecisionsClient`. This verifies that the host delivered `additionalContext` to this default child and locates the missing production advice at the role matcher and generic-role policy, rather than at hook delivery. The probe did not call Jev, so it does not establish usefulness or latency of a generic recommendation. The isolated profile was restored to its original matcher; production hooks were not changed. Automatic advice for default children remained unaccepted; no paired case had been scored at that stage.

In the current Desktop task, a newly spawned `explorer` child reported **before its first tool** that no fresh JevCompass diagnostic ID or selected identifiers were visible. It excluded historical ID `975cef1e` and then found `DecisionsClient` through Serena at `src/jevcompass/decisions.py:38`. The shared global advisor log has uncorrelated entries and cannot establish whether this specific child's hook ran. This current-task observation is not a fresh Desktop session acceptance smoke and does not resolve trust, reload, or delivery there. The production `~/.codex/hooks.json` still contains a trusted SubagentStart entry and no active Jev PreToolUse. The product's actual current metrics path is `~/.local/state/jevcompass/advisor.jsonl`; the old `~/.codex/log/jev-advisor.jsonl` belongs to the prior adapter and must not be used as evidence for this product. The current metrics file has SubagentStart entries today, but without a diagnostic trace tied to this specific Desktop child they cannot establish whether its hook ran or its advice was delivered. The separate isolated CLI profile recorded the routine prompt `mode-skip` in its own `~/.local/state/jevcompass/advisor.jsonl`.

A controlled second Desktop `explorer` probe used the product log's line count as a boundary: it rose from 102 to 103 immediately after the child spawned, with exactly one new `SubagentStart` / `codebase` / `cache` entry (34.87 ms), while the child's first response before any tool was `NONE` for a new diagnostic ID or separate advice with selected IDs. A direct synthetic invocation of the installed `jevcompass hook` for `explorer` returned valid `hookSpecificOutput.hookEventName=SubagentStart` and 486 bytes of `additionalContext` including `serena` and `code-review-excellence`. This is strong evidence that the advisor ran but its context was not visible to that Desktop child; line-count correlation lacks an embedded child/session ID and is not a formal delivery trace. No acceptance credit is assigned to that pre-update probe.

## Correlated Desktop delivery after advice ID update (2026-09-24)

The product now emits a fresh random `JevCompass advice ID` in each nonempty advisory and writes the same ID in its local category/status/duration metric. The installed pipx environment was updated from the current source without changing hook definitions; 39 unit tests passed after adding a regression check that ordinary prompts generate no ID, Jev request, or metric. A direct installed-hook invocation produced valid `SubagentStart` JSON and a matching context/metric ID. In a subsequently spawned Desktop `explorer`, the product log advanced by exactly one entry: `SubagentStart`, `codebase`, `cache`, 36.26 ms, `trace=968a1d2b`. **Before using any tool**, the child independently reported the exact ID `968a1d2b` and selected tool `serena` plus skill `code-review-excellence`; the parent prompt supplied no value for the ID. This verifies delivery to this Desktop named-role child. It supersedes the earlier uncorrelated negative child observations but does not prove every Desktop session, automatic Plan delivery, usefulness, or the 20-pair study.

## Vanilla CLI prompt delivery smoke after VCR-02 (2026-09-24)

A fresh Codex CLI 0.155.1 `codex exec --json` session ran in a temporary Git repository containing only a synthetic Python timeout example with `--sandbox read-only`. Its first agent response (JSON event 5) reported fresh JevCompass advice ID `bf04acfe`, tool `exec_command`, and no skill IDs; this preceded the first tool event (JSON event 7). The following shell call inspected the synthetic `sample.py` without changing it. The matching local metric is `UserPromptSubmit`, category `codebase`, status `local`, duration 36.8 ms. Configured-only MCP candidates were excluded, leaving one available universal tool, so the advisor skipped a remote Jev call. The hook process had no `OPENROUTER_API_KEY` in this CLI environment; the smoke therefore verifies vanilla local fallback and delivery, not remote Jev selection. This is not evidence of Desktop delivery, paired usefulness, or acceptance.

## Vanilla subagent role contract update (2026-09-24)

The current Codex hook schema provides `agent_type` to `SubagentStart`, but no delegated task description. Codex documents `default`, `worker`, and `explorer` as built-in agents. JevCompass registers role-level advice only for `explorer` and `worker`; it skips `default` and custom roles, including the local-only `luna_worker` profile. The advisor does not inspect transcript files or infer task content. Synthetic regression tests exercise vanilla event fields and verify that transcript, session, and project paths are not forwarded to Jev. This resolves the VCR-04 product-scope question; live delivery and usefulness are still measured separately in VCR-05.

## VCR-05 focused vanilla catalog spot-check (2026-09-24)

Safe manual `recommend` calls used only allowlisted categories, domains, role, and reviewed catalog metadata; they did not transmit a task prompt, source code, or path. The initial generic `coding/software` call returned `exec_command` plus `create-plan` (657.48 ms), although this category did not express planning intent. After removing that planning-only skill, a second call selected `api-design-principles` (586.97 ms), which was also too specialized for generic coding. The catalog now gives API design and documentation separate pools and restricts planning, Kubernetes, and packaging skills to matching task kinds/domains. A final generic coding call selected only `exec_command` (620.73 ms); `testing/python` selected `pytest` plus `python-testing-patterns` (632.82 ms). These are unblinded category-level spot-checks, not prompt-level usefulness scores, first-tool delivery tests, or any of the 20 paired cases.

## VCR-07 keyless ambiguous shortlist smoke (2026-09-24)

A blank `CODEX_HOME` fixture with skill and configured-MCP discovery disabled produced the reviewed `project-setup` candidates `exec_command` and `git`. With `OPENROUTER_API_KEY` unset, the real Decisions client stopped before HTTP transport and the advisor returned both IDs under `Local unranked fallback; Jev did not select these candidates.` The local metric was `UserPromptSubmit` / `project-setup` / `local` / 0.42 ms; output contained no `decision` or `continue` field. The full suite passed 53 tests and 36 subtests. This is synthetic in-process contract evidence, not fresh-agent delivery, remote Jev selection, or paired usefulness evidence.

## VCR-09 built-in shell availability (2026-09-24)

The curated `exec_command` entry now uses a dedicated Codex shell availability check instead of searching for `bash` on `PATH`. With no config file, the check follows Codex's documented default (`features.shell_tool` enabled); an explicit `[features] shell_tool = false` marks it unavailable. Tests also cover explicit enablement, malformed TOML, the no-`bash` host case, and independent `git` PATH detection. The full suite passed 55 tests and 39 subtests. This local configuration signal does not prove tool exposure in a particular live model/session; the agent must still confirm availability there.

## VCR-08 default-role recommendation audit (2026-09-24)

Serena review confirmed that vanilla `SubagentStart` supplies `agent_type` but no delegated task description. An isolated wildcard-matcher CLI probe showed a `default` child receives `additionalContext` before its first tool, but it carried only a marker and did not call Jev or test advice usefulness. There are no scored mixed-task recommendations for this role. A generic code-oriented profile could recommend shell, Git, or code-navigation tools for unrelated tasks and repeat guidance that is already obvious. Decision: keep `default` and custom roles silent. Revisit only with blinded mixed-task cases scoring recommendation usefulness, first action, candidate availability, and noise; continue to verify no task or private session context enters Jev requests.

## VCR-06B remote Jev selection through a fresh CLI hook (2026-09-24)

A fresh Codex CLI 0.155.1 `codex exec --json --sandbox read-only --ephemeral` session ran in a temporary Git repository containing only a synthetic Python example. Local classification was `review/python`; the Jev request is constructed from coarse category/role/domain and curated catalog candidate metadata, not the user prompt, file contents, or repository path. The review-candidate cache was a miss before the run.

The first assistant response appeared at JSON event index 3, before the first tool event at index 4. It reported fresh advice ID `5914ac30`, mentioned the `git` recommendation and the optional `code-review-excellence` skill, then identified `exec_command` as the one read-only operation it would use. The run exited 0. The matching local metric is `UserPromptSubmit` / `review` / `jev` / 696.03 ms, which distinguishes a live Jev decision from local fallback or a cache hit.

This confirms one remote selection reached a vanilla CLI agent before its first tool. The model chose not to use the optional skill for this tiny check; this smoke does not score advice usefulness, count toward the 20 paired cases, or prove Desktop prompt-hook coverage.

## Pi + Jev custom-harness article review (2026-09-24)

Reviewed ["Building a Custom Harness with Pi and Jev"](https://x.com/omarsar0/article/2102762406204076532) and its [DAIR.AI Academy tutorial page](https://academy.dair.ai/resources/jev-decisions-in-a-pi-sdk-harness). The article demonstrates three decision points inside a custom Pi agent loop: choose a model before a run, gate each tool call, and grade the final answer. It also recommends typed batched questions, policy thresholds in ordinary code, different failure behavior for routing and safety gates, bounded verification retries, and sanitized decision logs. The tutorial labels its sample policies as educational rather than production-tuned.

Transfer assessment:

- **Already adopted:** JevCompass batches separate `choice` questions for tool and skill candidates, validates selected IDs and confidence locally, builds the advice locally, caches decisions, logs only safe metrics, and falls back to a local unranked shortlist when Jev is unavailable or uncertain.
- **Defer model routing:** the article controls the model at the start of a custom run. JevCompass's vanilla Codex hooks can add advisory context, but do not expose a documented field to change the active model; do not imply that an automatic route occurred. Revisit an explicit pre-run model/effort suggestion only after core cross-host acceptance.
- **Do not port the broad tool gate:** Pi's `beforeToolCall` has a direct block/terminate decision. A global Codex `PreToolUse` gate previously interfered with routine work, and Codex `ask` is not a supported hook decision. Keep ordinary operations outside Jev; any permission assistance remains the separate, opt-in VCR-11 investigation.
- **Do not add automatic answer grading now:** the example sends answer and supporting tool/file evidence to Jev and retries. That would expand private-data transmission and duplicate authoritative tests/review without proving a vanilla Codex hook can safely access the right evidence. Keep test outcomes and repository-required checks decisive.

The useful lesson is to keep Jev's judgments narrow and typed while policy, fallbacks, and evidence stay in local code. No article-derived code change is justified before VCR-06 cross-host delivery and the paired usefulness pilot are complete.

**Current Codex verification surface:** the official [Hooks guide](https://learn.chatgpt.com/docs/hooks) documents that a `Stop` event can include `last_assistant_message` and `stop_hook_active`; returning `decision: "block"` continues the turn with a new prompt. This makes a bounded verifier technically possible in vanilla Codex. The event does not provide stable task criteria or tool evidence, however, and the documented transcript path is explicitly not a stable hook interface. Do not parse transcripts or send final answers to Jev by default. VCR-15 tracks a later, opt-in feasibility test requiring an explicit rubric/evidence source, a strict one-retry cap, and local tests as the authority.

A separate vanilla-onboarding opportunity is VCR-13: Codex's plugin format can package skills, MCP configuration, and lifecycle hooks for Desktop and CLI, but hooks still need an executable in the local environment and explicit trust. Evaluate that distribution layer after core acceptance; it does not make Pi SDK hooks or model-routing controls portable.

## Synthetic classifier regression fix (2026-09-24)

A local synthetic regression test reproduced review prompts containing bug/regression wording being routed to `debugging`. The classifier now prioritizes review before incidental debugging terms, recognizes English `fix`/`defect` wording for debugging, prioritizes documentation over incidental test mentions, and maps Bash/Shell mentions to the software domain. Three focused classifier/contract tests pass, and the full suite passes (72 tests, 55 subtests). Graphify code artifacts were rebuilt using the installed pipx interpreter because this repository does not contain `graphify-out/.graphify_python`. This is local classifier evidence only: it does not count toward recommendation usefulness, host delivery, or the 20-case paired pilot. No private prompt or code was sent to Jev.

## Classifier token-boundary regression fix (2026-09-24)

While validating routine-control wording, `fix\w*` was found to match the beginning of `fixture`, routing simple file-listing requests to debugging. The English fix alternatives now require complete action forms, preserving `fix`, `fixed`, `fixes`, and `fixing` while leaving `fixture` unclassified. Regression tests cover two routine fixture prompts and a Bash fix task. The full suite passes (73 tests, 55 subtests); this is still local synthetic evidence, not a live pilot score.

## VCR-06D matched case bank prepared (2026-09-24)

The 20-case synthetic core is recorded in [PILOT_CASES.md](PILOT_CASES.md): eight substantive prompts split evenly between CLI and Desktop, six named-role subagent starts on Desktop, and six CLI routine controls. This makes 10 core cases per host. Three extra Desktop routine pairs are specified outside the core count to test silent behavior there. The earlier six CLI routine pairs inspected the private JevCompass repository with custom smem hooks; they remain historical behavior evidence and are excluded from this synthetic acceptance bank.

Local preflight confirmed all eight prompt texts classify to their expected category/domain and all nine routine prompts return no classification. It made no Codex or OpenRouter request. No core matched arms have run; subagent usefulness, fresh Desktop prompt delivery, synthetic privacy, and all acceptance thresholds remain unverified.

## VCR-46 generic-role advice calibration (2026-09-24)

A role-only `SubagentStart` event provides no task description. After the generic coding pool was narrowed, a vanilla-like `explorer` or `worker` role with only `exec_command` would receive the same obvious shell suggestion. The advisor now records `low-signal-skip` and emits no context for that exact singleton, while keeping prompt singletons and role profiles with specific candidates. Direct synthetic `explorer` and `worker` hook calls exited 0 with empty output; the explorer decision took 41.6 ms and made no Jev call. Earlier Desktop explorer trace-delivery smoke remains historical evidence for the previous catalog and is not evidence that the current shell-only role receives advice. Revalidate named-role coverage on both hosts when relevant capabilities are actually present. This calibration does not count toward the 20-case acceptance study.

## VCR-43 W02 equal-arm preflight diagnostic (2026-09-24)

The W02 runner now has optional `--preflight`, which appends the same request to both arms to report an advice ID and selected catalog IDs before any tool call. Its normal prompt is unchanged and neither answer nor command text is retained. Focused tests cover classification, event order, and bounded output. Two fresh randomized treatment-then-baseline pairs used the fictional W02 fixture. In both, treatment reported a trace before its first command, matching a `UserPromptSubmit/source-review/local` metric (first `2524df67`, 3.67 ms; second `f807974b`, 3.02 ms). Both reported zero selected IDs, consistent with a local unranked fallback rather than a Jev selection. The second baseline reported no ID or IDs, and both answers passed all four mechanical indicators. Its first command was 8.54 s treatment and 22.09 s baseline, but preflight and first-command timestamps do not establish first productive action or a stable speed gain. The first pair's full safe summary was not retained, so do not score it. This diagnostic establishes trace visibility when explicitly requested; it does not prove spontaneous consumption of advice or recommendation usefulness. W02 remains supplemental and core acceptance 0/20.

## VCR-42 second supplemental W02 CLI pair (2026-09-24)

A second randomized W02 pair ran treatment then baseline with `gpt-6-sol` in byte-identical fictional fixture copies (SHA-256 `0129740378da799de8da62d331d9f87cec7e22bdd66cccfc7b87370470fe1144`). Both arms exited 0 without changing files. Both deterministic answers indicated the canonical price/source, missing source-system count, and intended draft location. The unsent-draft keyword indicator was true for baseline and false for treatment; raw answer text was intentionally discarded, so the cause cannot be distinguished from a phrasing false negative. The first command arrived at 7.52 s treatment and 8.49 s baseline; neither time identifies the first productive action. Treatment logged `UserPromptSubmit/source-review/local` at 2.99 ms, trace `ff68fa93`, but the first assistant event did not echo the ID or selected shortlist. This is local fallback, not a Jev-ranked selection, and delivery remains unconfirmed in this pair. Compared with the earlier baseline-then-treatment W02 pair, there is no consistent first-command speed advantage. Both runs remain unblinded supplemental evidence, outside the 20-case core. Do not infer advice relevance or answer quality from the keyword checks.

## VCR-31 supplemental W02 paired CLI probe (2026-09-24)

The bounded `scripts/pilot_w02_pair.py` runner executed a randomized baseline-then-treatment pair in two temporary, identical copies of the fictional W02 fixture (`SHA-256 0129740378da799de8da62d331d9f87cec7e22bdd66cccfc7b87370470fe1144`). Both Codex CLI `gpt-6-sol` arms used read-only ephemeral profiles, the same prompt and a 120-second per-arm limit. The treatment profile installed only this checkout's advisory hooks; neither arm received `OPENROUTER_API_KEY`. The two arms exited 0 and each answer met all four deterministic indicators: canonical rate with source, missing source-system count, intended draft path, and unsent status. These indicators are a mechanical check, not a blinded quality rating.

The baseline's first actual command arrived at 12.32 s; treatment's at 14.54 s. The treatment hook logged `UserPromptSubmit / source-review / local` at 3.33 ms (trace `a7160d1e`), but the first assistant message did not contain that advice ID. Two startup `error` items were initially mislabeled as tools in the runner; the parser and regression test now exclude them, and the first-command times above use the actual `command_execution` events. The pair shows no time advantage and does not establish advice delivery. Raw CLI events, assistant text, credentials, and temporary profiles were not retained. This W02 probe is supplemental, outside the 20-case core; core progress remains 0/20. The next task traced the context in a fresh CLI session; see VCR-32 below. Recommendation usefulness still requires the core paired pilot.

## VCR-32 CLI context-delivery diagnosis (2026-09-24)

A test-only `UserPromptSubmit` hook in an isolated ephemeral read-only Codex CLI profile injected a fresh random canary and asked the agent to echo it before a tool. The first assistant event contained the canary before the first actual tool; the bounded probe returned only boolean evidence and retained no transcript, canary, or auth file. This establishes that the CLI host delivered `additionalContext` in that run, independent of JevCompass classification.

A separate treatment-only W02 run with an explicit pre-tool instruction confirmed the production advisor path: first assistant advice ID `203e5703` preceded the first command, and a matching `UserPromptSubmit/source-review/local` metric took 3.2 ms. Diagnostic mode also logged a preliminary `permission_mode=bypassPermissions` entry under the same trace; the pilot correlator now selects the decision entry rather than this diagnostic record. The earlier no-echo paired run remains a valid negative observation about observable ID repetition, not proof that the host dropped context. Neither diagnostic constitutes a blinded quality comparison, Desktop verification, or a core pilot pair; core progress remains 0/20.

## VCR-38 privacy-bounded blind receipt export (2026-09-24)

The CLI core runner accepts optional `--blind-dir DIR`. It writes opaque-token, per-arm JSON receipts into `DIR/receipts/` and a separate `DIR/mapping.json` for the coordinator. Share only the `receipts/` directory with a blind evaluator; retain the mapping privately until scoring is frozen. Receipts include case ID, coarse first-action type/time, bounded execution and mechanical outcome indicators, and an explicit limitation. They omit arm labels, advice IDs, prompt, response, commands, code, paths, and trace. Candidate IDs inferred from an assistant's first pre-tool message are kept only in the private mapping and are not proof of backend selection. Directory and file permissions are restricted, existing outputs are never overwritten, and blind-mode stdout omits per-arm data. Offline dry-run and unit tests verify this export; it does not itself establish blinded usefulness or acceptance. Native macOS and Linux are the current host targets; native Windows hook quoting remains [Issue #1](https://github.com/acidkill/jev-advisor-for-codex/issues/1).

## VCR-41 offline blind score reconciliation (2026-09-24)

The CLI runner has an offline scoring path for its synthetic blind receipts. A scorer receives only `DIR/receipts/` and the fixed case rubric, rates both opaque arms without knowing their labels, and saves a JSON file of the form `{"schema":"jevcompass-blind-human-scores-v1","scores":[{"arm_token":"TOKEN","first_productive_action_ms":null,"task_quality":null,"required_checks_preserved":null,"blocked":null,"privacy_disclosure":null}]}` with one row per receipt. Use `null` when the available evidence cannot establish a rating; do not infer task quality from exit status or metadata alone. Record `sha256sum scores.json` before the coordinator reveals `DIR/mapping.json`, then run `python3 scripts/pilot_cli_core.py --score-receipts DIR/receipts --score-mapping DIR/mapping.json --score-file scores.json --score-sha256 DIGEST`. The digest proves the score bytes matched the committed file, but does not prove the evaluator remained blind. Keep the mapping private during scoring; the scorer rejects missing, duplicate, malformed, or mismatched arms. It reports paired task quality and first productive action where rated, never promotes an incomplete dataset to acceptance. Recommendation usefulness requires a separate review of actual advice after unblinding; this metadata-only CLI export cannot establish its 80% threshold, delivery, Jev latency, routine silence, or Desktop coverage. No human score has been assigned to the existing P01 export, and core acceptance remains 0/20 fully scored.

Optional `--blind-quality-artifacts` with `--blind-dir` adds `quality_artifacts/` for the **fictional CLI fixture only**. Each opaque-token JSON contains changed contents of fixed allowlisted files for P01/P03/P05, or a bounded final answer for P07; routine cases produce no quality artifact. It does not include commands, transcripts, credentials, host paths, or baseline/treatment labels, and does not upload content to Jev. Keep `mapping.json` private until scoring is committed. Inspect every artifact locally before sharing the `receipts/` and `quality_artifacts/` directories with an evaluator: allowlists, size limits, symlink rejection, and common secret-pattern checks cannot prove that generated text contains no sensitive data. The earlier P01 export has metadata only; rerun a pair with this flag to obtain assessable output. The existing scorer still accepts the same score schema and does not infer task quality automatically.

A new randomized P01 CLI pair used equal `gpt-6-sol` medium settings, `--preflight`, and the opt-in quality export. Both arms exited 0, passed the focused unittest check, and produced bounded changed helper/test files. The blind evaluator saw only opaque receipts and artifacts; after one schema correction from string to boolean, its SHA-256-committed scores marked both task outcomes and required checks true. The scorer reconciled one rated quality tie, with blocked/privacy and first-productive-action ratings unknown; it explicitly returned `not_accepted`. After unblinding, the treatment arm reported `exec_command`, baseline reported no advice, and the first-action class/time was `other` / 9.73 s treatment versus `source_read` / 8.27 s baseline. Those are first-action heuristics, not productive-action timings. No OpenRouter key was forwarded into either arm; this does not measure remote Jev selection or prove usefulness. The local private evidence remains under `/tmp/jevcompass-vcr50-p01-20260924/`; do not publish its mapping. One CLI quality pair is rated, while **0/20 cases are fully scored for acceptance** across delivery, usefulness, timing, and both hosts.

A second randomized three-case CLI run (P07, P03, P05) used equal `gpt-6-sol` medium settings, equal preflight, and six private fictional quality artifacts. A separate evaluator scored the six opaque receipts/artifacts before arm mapping was read, then committed score bytes by SHA-256. The scorer reconciled **three task-quality ties**: both P03 Bash repairs met syntax and unset-variable checks; both P05 READMEs omitted the required `python -m tinytext --help` and remained incoherent on help, with the fixture's deliberately failing test unchanged; both P07 answers described contract elements, although the mechanical keyword indicator was false. Required-check ratings passed only for P03 and failed for P05/P07; do not treat keyword or score booleans as complete test evidence. After unblinding, the P03 treatment reported `exec_command`, `git`, and `browser-testing-with-devtools` even though the task was a Bash defect; P05/P07 treatment reported no IDs. First-action heuristics were P03 11.46 s treatment vs 8.32 s baseline, P05 9.94 vs 6.82 s, and P07 8.46 vs 9.99 s; these do not measure productive-action time. No key was forwarded and no remote Jev choice was measured. The scorer returned `not_accepted`; block/privacy ratings, advice usefulness, Desktop coverage, and productive-action timings remain unverified. Combined with P01, **four CLI prompt pairs have blinded task-quality scores, but 0/20 core cases satisfy all acceptance evidence**. Private evidence remains under `/tmp/jevcompass-vcr52-cli-prompts-20260924/`; keep mapping private.

After narrowing shell debugging candidates, a new equal-setting P03 pair ran with blind quality export. The separate blind evaluator rated both Bash outputs correct and required syntax/defect checks preserved; SHA-256-committed reconciliation returned a task-quality tie and `not_accepted`. After unblinding, treatment reported only `exec_command` (previously it had also reported Git and a browser skill); baseline reported no advice. The first source-read heuristic was 9.04 s treatment versus 7.38 s baseline. This confirms improved candidate relevance for one live CLI prompt, not faster productive action or improved output quality. Private evidence is `/tmp/jevcompass-vcr54-p03-shell-20260924/`; 0/20 cases have complete cross-host acceptance evidence.

A fresh P05 pair tested the initial `package-docs` checklist with a vanilla isolated profile (no `python-packaging` skill). A separate evaluator scored both fictional README artifacts before unblinding and committed the score-file SHA-256. The reconciler found **baseline better**: baseline retained the required `python -m tinytext --help` and coherent install/help indicators, while treatment replaced the module-form command with `tinytext --help`; both arms had a failed test-command exit from the intentionally broken starter helper. After unblinding, treatment reported only `exec_command`; the first source-read heuristic was 10.22 s treatment versus 7.39 s baseline. The checklist's generic phrase `actual CLI --help` may have allowed an unsupported command form; this is a hypothesis to test, not a proven cause. The result contradicts a quality benefit claim and remains `not_accepted`. Private evidence: `/tmp/jevcompass-vcr59-p05-20260924/`; 0/20 core cases have complete acceptance evidence.

After revising the local checklist to preserve the existing verified CLI invocation, another randomized P05 CLI pair was blindly evaluated from fictional README artifacts. The committed score reconciliation found **treatment better** on task quality: treatment retained the required module-form help and coherent install steps, while baseline used `tinytext --help` and failed the install coherence indicator. Both arms still had a failed test-command exit from the deliberately broken fixture. The first-action heuristic was 8.94 s treatment versus 9.31 s baseline, which is not productive-action time. Neither arm reported a JevCompass advice ID or candidate identifiers before its first tool; hook delivery and causal attribution are therefore unconfirmed despite the direct synthetic hook test. No remote Jev key was forwarded. This single pair does not establish repeatability, required-check completeness, privacy, or overall acceptance. Private evidence: `/tmp/jevcompass-vcr61-p05-20260924/`; 0/20 core cases have complete acceptance evidence.

## VCR-40 live synthetic blind-export smoke (2026-09-24)

One fresh P01 CLI pair ran with `--preflight --blind-dir` and the same `gpt-6-sol` medium setting in both arms. The runner returned completed status, two opaque receipts, no OpenRouter key forwarded, and no other hooks. Both arms exited 0 and recorded `focused_unittest_exit=true`; first-action classes were `other` (7.48 s) and `source_read` (11.07 s). The evaluator files contain no arm labels or candidate IDs; the separate mapping remains private. The output directory and receipts directory were mode 0700, mapping mode 0600. This validates live export and bounded mechanical checks on a fictional fixture. It is not a blinded usefulness score, a Desktop pair, or evidence of a speed improvement. The private ephemeral evidence is `/tmp/jevcompass-vcr38-live-20260924/`; share only its `receipts/` subdirectory with an evaluator. Core acceptance remains 0/20 fully scored.

## VCR-34/35 CLI core runner and first paired arms (2026-09-24)

The CLI core fixture and runner now cover P01/P03/P05/P07 and R01-R06 using isolated temporary profiles, byte-identical fictional project copies, explicit `gpt-6-sol`/medium settings, randomized arm order, and no OpenRouter key. The evaluator checklist stays outside the copied fixture. The runner's first live R03 pair (treatment then baseline) exited 0 in both arms. Treatment produced no advisor metric or advice ID, as expected for `Check whether README.md exists`; first actual tool arrival was 8.48 s treatment and 6.00 s baseline. This is a negative-control behavior observation, not a task-outcome score.

A P01 coding pair also ran treatment then baseline and exited 0 in both arms. Treatment recorded `UserPromptSubmit/coding/local` at 10.4 ms, with no ID echoed in the first assistant event. First actual tool arrival was 8.69 s treatment and 14.38 s baseline; both arms emitted a file-change event. These timestamps are not first productive action times, and a single randomized pair cannot establish a speed effect. The initial runner discarded temporary edited fixture and answer text before outcome checks, so neither arm is scored for implementation correctness or usefulness. Two core CLI pairs have executed, **0/20 are fully scored for acceptance**. VCR-35 added bounded, fixture-specific mechanical checks before temporary cleanup. It reads test exit codes only from completed Codex sandbox command events, never re-executes agent-modified Python on the host, and marks absent evidence unknown. `bash -n` remains syntax-only; P05 documentation is checked statically. The R05 evaluator key was corrected: no `.py` files exist directly at the project root, whereas nested files do. Blinded usefulness still requires separate rating.

With those checks in place, a fresh randomized P01 pair (baseline then treatment) completed. Both arms ran the fixture unittest suite inside Codex's workspace-write sandbox with exit code 0. Treatment's local `coding` hook metric took 3.56 ms, but the first assistant message did not echo its advice ID. The source-read command heuristic marked 9.28 s baseline and 18.34 s treatment; it is not a first productive action rating. There is no demonstrated speed benefit in this pair, and its absent echo leaves agent-visible advice unconfirmed. This adds mechanical test evidence, not a blinded relevance/quality score; 0/20 core cases are fully accepted.

## VCR-36 remaining CLI core pairs (2026-09-24)

Eight more randomized CLI pairs ran with identical temporary source copies, `gpt-6-sol`/medium, matching read-only or workspace-write settings, and no OpenRouter key. Every arm exited 0. R01, R02, R04, R05, and R06 produced true simple answer indicators in both arms; treatment emitted no Jev metric or advice, matching their negative-control role. Together with R03, all six routine controls have no Jev calls in the observed CLI pairs.

| Case | Mechanical outcome baseline / treatment | Treatment hook | First tool baseline / treatment |
|---|---|---|---|
| P03 | Bash syntax and unset-variable indicators true / true | `debugging/local`, 3.73 ms | 8.30 / 12.95 s |
| P05 | Install instruction coherent true / false; help coherent true / true; observed fixture test exit nonzero / nonzero | `documentation/insufficient-candidates`, 3.61 ms | 8.55 / 7.27 s |
| P07 | Contract keyword indicators false / true | `api-design/insufficient-candidates`, 2.86 ms | 8.87 / 8.61 s |

None of these treatments echoed an advice ID before its first tool. `insufficient-candidates` returned no advice, so P05 and P07 are actual coverage misses, whereas P03's local hook created context but its consumption is unconfirmed. P05's failed test reflects the fixture's deliberately broken whitespace helper; the documentation task did not require fixing it. The install coherence false is a local mechanical signal to investigate against the actual generated README, not a final quality verdict. P07's true indicator is also only a keyword check. These CLI runs give behavior and regression targets; **0/20 cases have blinded usefulness and first-productive-action assessment**, and Desktop pairs remain unrun.

## VCR-37 singleton shell advice trial (2026-09-24)

A temporary catalog trial added the built-in `exec_command` as the sole available vanilla candidate for `documentation/python` and `api-design/python`. A local smoke confirmed both categories would receive advice without pretending an uninstalled skill or MCP was available. Initial P07 treatment returned `api-design/local` in 4.37 ms and its keyword contract indicator was true (baseline false); a P05 treatment returned `documentation/local` in 5.93 ms but its install-instruction indicator was false (baseline true). Another isolated P05 treatment without the trial candidate produced coherent install guidance. This variation cannot establish a causal benefit or harm from the advice.

An optional `--preflight` mode now appends the same brief request for an advice ID before the first tool in both arms of substantive CLI cases. The original routine prompts stay exact. In a P05 preflight pair (baseline then treatment), treatment visibly reported ID `c913c6f5` before its first tool and correlated to `documentation/local` at 3.31 ms. Baseline install coherence was true and treatment false; first tool arrived at 7.07 s baseline and 8.64 s treatment. In a P07 preflight pair (baseline then treatment), neither first message visibly echoed advice; both keyword contract indicators were false. The trial's single obvious shell recommendation did not show reliable task or speed benefit. It was removed from the curated catalog; absent a more specific available tool or skill, vanilla API/documentation tasks remain silent. Preflight remains optional measurement instrumentation, not a normal user instruction or a scored blinded pilot.

## VCR-158 installed v0.1.12 CLI delivery smoke (2026-09-25)

The maintainer's pipx environment was updated from a digest-verified downloaded v0.1.12 wheel. `jevcompass --version`, pipx metadata, and `doctor` all report 0.1.12; `hooks.json` SHA-256 stayed `31cfe6a4f766aefa4bd73457891449b70f8dba34016de93c051547974fdd1b16`, preserving two advisory registrations and no Jev PreToolUse gate. A direct installed-hook synthetic event selected only the installed `openai-docs` skill and emitted an advice ID.

A fresh isolated Codex CLI 0.155.1 session used the **installed v0.1.12 wheel** (no source PYTHONPATH), temporary `CODEX_HOME`, a copied stock `openai-docs` SKILL.md, a fictional README, read-only sandbox, and no OpenRouter key. With `gpt-6-sol` / medium and an explicit preflight request, the first agent message (JSON event 5) reported advice ID `d7542a31` and `openai-docs`; the first tool started at event 6. The local metric in that isolated profile matched `UserPromptSubmit / codex-setup / local / 2.8 ms` and the same trace. Exit code was 0. A separate CLI attempt omitted the ID despite exiting 0; a further attempt showed the ID in the first response with a matching metric but did not record tool order. Thus this is one confirmed installed-wheel before-tool delivery, not a coverage estimate or effectiveness result. Fresh Desktop prompt delivery, macOS behavior, and blinded usefulness remain open.

## VCR-154 current Desktop explorer low-signal control (2026-09-25)

In the active Codex Desktop task, the maintainer's installed pipx JevCompass 0.1.11 and existing trusted SubagentStart registration were probed with a fresh `explorer` child on a synthetic fixture. Before its first tool, the child independently reported no fresh JevCompass ID or selected candidates, then located `normalize_whitespace` at `tests/fixtures/cli_core/tinytext/text.py:4` through a read-only search. The product metric file advanced from 394 to 395 lines with exactly one new `SubagentStart / codebase / low-signal-skip / 54.28 ms` event (`trace=e8781ffc`). The hook ran but intentionally emitted no advice because the available candidate pool contained only the generic shell. This is a low-signal negative control, not evidence of a host delivery failure or positive advice delivery. It does not verify the new `codex-setup` category on Desktop; that code has not yet been installed into the active pipx environment.

## VCR-153 Codex setup guidance CLI delivery (2026-09-25)

A new narrow `codex-setup` classification selects the installed `openai-docs` skill for explicit Codex Desktop/CLI hook and skill setup. The local catalog on this host also exposed generic `documents`, so the category explicitly excluded it. Unit tests cover the official skill, unrelated work, the absent-skill silent path, and the existing documentation precedence. A fresh isolated Codex CLI 0.155.1 session used a synthetic repository and only a copied stock `openai-docs` SKILL.md in its temporary profile; the treatment hook used this checkout's source, `gpt-6-luna` / low, and no OpenRouter key. The agent's first response contained advice ID `3af895c8` before first tool (first tool at 6432.77 ms); the matching local metric had `UserPromptSubmit / local / 2.75 ms` and the same ID. Exit code was 0. A direct local hook invocation showed only skill ID `openai-docs` in the advice. This is one CLI delivery smoke for synthetic setup, not a Desktop result, a remote Jev choice, a matched speed comparison, or a scored usefulness result.

## VCR-152 current release CLI negative advice controls (2026-09-25)

After installing the verified v0.1.11 release in the maintainer profile, two fresh isolated Codex CLI 0.155.1 synthetic pairs ran through the repository's `pilot_cli_core.py` harness with the same `gpt-6-luna` / low setting in both arms, no OpenRouter key, and preflight reporting in both prompts. Each arm exited 0. P01 (Python helper) had no advice ID; its treatment hook recorded `UserPromptSubmit / coding / low-signal-skip / 3.73 ms`. The baseline and treatment first tool times were 5535.48 and 5198.20 ms; treatment's focused unit test exited 0, while the baseline did not run that focused check. P03 (Bash defect) likewise had no advice ID; the treatment hook recorded `UserPromptSubmit / debugging / low-signal-skip / 5.61 ms`. First tool times were 5556.53 and 5236.71 ms. Both arms passed Bash syntax, while only the treatment passed the narrow unset-path fixture check. The harness isolated both workspaces, copied identical synthetic fixtures, and forwarded no OpenRouter key. These are mechanical regression observations, not scored usefulness or evidence of advice delivery: the current catalog correctly suppresses a lone generic shell candidate. They do not count as accepted blinded pairs or prove a causal benefit. No private repository source or prompt went to Jev.

## VCR-39 native Desktop explorer delivery smoke (2026-09-24)

A fresh Desktop `explorer` subagent was assigned a read-only task on the fictional `tinytext` fixture. Its initial-context report named JevCompass advice ID `19b24b54` and tool ID `exec_command` before identifying `tinytext/text.py:4` and `tests/test_text.py:7-8`. The host's safe local metric has the same trace with `SubagentStart / codebase / local / 40.14 ms`. No repository edit, private input, or external service call was requested. This is one correlated native Desktop named-role delivery smoke. It does not establish a matched baseline, recommendation usefulness, worker-role behavior, or a fresh Desktop `UserPromptSubmit` result.

## VCR-161 supplemental Codex setup matched pair (2026-09-25)

A third randomized, isolated Linux CLI 0.155.1 C01/R10 run used `gpt-6-luna` / low, read-only mode, identical fictional OrbitNote fixture and stock `openai-docs` skill in both arms, and no forwarded OpenRouter key. All four arms exited 0 within the 180-second cap. C01 treatment's local `UserPromptSubmit` metric was `local`, 9.96 ms, trace `be80b0ba`; the metrics parser in this revision mislabeled its category as `unknown` because `codex-setup` was absent from its allowlist. The first assistant message did not echo that ID before the first tool, so this run does **not** establish agent-visible delivery. C01 first tool arrived at 6.47 s treatment versus 6.65 s baseline; these are first-tool timings, not productive-action timings. R10 treatment emitted no metric or advice ID, as expected for the routine control.

Four final answers were written only to private mode-0600 files with opaque names under `/tmp/jevcompass-c01-20260925-b/`, with arm mapping separate. A reviewer read the two C01 and two R10 answers without the mapping. Both C01 answers earned 5/5 rubric completeness and safety, 4/5 concise usefulness, 5/5 first useful conclusion; citation accuracy was 4/5 for one and 3/5 for the other. Both R10 answers scored 5/5 across the rubric. After scoring, the map identified the 4/5 citation answer as treatment and the 3/5 answer as baseline. That narrow citation difference in one pair is not a causal benefit; there is no confirmed agent-visible advice, first-productive-action measure, or remote Jev choice. The 20-case core denominator is unchanged and acceptance remains open.

## VCR-167 C02 preflight delivery pair (2026-09-25)

A separate, precommitted C02 pair added the **same** request to both arms to report any JevCompass ID before the first tool. It reused the fictional OrbitNote fixture, stock `openai-docs` skill, isolated read-only Codex CLI 0.155.1 profiles, `gpt-6-luna` / low, and no OpenRouter key. Both arms exited 0. The baseline had no advice ID. Treatment's first assistant message reported ID `440030e5` before its first tool, matching the local `UserPromptSubmit / codex-setup / local / 3.35 ms` metric trace. This establishes agent-visible delivery in this one source-checkout CLI run. First tool arrived at 5.95 s treatment versus 7.71 s baseline; this is **not** first productive action or a reliable speed effect.

Two final answers were exported to separate private, opaque mode-0600 files under `/tmp/jevcompass-c02-20260925-a/`. A reviewer scored them against the precommitted rubric without the arm mapping, then the map was revealed: treatment 4.7/5 and baseline 4.3/5 for answer quality. Both were safe and cited the fictional fixture; the reviewer noted that the treatment final-answer artifact omitted a preflight line, which the event stream separately shows before the first tool. This one pair does not establish repeatability, causal quality improvement, remote Jev choice, Desktop delivery, macOS support, or core-20 acceptance. The arm mapping remains private.

## VCR-170 installed wheel C02 pair and Desktop low-signal control (2026-09-25)

A new C02 pair used the already installed **pipx JevCompass 0.1.12** interpreter, independently verified by package metadata; the treatment hook commands pointed to that interpreter and neither arm inherited checkout `PYTHONPATH` or an OpenRouter key. Both read-only CLI 0.155.1 arms completed against byte-identical fictional fixtures and stock `openai-docs` skill. Treatment's first assistant message reported fresh ID `092e0715` before first tool, matching `UserPromptSubmit / codex-setup / local / 3.89 ms`; baseline reported no advice. First-tool arrival was 9.44 s treatment versus 5.67 s baseline. This contradicts a speed claim for this pair, although first-tool time is still not first productive action.

A separate reviewer scored the two final answers blind against the fictional fixture before seeing the mapping. After unblinding, treatment was 4.2/5 (complete and accurate but less concise, first useful conclusion buried) and baseline 4.7/5 (concise with a minor missing CLI-skill detail). Both were safe. The private answer files and arm map are under `/tmp/jevcompass-c02-wheel-20260925-a/` and remain off Git. This is a negative quality observation from one installed-wheel pair, not proof of general harm. It adds an independently confirmed wheel-delivery smoke, but no remote Jev judgment and no core-20 acceptance credit.

In the current Desktop task, one new `explorer` first reported `NO JEVCOMPASS ADVISORY` before a read-only synthetic lookup. The nearby global product metric was `SubagentStart / codebase / low-signal-skip / 94.75 ms`, consistent with deliberate silence for a lone generic candidate. Without a unique child/session correlation this is a low-signal control, not a fresh Desktop positive-delivery test.

## VCR-171 installed Decisions API synthetic check (2026-09-25)

The installed pipx v0.1.12 wheel independently called OpenRouter Decisions API with a synthetic `review/python/primary` state and two generic tool descriptions (`git`, `exec_command`). The response contained a valid `choice` ID and numeric confidence in 328.2 ms; no user prompt, path, code, memory, auth value, or raw backend response was printed. A source-checkout call with a separate generic review batch also returned two `choice` answers in 486 ms. These establish that the installed HTTP decision client can reach and parse Jev on safe metadata, not that a live Codex hook invoked the remote choice or that Jev improves task outcomes. C02's single `openai-docs` candidate was selected locally and did not use the remote model.

## VCR-172 installed hook to remote Jev and cache smoke (2026-09-25)

The already installed pipx v0.1.12 package processed a synthetic `UserPromptSubmit` event for a generic Python code review using its real `hook_main`. Its cache and metric locations were redirected to a temporary directory in process; the installed package was imported with `python -I`, without checkout `PYTHONPATH` or hook configuration changes. A cold run emitted a nonblocking `hookSpecificOutput` with advice ID `0b53fbae`, a Jev source marker, and `UserPromptSubmit / review / jev / 348.31 ms` in its local metric (368.38 ms wall time). Replaying the same safe event in the same process yielded a new advice ID `0a0893da` and `cache / 50.08 ms` (50.66 ms wall time), with one cache file. A separate cold run gave `jev` in 577.21 ms and exit 0. The request path locally constructs `task_kind`, role, domain and approved catalog candidate descriptions; the synthetic event carried no private code, path, memory, credential or client data. No raw backend response was printed or committed.

This confirms the released hook can invoke a remote Jev decision on an eligible local candidate set and reuse it from cache. It does not prove Codex agent delivery for this remote advice, first productive action, useful task outcomes, macOS behavior, or the 20-case acceptance threshold.

## VCR-173 installed remote Jev advice before first CLI tool (2026-09-25)

A new C03 runner case created a tiny fictional Python `counter.py` with `return value - 1` and installed the same two reviewed skill candidates in fresh baseline and treatment profiles. The installed pipx 0.1.12 hook, isolated CLI 0.155.1, `gpt-6-luna` / low and read-only sandbox were used; the checkout was removed from `PYTHONPATH`. The OpenRouter key was forwarded only to the treatment arm by explicit `--allow-openrouter-key`, so this is a delivery smoke, **not** a matched effectiveness experiment. Dry run confirmed equal fixture and candidate-skill digests and no model call. All 199 Python 3.11 tests passed; graphify was rebuilt with its configured interpreter.

Both live arms exited 0. Baseline reported no advice. Treatment's first assistant message reported fresh ID `6d93a5fc` before its first command; the isolated metric matched `UserPromptSubmit / review / jev / 504.88 ms` with the same trace. It did **not** repeat candidate IDs (`agent_reported_candidate_ids=[]`), so this proves ID delivery of a remote-ranked advisory, not that either skill was named or applied by the agent. First-tool arrival was 5.17 s baseline versus 6.41 s treatment; no speed improvement is shown here. Answers and raw backend output were excluded from receipts. Fresh Desktop remote advice, skill usage, first productive action, macOS behavior and core-20 acceptance remain unverified.

## VCR-174 two additional blind C03 review observations (2026-09-25)

Two more isolated, read-only C03 CLI pairs ran with installed pipx v0.1.12, `gpt-6-luna` / low and identical fictional `counter.py` plus the same two reviewed skills in both arms. Both arms completed in each pair. Treatment advice IDs `9294ca29` and `734a28f9` appeared before first tool and matched fresh `UserPromptSubmit / review / jev` metrics (590.29 and 314.20 ms); neither baseline reported advice. The first treatment repeated no candidate IDs; the second repeated `code-review-excellence`. Neither fact proves the skill was opened or used. First-tool arrival was 5.33 vs 6.59 s in pair A and 4.97 vs 6.44 s in pair B (baseline vs treatment); first productive action was not measured.

Four final answers were exported to mode-0600 opaque files under `/tmp/jevcompass-c03-blind-20260925-a/` and `-b/`, with arm mappings separate. A reviewer received only the four unlabeled answers and the synthetic fixture specification. Before reading the mapping, the reviewer rated all four factual findings 5/5. In pair A, one answer identified the line-2 decrement without the fix (clarity 4/5), while the other provided the correct `value + 1` fix (5/5); the latter mapped to treatment. In pair B, both gave the correct finding and fix; one used an unsupported-looking fixture link (baseline), the other added unrelated `NO JEVCOMPASS ADVISORY` text (treatment). Both were 4/5 for concise usefulness. This is mixed, narrow quality evidence, **not** repeatable superiority: the key was forwarded only to treatment, the prompt explicitly requested an advice preflight, the task is trivial, and no skill-read or first-productive-action evidence was collected. The core-20 acceptance denominator remains unchanged.

## VCR-175 first verified skill-file and target-file reads (2026-09-25)

The C03 runner now records only successful completed reads of the exact fictional `counter.py` and the two fixed candidate `SKILL.md` paths, with elapsed time and allowlisted IDs. It recognizes a single direct `cat`, `head`, `tail`, `sed`, or `nl` command, one exact `bash`/`sh -lc` wrapper, and at most two recognized reads joined by `&&`; listings, failed commands, other operators, nested wrappers, and arbitrary scripts do not count. It never stores raw commands or paths. An initial live run yielded null on both arms because actual CLI commands used a shell wrapper and `nl`; a structural-only diagnostic identified the command form. After adding those narrowly recognized forms, 203 tests passed and graphify rebuilt.

A subsequent fresh installed-wheel C03 pair completed in both arms. Baseline's first verified `counter.py` read was at 8.54 s. Treatment reported Jev ID `3b0679b0` before first tool, correlated to `UserPromptSubmit/review/jev/550.15 ms`, then read the installed `code-review-excellence/SKILL.md` at 26.48 s and `counter.py` at 26.49 s. No candidate ID appeared in its first assistant message. This verifies a skill-file read, not actual application of its instructions. It is one run with treatment-only API key and a tiny target; it contradicts a speed claim for this run but does not isolate the cause or establish an average. The agent was shown a capability description without the catalog's `use_when`/`avoid_when` criteria; a follow-up should test whether concise scope criteria reduce unnecessary skill reads without hiding useful guidance.

## VCR-176 skill use/skip criteria in advice (2026-09-25)

The proposed advisor context now includes the approved catalog's short `use_when` and `avoid_when` criteria for selected skills, and asks the agent to inspect task scope before opening a skill. An oversized criterion is omitted; if several criteria exceed the 1,500-character context cap, the adapter falls back to the original bounded advice rather than staying silent. Three current-profile context lengths were 1,016 characters for review/Python (four candidates), 565 for Codex setup (one stock skill), and 1,010 for Python package documentation (two candidates). No private prompt or backend free text is included. The pilot runner now labels a version-checked interpreter `installed-distribution` because it cannot authenticate the wheel's published origin.

A wheel built from this checkout and installed into an isolated Python 3.11 environment retained distribution version 0.1.12 but is **not** the public v0.1.12 artifact. A direct installed-module check confirmed the review skill's `use when`/`skip when` text. Three fresh synthetic C03 CLI pairs used that local wheel, isolated homes, a treatment-only OpenRouter key and `gpt-6-luna` / low. All arms exited 0; treatment advice IDs before tools correlated with `review/jev` metrics at 562.44, 385.09 and 344.79 ms. No candidate `SKILL.md` read was detected in any treatment. Verified `counter.py` reads were baseline/treatment 9.93/9.03 s, 5.09/8.17 s, and 9.04/9.43 s. This small, non-equivalent comparison does not prove the criteria caused the absence of skill reads or improved speed. The earlier published-wheel observation of a skill read before source inspection remains a motivation, not a controlled baseline. A more substantial task and equal environment are still needed for efficacy.

## VCR-177 C04 equal-environment review pairs (2026-09-25)

C04 tests the published v0.1.13 wheel in two fresh, randomized, read-only Codex CLI pairs against a fictional retry client, contract and green baseline tests. Both arms received byte-identical fixture/skill copies, the same `gpt-6-luna` / low configuration and the same presence of `OPENROUTER_API_KEY`; only treatment registered JevCompass. This fixes C03's unequal-key comparison. The key and raw source, prompts, commands, answers and backend responses are absent from public receipts. Anonymous answers were scored against the prewritten [rubric](tests/evaluation/c04_expected.md) before revealing the arm map. The three seeded defects are broad exception retry, ignored numeric `Retry-After`, and sleep after a final HTTP attempt.

Both pairs completed with exit 0. Treatment reported a fresh Jev advice ID before its first tool, matching `UserPromptSubmit/review/jev` at 597.64 ms and 503.15 ms; baseline reported no advice. Neither treatment first message repeated a candidate skill ID. In pair 1, both arms described all three real defects and proposed focused tests, but treatment cited the correct source lines for all three (strict rubric 3/3) while baseline cited incorrect lines (0/3). In pair 2, both arms again described all three defects with focused tests, but both cited incorrect lines (0/3 each). The first pair therefore favors treatment on line accuracy, and the second ties; two small pairs do not establish a repeatable benefit or explain causality.

The conservative action counter recognized no fixture or candidate-skill read in pair 1; in pair 2 it recognized a successful baseline read of all three target files at 7.68 s and no treatment target read. These nulls mean the command forms were not recognized or no qualifying read occurred; they must not be interpreted as no inspection or as a first-productive-action comparison. First-tool arrivals were baseline/treatment 5.48/7.20 s and 5.57/7.36 s, but a first tool is not necessarily useful work. No skill file read was verified and an advice ID alone does not prove the recommendation was used. C04 is supplemental and does not change the frozen core-20 denominator or acceptance status.

## VCR-179 command-shape diagnostic and additional C04 pairs (2026-09-25)

A fixed-enum, capped diagnostic now counts successful command forms that the conservative exact-file reader did not match. It stores only `search`, `read`, `python`, `compound` or `other` counts, without raw commands, arguments or paths. A narrowly validated `cd` into the exact synthetic fixture followed by one recognized read is accepted; other directory changes and arbitrary command chains remain excluded. Local tests verify both recognition and redaction.

Two further randomized equal-key C04 pairs on the installed public v0.1.13 wheel completed. Jev advice IDs `407ff7bd` and `46ea8d79` correlated before first tool with `review/jev` hook durations 578.13 and 519.53 ms. The diagnostic reported two unmatched successful `compound` commands in **each** arm of both pairs, including after the narrow `cd` recognition. It therefore identifies an instrumentation gap, not absence of source inspection. No candidate-skill read was verified. First-tool times were baseline/treatment 6.33/7.70 s and 6.00/6.47 s; without a recognized first productive action these are not speed outcomes.

A blind reviewer again found that all four answers identified the three seeded defects and proposed focused tests. Strict file/line rubric scores were baseline/treatment 3/3 vs 0/3 in both new pairs. Across all four C04 pairs the exact-line score favored treatment once, baseline twice, and tied once; all eight answers identified all three defects semantically. This does not show a repeatable quality advantage from Jev; the actual selected skill use is still unverified. Further remote-choice trials on the same fixture have diminishing value until a task exposes a real decision between tools or skills and action telemetry can observe it.

## VCR-195 persisted child evidence and separate delivery outcome (2026-09-25)

Codex CLI 0.155.1 `--ephemeral` suppressed child rollout files, so the original matched-pair harness could not observe child events in the parent JSON stream. A single non-ephemeral run in a disposable `CODEX_HOME` exposed separate root and child JSONL files. The child metadata's `forked_from_id` matched the root session ID; the child nickname/path reflected the task name rather than the literal `explorer` role. The harness now inspects only files in its temporary profile and discards raw content on exit. It requires exactly one linked child, bounded developer-context advice before the first tool, a unique spawn-hook trace, and matching IDs. Other default-hook IDs cannot satisfy the spawn correlation. Its overall task status additionally checks ordered skill and fixture references. Host-schema drift or missing evidence remains inconclusive.

A single treatment smoke confirmed ID `18aa635f` in the child's initial context and matching PreToolUse metric, with the focused-test skill named and skill/fictional-brief paths observed in successive tool requests. A subsequent randomized `gpt-6-luna` pair exited 0 in both arms. The baseline had no spawn metric and ordered skill/brief path references appeared in its tool requests (36.38 s). The treatment's child had ID `6c92d87e` before the first tool, matching its sole spawn metric and naming `jevcompass-focused-tests` (27.28 s); the skill reference appeared, but the brief read was not observed. Thus **spawn advice delivery was confirmed for that treatment, while the complete paired task remained inconclusive**. These elapsed times are not first-productive-action measurements. The prompt itself asked the child to read the skill, so this test cannot establish that Jev changed its choice or improved its output. No raw transcript, credential, or prompt was published, and this pair does not count toward core-20 acceptance.

## VCR-194 offline matched spawn-advice harness (2026-09-25)

Run `python3 scripts/pilot_spawn_advice_pair.py --dry-run` to verify two isolated synthetic CLI profiles before any model call. Both arms receive byte-identical fictional webhook requirements and the reviewed `jevcompass-focused-tests` skill; the treatment alone adds the narrowly matched opt-in spawn hook. The command prints hashes, hook parity and a `dry_run_verified` status. Nine focused Python tests pass. The full Python 3.11 suite also passed (240 tests, including these tests); graphify was rebuilt. No live pair was run.

A later explicit `--run --model MODEL` copies the current Codex CLI authentication into private temporary profiles, randomizes arm order, caps each run at 120 seconds and discards raw event streams. The summary contains only bounded IDs and booleans. Its child-delivery result is *inconclusive* unless a child ID and explorer marker, advice before the child's first tool, and an ordered skill then fixture read are observable in that child's own stream; a treatment advice ID must correlate to the local hook metric. Some Codex CLI versions may not expose child-level stream events. A successful dry run is instrumentation readiness only, not host delivery, skill use, or a quality gain.

One randomized live `gpt-6-luna` pair on Codex CLI 0.155.1 used the same synthetic fixture and skill in each arm, with `--timeout 120`. Baseline exited 0 in 36.54 s and treatment exited 0 in 19.74 s; treatment's PreToolUse metric was observed. The parent `codex exec --json` stream exposed neither a spawn tool event nor an identifiable child event in either arm. The harness returned exit 1 and `inconclusive`, correctly withholding a delivery claim. The times are whole-run durations from one pair, not time-to-first-useful-action, and cannot support a speed comparison. The temporary profiles were removed; no raw transcripts were published. Next isolate and inspect a child-owned session transcript under a disposable Codex home without printing raw events.

## VCR-182 opt-in spawn advice delivery probe (2026-09-25)

An isolated, temporary Codex CLI 0.155.1 profile registered the two default hooks plus a **separately opted-in** `PreToolUse` hook matched only to `Agent`, `spawn_agent`, or the host's observed `collaborationspawn_agent`. The profile copied authentication without printing it and used only a fictional `review.py` fixture. A temporary diagnostic recorded tool names and input shapes, never command or task contents: `Bash` and `collaborationwait_agent` appeared but did not match the product's narrow hook; the spawn event had `task_name`, `message` and `fork_turns`. The child message was an opaque single token, while the descriptive `review_python_correctness` title supplied the local `review/python` classification. This revises VCR-06E's conclusion for this host **only**: a readable message is not guaranteed, but a descriptive task name can carry a bounded intent signal.

The source-checkout product hook emitted nonblocking ID `8476cc9f` with `PreToolUse / review / local / 9.2 ms`; it did not remotely rank candidates in this trial. The child's own persisted session transcript contains that exact ID in a developer-context item **before** its first assistant message and first tool. The child explicitly reported `exec_command` and `security-requirement-extraction` with the ID before using a tool. The parent's final relay incorrectly said `NO JEVCOMPASS ADVISORY`, so the conclusion uses the child transcript and correlated metric, not the relay. A separate fixed-marker control reached the child as well. There was no observed candidate skill-file read or evidence of improved result quality. The isolated test profile's broad diagnostic hook was removed; no production hook configuration changed. Desktop spawn coverage, efficacy, host variations and a published wheel remain unverified. This is a delivery experiment, not core-20 acceptance credit.

## VCR-198 conditional skill-read C05 pair (2026-09-25)

One supplemental randomized C05 CLI pair used a locally built, installed JevCompass distribution from source commit `615e4ec` (its metadata remains 0.1.15; this is **not** the published v0.1.15 wheel). Both isolated, read-only arms received the same fictional webhook brief and copies of `create-plan` and `security-requirement-extraction`, the same `gpt-6-luna` / low setting and equal OpenRouter key presence. Only treatment registered JevCompass hooks. The source change conditionally asks an agent to read a matching `SKILL.md` before drafting or editing. The dry run verified identical fixture and candidate digests; both live arms exited 0. Treatment read `create-plan` at 7.29 s together with the brief; baseline read no candidate skill and read the brief at 5.79 s. This is one observed skill read, not a general skill-use rate or a faster first useful action.

An independent reviewer scored only opaque answers against the frozen 7-point rubric, without access to the arm map. Both scored **6/7**, were factually sound and actionable, and omitted explicit exact-at-limit test values. The score file was frozen at SHA-256 `8906a9d8b4407c67023ab8ef4e9b8b48b3a37e1074aa0f32699adc333d28de72` before opening the mapping. Private answers, mapping and receipts remain under `/tmp/jevcompass-c05-v197-live-20260925a/`. The pair shows a skill read in treatment and equal blind quality, but does not isolate the wording's causal effect or establish speed and quality improvement. It is outside the frozen core-20 denominator; Desktop and macOS remain unverified.

## VCR-196 C05 on public v0.1.15 (2026-09-25)

A supplemental, randomized C05 signed-webhook planning pair used the installed public v0.1.15 distribution in two isolated, read-only Codex CLI profiles. The fictional brief, two candidate skills (`create-plan`, `security-requirement-extraction`), `gpt-6-luna` / low setting and OpenRouter key presence were identical. Only treatment registered the two JevCompass hooks. Both runs exited 0. Treatment advice ID `298127d8` correlated with `UserPromptSubmit / planning / jev / 569.68 ms` before its first tool; its initial report repeated no candidate skill ID, and neither arm read a candidate `SKILL.md`. The first confirmed brief read occurred at 5.18 s baseline versus 15.78 s treatment. This is one observation, not a causal latency estimate.

An independent reviewer saw only opaque answer files and the frozen 7-point rubric. Both were scored **7/7**, factually sound and actionable. The score file was frozen at SHA-256 `31586bb01afeaf2b18a95219a21c444c4b1564255535f3da9442eabf0eb78daa` before the separate arm mapping was opened: the treatment and baseline tied. Private synthetic answers, mapping and receipts remain under `/tmp/jevcompass-c05-v015-20260925a/`. This pair confirms CLI advice delivery before first tool, but not skill application, speed improvement or a quality advantage. It is outside the frozen core-20 denominator; Desktop and macOS acceptance remain open.

## VCR-181 C05 meaningful skill choice pair (2026-09-25)

A locally prewritten, supplemental C05 case compared a signed-webhook planning task with two genuinely available skills, `create-plan` and `security-requirement-extraction`, in isolated, read-only Codex CLI profiles. The published v0.1.13 distribution, fictional brief, copied skills, `gpt-6-luna` / low and presence of the OpenRouter key were equal in both randomized arms. Only treatment registered the two JevCompass hooks. Both runs exited 0. The treatment reported fresh advice ID `201ba817` before its first tool, correlated with `UserPromptSubmit / planning / jev / 554.34 ms`; its first message repeated no candidate ID, and no candidate `SKILL.md` read was observed. The treatment's first verified brief read was at 6.38 s versus 5.80 s baseline. This narrow source-read measure shows no speed gain in the pair and does not prove that the suggested skill was used.

An independent reviewer scored the two opaque final answers against the 7-point rubric before receiving the arm mapping. Both scored **6/7**: each covered signature checking, replay protection, payload boundaries, reversible rollout, local validation and grounding; each omitted an explicit record of which rotated key verified a request. Both were factually sound and actionable. No quality winner or unsupported claim was identified. The anonymous answers and separate arm map remain private under `/tmp/jevcompass-c05-first-20260925/`. This is one synthetic CLI pair outside the frozen core-20 denominator; it establishes remote advice ID delivery for a real candidate choice, but neither actual skill application nor repeatable speed or quality improvement. Fresh Desktop positive delivery and macOS runtime are still unverified.

## VCR-180 role-only Desktop signal and skill prerequisite (2026-09-25)

A new native `explorer` subagent was asked to report its initial advice ID before the first tool and then answer a specific read-only codebase question. It reported `NO JEVCOMPASS ADVISORY` before tools and completed the question. A contemporaneous local metric recorded `SubagentStart / codebase / low-signal-skip` in 49.48 ms. The metric has no child-visible ID to correlate, so this indicates a plausible intentional abstention rather than proving delivery or failure for that precise child. The installed host package was still v0.1.12. Role-only `SubagentStart` has no task description; a vanilla profile with only the generic shell is therefore silent by design. Fresh Desktop prompt delivery and useful role-specific advice remain open.

Catalog inspection also found a concrete false-availability risk: the reviewed `browser-testing-with-devtools` skill's own metadata says Chrome DevTools MCP is required, while discovery previously checked only for its `SKILL.md`. The source catalog now records that prerequisite. With no such server, the skill is unavailable; a configured server makes it `configured` only, which the advisor excludes from automatic ranking until the session can confirm tool access. This change is opt-in until a new published release. [Codex hook documentation](https://learn.chatgpt.com/docs/hooks) confirms that `SubagentStart` receives the role but not the task text and that `PreToolUse` can match `Agent`/`spawn_agent`; any proposed narrow spawn-time experiment needs separate host coverage and must never gate routine commands.

## VCR-189 optional bundled-skills paired pilot (2026-09-25)

Two randomized, synthetic Codex CLI matched pairs compared fresh profiles with and without the wheel-installed optional `jevcompass-focused-tests` and `jevcompass-regression-review` skills. Both arms used `gpt-6-sol` / medium, read-only fixtures and a 90-second cap. All four runs exited 0. No Jev provider key or hook was involved. A blind reviewer scored focused-test planning **4/4 versus 4/4** and regression review **4/4 versus 4/4**; the review arms both found the broad `BaseException` handling defect and proposed a relevant test. Treatment named bundled skill IDs before its first tool, but there was no Jev advice ID. First-tool times were 10.16/12.45 s for the test task and 11.79/14.50 s for review (baseline/treatment). These are tool-arrival times, not productive-action times. Both test arms could not import `_pytest` and correctly reported failed collection; their test execution quality was therefore unresolved. Blind scores were frozen before unblinding (SHA-256 `138ca8e585d64405e530cec9e8bddd80c87716186d507cafb57306e336660bc1`). Private synthetic transcripts and arm mapping stay outside Git under `/tmp/jevcompass-skill-pair-20260925.PMGery/`.

A separately bounded follow-up repeated only the focused-test pair with a Python stdlib `unittest` fixture, byte-identical across arms (fixture SHA-256 `d04c60abcd0c6fab10df83213c62ded2d79a60ba36f617944d8dfc725cac57ca`). Both runs exited 0 within 90 seconds and chose the same checks: focused 3 tests passed, then full 4 tests passed, with exit 0 for each command. Blind scores again tied **4/4 versus 4/4**. First useful focused validation completed at **22.92 s baseline versus 29.04 s treatment**. Treatment reported both skill IDs before its first tool and no Jev advice ID. Local receipts are under `/tmp/jevcompass-unittest-followup-20260925.pKHQc5/`. The small pairs show skill discoverability but no measured quality gain or faster validation. They do not establish general efficacy, Desktop behavior, or macOS support. The optional skills remain opt-in and are not in PyPI 0.1.15.

## VCR-191 one-skill plus real-hook coding pair (2026-09-25)

A subsequent randomized synthetic Codex CLI 0.155.1 pair compared a baseline fresh profile to one with JevCompass's two default hooks and only the optional `jevcompass-focused-tests` skill. Both used the same fixture, `gpt-6-luna` / low settings, a 120-second cap, and workspace-write scope. The treatment's real `UserPromptSubmit` hook emitted a locally assembled coding advice ID `aaa44d59` before its first tool; the correlated `coding/local` metric took 10.91 ms. The first response did not name candidate IDs, and no skill file read appears in the captured treatment commands. A separate local `recommend --category coding --domain python` call against the same isolated profile (with the OpenRouter key unset) listed `exec_command` and `jevcompass-focused-tests`; this reconstructs available candidates, not the exact historical hook payload. The advice ID proves hook context delivery but does not prove that the skill was selected or used. No OpenRouter key was supplied.

Both arms completed. Under a frozen four-point rubric, baseline scored **3/4** and treatment **2/4**. Baseline ran the README-required full `python -m unittest discover -s tests` suite and passed ten tests after an initial discovery miss. Treatment's focused and full `pytest -q` checks passed, but it did **not** run the repository-required unittest command. The scorer operated both arms, so this is not an independently blinded efficacy comparison. The result is a concrete omission under treatment, not proof that JevCompass caused it, and it fails the required-check acceptance gate for this pair.

The trial had two containment defects: a treatment shell listing briefly crossed the fixture boundary before correction, and its hook initially used the real HOME for one local metric. The worker removed the uniquely traced metric and verified its absence. No repository files changed. The randomized mapping, rubric, synthetic transcripts, scores, and cleanup hashes are local under `/tmp/jevcompass-focused-pair.5TnRSt/`. Future pairs must give each arm a separate temporary HOME and Codex home, retain authorization within the isolated profile, and use fixture-contained commands. This pair does not support publishing the optional skill as a productivity or quality improvement.

## VCR-192 fresh Desktop explorer abstention on current host (2026-09-25)

After the active pipx installation was refreshed to public JevCompass 0.1.15, a new native Desktop `explorer` child received a read-only codebase question and was instructed to report its initial JevCompass advice status **before its first tool**. It reported `NO JEVCOMPASS ADVISORY`, then correctly located the local `_context` implementation and its ID validation. The most recent local `SubagentStart` metric after that probe was `codebase / low-signal-skip / 90.3 ms`. The skip has no advice ID for exact child-to-log correlation, so this supports intentional abstention under insufficient role-only candidates, not a delivered recommendation or proof of host ordering. No key or paid request was used. A prior named-role Desktop positive smoke in a different candidate setting remains separate; fresh Desktop prompt-hook delivery and broad subagent usefulness are not established.
